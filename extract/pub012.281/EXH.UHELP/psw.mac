PSW

PSW is a privileged, interactive command to list and modify the system
PASSWORD data set.

While the system password protectin mechanism is still functional, in
practive it has been superceded by RACF (and on MVS, RAKF).
