ATIX
ATN

This routine displays, by attention index, all units having that
index. When a program (usually a Start task) controls that index,
the ASID and jobname are displayed. The module name is shown if
it is available (for MVS and earlier systems, nucleus CSECTs are
not shown).
