SUB   ª dsnameª(member)³³

SUBmit presents a full-screen menu allowing the user to specify an
input data set, member for a PDS, and volume serial for an
uncataloged data set.

The requested member or data set is submitted to an internal reader.
and destination or writer name. After parameters have been
checked, hit ENTER to effect submission.
