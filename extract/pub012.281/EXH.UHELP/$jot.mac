$JOE < class × jobnumber >
$JOT

The $JOE and $JOT displays access the JES2 Job Output Table and
Elements. $JOT returns the table address and a count of used elements.

$JOE displays individual JOEs (to be written).
