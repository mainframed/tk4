# is a command prefix. The text following the pound sign is sent
to the Operating System as an operator command. The function
is restricted by CRT and privilege level.
The # command sends any text following it to OS as an operator
command. All commands other than K(Control) are permitted.
#CP is a special form which sends a command to VM.
# text - send "text" to OS as a command.
the text may start with $ for HASP/JES2
and * or 8 for ASP/JES3 commands.
#CP text - send "text" to VM as a command.
Required - non-blank input.
Any command other than K(Control) following #, e.g. #HALT EOD
