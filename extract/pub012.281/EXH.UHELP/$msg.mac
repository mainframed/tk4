$MSG jobname × number <,options...>

This function displays summary information for a JES2 job.

The summary includes the basic JCT information (time on, off, output
volume, etc.), then excerpted JCL, /*, and //* data. For a job that
has completed execution, step completion and other selected
messages are displayed.

No data are displayed unless the requesting CRT is set to some
privilege level (e.g. Primary CRT, UNLock, PASsword, etc.).
