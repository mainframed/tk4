DMP

DMP reads a few blocks of the dump data sets (SYS1.DUMP00 through
SYS1.DUMP02), and displays whether they are empty. For used data sets it
shows the date and time of the dump, the source (e.g., SVCDUMP),
and the comments field.
