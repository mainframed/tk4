EXIT

EXI provides a convenient way for a remote user to end an Exhibit
session. It may also be used for local network session, but should not
be used if they are intended as "permanent" displays. The
request causes a confirmation message to press ENTER to terminate.
