UNLOCK hush

The UNLOCK function enables restricted modules such as ALL,
ALT, SCR, etc. The UNLOCK command is accepted only from units
defined as Operator CRTs; it is rejected from devices used as
Secondaries, such as the Tape Library CRT.

The input specifications are restricted to supervisory
operations staff who can be trusted ?
