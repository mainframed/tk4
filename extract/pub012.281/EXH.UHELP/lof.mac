LOF   ª dsnameª(member)³³

ListOFfline presents a full-screen menu allowing the user to specify
an input data set, member for a PDS, and volume serial for an
uncataloged data set.

For output, the user may specify the output class, form, copy count,
and destination or writer name. After parameters have been
checked, hit ENTER to effect printing. Note that the job separator
will be for the current task.
