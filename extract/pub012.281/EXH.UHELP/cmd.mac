CMD member × mask ª ,nth ³

The CMD program searches for the requested member in all libraries
specified in the OPTions data set under the ªCLISTS³ group. When the
member is found, a UPD display is invoked.

The request may use a mask, using ? as a single character match, and *
as a terminal match.

The optional "nth" value is an integer asking to retrieve the nth
occurrence of a match
