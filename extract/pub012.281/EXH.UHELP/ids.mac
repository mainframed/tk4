IDS

The IDS command displays the CPU model, hardware serial and version;
and shows the channel type and model for all active channels.
This command is not valid on a 360 type CPU; it will function only
on a 370, 303x and 40xx and compatible CPUs.
