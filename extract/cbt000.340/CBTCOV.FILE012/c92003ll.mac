%TUTORIAL ------------  DEFINE CLUSTER - DATA COMPONENT  ---------------
%NEXT SELECTION ===>_OPT                                %
+
   VALID REPLIES FOR DATA COMPONENT PARAMETERS ARE:
      %CI FREESPACE+..SPECIFIES THE AMOUNT OF FREE SPACE (AS A PERCENTAG
                      LEFT IN EACH CONTROL INTERVAL. MUST BE IN RANGE 0
                      DEFAULT -%0.+
      %CA FREESPACE+..SPECIFIES THE AMOUNT OF FREE SPACE (AS A PERCENTAG
                      LEFT IN EACH CONTROL AREA. MUST BE IN RANGE 0 - 10
                      DEFAULT -%0.+
      %AVERAGE RECORD LENGTH+- SPECIFIES THE AVERAGE RECORD LENGTH.
      %MAXIMUM RECORD LENGTH+- SPECIFIES THE MAXIMUM RECORD LENGTH.
                      DEFAULT - AVERAGE RECORD LENGTH.
      %KEY LENGTH+....SPECIFIES KEY LENGTH FOR INDEXED CLUSTERS.  MUST B
                      RANGE 1 - 255.
      %KEY OFFSET+....SPECIFIES THE DISPLACEMENT FROM THE BEGINNING OF A
                      WHERE THE KEY BEGINS.
                      DEFAULT -%0.+
      %VOLUMES+.......SPECIFES THE VOLUME/S WHERE THE DATA COMPONENT IS
                      RESIDE.
)INIT
&ZHTOP = V00T
&ZHINDEX =V00TI
)PROC
)END
