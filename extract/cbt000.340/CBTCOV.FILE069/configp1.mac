*
*     DYNAMIC PARTIONING -- USED BY  'CF MEMBER(PP)' OPER COMMAND
*
*     THIS MEMBER WILL INITIATE DYNAMIC PARTITIONING FROM SINGLE IMAGE
*     MODE TO PARTIONED MODE. SIDE 1 WILL BE PARTITIONED OFF.
*     MAKE SURE ANY DEVICES CONNECTED ONLY TO SIDE 1 ARE VARIED OFFLINE
*     PARTITIONING MUST BE COMPLETED ON "PARCTL" FRAME ON SYSTEM CONSOLE
*
         CHP(ALL,1),OFFLINE,UNCOND   /*  SIDE 1 CHANNELS OFFLINE     */
         CPU(3,4),OFFLINE            /*  SIDE 1 CP'S OFFLINE         */
         ESTOR(E=2),OFFLINE          /*  2ND 64 MEG EXT. STOR. OFF   */
         STORAGE(E=2),OFFLINE        /*  3RD 32 MEG CENT STOR. OFF   */
         STORAGE(E=3),OFFLINE        /*  4TH 32 MEG CENT STOR. OFF   */
