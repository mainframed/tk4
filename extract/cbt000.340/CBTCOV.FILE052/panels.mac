./ ADD NAME=TMAILCFD 0118-89313-93152-1249-00040-00027-00000-$A1238
)Attr
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
   ! type(output) intens(low ) caps(on) just(asis ) color(yellow)
   # type(text) intens(high) caps(on) just(asis ) color(yellow)
   ` type(text) intens(high) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+-------------------% TSO/E-Mail File Delete Confirmation +-------------
%Command ===>_zcmd
%
%
%     Current Data Set Name:  !mailtds
%
%     Delete the above data set:===>_dmopt+Yes  - delete data set and me
%                                         +No   - delete message only
%                                         +File - delete only the data s
%
#    !mailsubj
#           Userid:!mailfid
#             Node:!mailfloc
%
%     Press 'END' or 'PF3' now to prevent the delete and thus retain the
%     file.
%
%     Or use the#DM%option to delete the message and retain the data set
%     Or use the#DF%option to delete the data set and retain the message
%
)Init
  &zcmd = &z
  &dmopt = Yes
)ReInit
  &zcmd = &z
  &dmopt = "Yes"
)Proc
 &dmopt = trans(trunc(&dmopt,1) Y,YES N,NO F,FILE *,'?')
 ver (&dmopt,list,YES,NO,FILE)
)End
./ ADD NAME=TMAILCML 0111-91169-93148-1238-00037-00029-00000-$A1238
)Attr
   @ type( input) intens(high) caps(off) just(left ) color(white)
   ^ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
   ! type(output) intens(low ) caps(off) just(asis ) color(yellow)
   # type(text) intens(high) caps(on) just(asis ) color(yellow)
   ` type(text) intens(high) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+------------------------% TSO/E-Mail Change Display +------------------
%Command ===>_zcmd
%
%
`  Mail from:!fid
         `on:!maildatd !mailtime
%
`  Mail subject:
%
#==>@mailsubj
%
#==>@mailactn   +
%
`   Change the subject and status if you wish, then press 'ENTER" to
`   continue or press 'PF3' to cancel the update.
%
%
%
)Init
  &zcmd = &z
  .cursor = mailsubj
 if (&mailfloc NE &z)
   &fid  = '&mailfid at &mailfloc'
 if (&mailfloc = &z)
    &fid  = &mailfid
)Proc
)End
./ ADD NAME=TMAILCPY 0118-89334-93148-1238-00056-00047-00000-$A1238
)Attr
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
   ! type(output) intens(low ) caps(on) just(left ) color(yellow)
   | type(output) intens(high) caps(off) just(left ) color(white)
   # type(text) intens(high) caps(on) just(left ) color(yellow)
   ` type(text) intens(high) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+---------------------------% TSO/E-Mail File Copy +--------------------
%Command ===>_zcmd
|maillmsg
%
%     Current Data Set Name:  !mailtds
%
%     Target Data Set Name ===>_newname
%              Disposition ===>_newdisp+   NEW, OLD, SHR or MOD
% Replace existing members ===>_newrep+    optional^Yes+or^No+(default i
%
% Generate ISPF Statistics for new member ===>_newstat^Yes+or^No+
%
% Note:  If^NEW%is specified then the data set will be created using the
%        allocation attributes as the input data set.
%
#    !mailsubj
#           Userid:!mailfid
#           Node:  !mailfloc
%
%     Press^Enter%to copy or^END%to terminate Copy process.
%
)Init
 .HELP = TMAILHC
 IF (&newname = &z)
  .CURSOR = newname
 IF (&newname NE &z)
  .CURSOR = newdisp
  &newrep  = trans(&newrep  &Z,YES *,*)
  &newdisp = trans(&newdisp &Z,NEW *,*)
  &newstat = trans(&newstat &Z,Yes *,*)
)Proc
 VER (&newname,nb,dsname)
 &newdisp = trans(trunc(&newdisp,1)
                  N,NEW
                  M,MOD
                  S,SHR
                  O,OLD
                  *,*)
 VER (&NEWDISP,NB,LIST,NEW,SHR,OLD,MOD)
 &NEWREP = TRANS(TRUNC(&NEWREP,1) Y,YES N,NO &Z,YES)
 &NEWSTAT = TRANS(TRUNC(&NEWSTAT,1) Y,YES N,NO &Z,YES)
 VER (&NEWREP,NB,LIST,YES,NO)
 VER (&NEWSTAT,NB,LIST,YES,NO)
 VPUT (NEWREP NEWDISP NEWSTAT) PROFILE
)End
./ ADD NAME=TMAILDEF 0126-89347-93148-1238-00071-00044-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ~ TYPE(INPUT) CAPS(OFF) INTENS(HIGH) COLOR(yellow) hilite(uscore)
   ` TYPE(INPUT) CAPS(ON)  INTENS(HIGH) COLOR(yellow) hilite(uscore)
)Body
+---------------------------% TSO/E-Mail Defaults +---------------------
%Command ===>_zcmd
%
^     Verify your TSO Mail Processing Defaults:
+
^     Your Name:          ~SENDFRM1                         +
^     Mailing Address:    ~SENDFRM2                               +
^     Other Info:         ~SENDFRM3                               +
+
^     Current Node:       `SENDCTR +
+
^     Should the Message be logged:          `Z  + (Yes or No)
^     Do you want your message acknowledged: `Z  + (Yes or No)
+
^     Retain 3 Mail Logs for Debugging:      `Z  + (Yes or No)
+
^     Printing Information for Messages:
      (if blank you will be prompted each time):
^     Printer Name:     `TSOMPRT +
^     Optional:  Chars: `Z    ^   FCB:   `Z   ^  Form: `Z   +
^                Class: `Z^       Title: `Z  +   (Yes or No)
+
)Init
  .ZVARS = 'TMAILLOG TMAILACK TMDEL PRTCHAR PRTFCB PRTFORM PRTCLASS PRTH
  &TMAILLOG = TRANS(TRUNC(&TMAILLOG,1) Y,Yes N,No &Z,Yes *,*)
  &TMAILACK = TRANS(TRUNC(&TMAILACK,1) Y,Yes N,No &Z,Yes *,*)
  &TMDEL    = TRANS(TRUNC(&TMDEL,1) Y,Yes N,No &Z,No *,*)
  &PRTHEAD  = TRANS(TRUNC(&PRTHEAD,1) Y,Yes N,No &Z,Yes *,*)
  &PRTCLASS = TRANS(&PRTCLASS &Z,A *,*)
  .HELP = TMAILHD1
)Proc
    VER (&SENDFRM1,NB)
    VER (&SENDFRM2,NB)
    VER (&SENDFRM3,NB)
    VER (&SENDCTR,NB,NAME)
    &TMAILLOG = TRANS(TRUNC(&TMAILLOG,1) Y,Y y,Y n,N N,N &Z,Y *,*)
    &TMAILACK = TRANS(TRUNC(&TMAILACK,1) Y,Y y,Y n,N N,N &Z,Y *,*)
    &TMDEL    = TRANS(TRUNC(&TMDEL,1) Y,Y N,N &Z,N *,*)
    &PRTHEAD  = TRANS(TRUNC(&PRTHEAD,1) Y,Y N,N &Z,Y *,*)
    VER (&TMAILLOG,NB,LIST,Y,N,y,n)
    VER (&TMAILACK,NB,LIST,Y,N,y,n)
    VER (&TMDEL,NB,LIST,Y,N)
    VER (&TSOMPRT,NAME)
    IF (&TSOMPRT NE &Z)
       VER (&PRTHEAD,LIST,Y,N)
       &TSOMPRTO = &Z
       IF (&PRTFORM NE &Z)
         &TSOMPRTO = ' FORM(&PRTFORM)'
       IF (&PRTFCB NE &Z)
         &TSOMPRTO = '&TSOMPRTO FCB(&PRTFCB)'
       IF (&PRTCHAR NE &Z)
         &TSOMPRTO = '&TSOMPRTO CHAR(&PRTCHAR)'
       IF (&PRTCLASS NE &Z)
         &TSOMPRTO = '&TSOMPRTO CLASS(&PRTCLASS)'
       IF (&PRTHEAD = N)
         &TSOMPRTO = '&TSOMPRTO NOTITLE'
    VPUT (SENDFRM1 SENDFRM2 SENDFRM3 TMAILLOG TMAILACK) PROFILE
    VPUT (TSOMPRT PRTFORM PRTFCB PRTCHAR TSOMPRTO TMDEL) PROFILE
    VPUT (SENDCTR PRTCLASS PRTHEAD) PROFILE
    VPUT (SENDCTR) SHARED
)End
./ ADD NAME=TMAILEDT 0171-87217-93224-0449-00080-00042-00000-$A1238
)ATTR
  \ TYPE(INPUT) CAPS(OFF) INTENS(HIGH) COLOR(GREEN)
  @ TYPE(INPUT) CAPS(ON) INTENS(HIGH) COLOR(WHITE)
  ~ TYPE(INPUT) CAPS(OFF) INTENS(HIGH) COLOR(TURQ)
  _ TYPE(INPUT) CAPS(OFF) INTENS(HIGH) FORMAT(&MIXED)
  ` TYPE(INPUT) CAPS(ON) INTENS(HIGH) FORMAT(&MIXED)
  | AREA(DYNAMIC) EXTEND(ON) SCROLL(ON) USERMOD(20)
  ! TYPE(OUTPUT) INTENS(HIGH) PAD(-)
   # type(output) intens(high) color(turq)
 01 TYPE(DATAOUT) INTENS(LOW)
 02 TYPE(DATAOUT) INTENS(HIGH)
 03 TYPE(DATAOUT) SKIP(ON) /* FOR TEXT ENTER CMD. FIELD */
 04 TYPE(DATAIN)  INTENS(LOW)  CAPS(OFF)
 05 TYPE(DATAIN)  INTENS(HIGH) CAPS(OFF)
 06 TYPE(DATAIN)  INTENS(LOW)  CAPS(IN)
 07 TYPE(DATAIN)  INTENS(HIGH) CAPS(IN)
 08 TYPE(DATAIN)  INTENS(LOW)  FORMAT(DBCS)
 09 TYPE(DATAIN)  INTENS(LOW)  FORMAT(EBCDIC)
 0A TYPE(DATAIN)  INTENS(LOW)  FORMAT(&MIXED)
 20 TYPE(DATAIN)  INTENS(LOW)  CAPS(IN) FORMAT(&MIXED)
)BODY WIDTH(&ZWIDTH) EXPAND(//)
%EDIT TSO Message #tmever%------------/-/-------------------------------
+Command ===>_ZCMD                    / /                     +Scroll ==
+To:~SENDID
+From:~SENDFRM1                         +Log the message:`Z+ (Y or N)
~SENDFRM2                               +Acknowledgement:`Z+ (Y or N)
~SENDFRM3                               +Append Epilog:  `Z+ (Y or N)
+Subject:\SENDSUBJ
|ZDATA -------------------------------/-/-------------------------------
|                                     / /
| ------------------------------------/-/-------------------------------
 %&PFE+= Send message   %CANCEL+to abort message     Stamped: &SENDSTMP
 +
)INIT

  .HELP = ISR20000         /* DEFAULT TUTORIAL NAME     */
  .ZVARS = 'ZSCED LOG ACK EPI'     /* SCROLL AMT VARIABLE NAME  */

  &TMAILCAN = &Z
  &PFE = PFK(END)
  IF (&PFE = &Z)  &PFE = 'END'
  &LOG = TRANS(&TMAILLOG Y,Y N,N &Z,Y *,*)
  &ACK = TRANS(&TMAILACK Y,Y N,N &Z,Y *,*)
  &EPI = TRANS(&TMAILEPI Y,Y N,N &Z,Y *,*)
  IF (&LOG = &Z)  &LOG = 'Y'
  IF (&ACK = &Z)  &ACK = 'Y'
  IF (&EPI = &Z)  &EPI = 'Y'
  IF (&SENDSUBJ = &Z)
     .CURSOR = SENDSUBJ
  IF (&SENDSUBJ NE &Z)
     IF (&FWD = 'on')
        .CURSOR = zcmd
     IF (&FWD NE 'on')
        .CURSOR = zdata

)REINIT

  REFRESH(ZCMD,ZDATA, SENDSUBJ,SENDFRM1,SENDFRM2,SENDFRM3)
  .HELP = ISR20000         /* DEFAULT TUTORIAL NAME     */

)PROC

  &ZCURSOR = .CURSOR
  &ZCSROFF = .CSRPOS
  &ZLVLINE = LVLINE(ZDATA)

    VER(&SENDFRM1,NB)
    VPUT (SENDID SENDSUBJ) SHARED
    VER (&LOG,NB,LIST,Y,N)
    VER (&ACK,NB,LIST,Y,N)
    VER (&EPI,NB,LIST,Y,N)
    &TMAILLOG = &LOG
    &TMAILACK = &ACK
    &TMAILEPI = &EPI
    VPUT (SENDFRM1,SENDFRM2,SENDFRM3,TMAILLOG,TMAILACK,TMAILEPI) PROFILE

)END

/*  DYNAMIC AREA SCREEN WIDTH FROM PQUERY. (80,132,160)
/*  DYNAMIC AREA SCREEN DEPTH FROM PQUERY. (24,32,43,27,60)
./ ADD NAME=TMAILFFL 0109-91179-93148-1238-00051-00030-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(asis ) color(turq)
   ` type(text)   intens(low ) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ! type(text) intens(high) caps(off) just(asis ) color(white)
)BODY
+-------------------------% TSO/E-Mail File Forward +-------------------
%Command ===>_zcmd
+
`Forward file to user specified below:
+
+
`To:       Nickname %===>_FFNICK  `(Nickname or Distribution List)%*+
+
`or        Center   %===>_FFNODE  `(NJE Node Name)
`          Userid   %===>_FFUSER  `(Userid)
+
`Log Transmission   %===>_Z  `    `(Yes or No)
`Acknowledgement    %===>_Z  `    `(Yes or No)
+
+
+File to be forwarded:^mailtds
+       received from:^fid
+                  on:^maildatd ^mailtime
+
+Press 'ENTER' to send the file or 'PF3' to cancel
)INIT
  .ZVARS = 'TMAILLOG TMAILACK'
  &TMAILLOG = TRANS(&TMAILLOG Y,Yes N,No &Z,Yes )
  &TMAILACK = TRANS(&TMAILACK Y,Yes N,No &Z,Yes )
 if (&mailgloc NE &z)
   &fid  = '&mailfid at &mailfloc'
 if (&mailfloc = &z)
    &fid  = &mailfid
)PROC
   &TMAILLOG = TRUNC(&TMAILLOG,1)
   &TMAILACK = TRUNC(&TMAILACK,1)
    VER (&TMAILLOG,NB,LIST,Y,N,YES,NO)
    VER (&TMAILACK,NB,LIST,Y,N,YES,NO)
   &FFLLOG = &Z
   &FFLACK = &Z
   IF (&TMAILLOG = Y)
      &FFLLOG = MLOG
   IF (&TMAILACK = Y)
      &FFLACK = MACK
)END
./ ADD NAME=TMAILFIL 0128-89306-92149-1030-00073-00027-00000-$A1238
)Attr
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(high) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
)Body
+------------------------% TSO/E-Mail File Processing +-----------------
%Selection ===>_zcmd
%
%
          %0  Read    `- Read the message that accompanied the data set
          %1  Browse  `- Browse the data set
          %2  Edit    `- Edit the data set
          %3  Print   `- Print the data set
          %4  Rename  `- Rename the data set
          %5  Copy    `- Copy the data set
          %6  PDS     `- Use the PDS Command on the data set
          %7  Delete  `- Delete the data set
          %8  Unterse `- Unterse a file from IBMLINK
          %9  XMIT    `- Transmit the file to another user
%
%     Data Set Name:!mailtds
%
#    !mailsubj
#           Userid:!mailfid
#           Node:  !mailfloc
#           Date:  !maildatd    #Time: !mailtime
)Init
  .HELP = TMAILH2C
  &zcmd = &z
)Proc
       &ZCMD = TRANS(&ZCMD RE,0
                           REA,0
                           READ,0
                           B,1
                           BR,1
                           BRO,1
                           BROW,1
                           E,2
                           ED,2
                           EDI,2
                           EDIT,2
                           P,3
                           PR,3
                           PRT,3
                           PRNT,3
                           PRIN,3
                           RN,4
                           REN,4
                           RENA,4
                           RENM,4
                           C,5
                           CO,5
                           COP,5
                           CPY,5
                           COPY,5
                           PDS,6
                           DEL,7
                           PTF,8
                           UNT,8
                           UNTE,8
                           UNTER,8
                           UNTERS,8
                           UNTERSE,8
                           XMIT,9
                           *,*)
            &mailsmsg = 'Error'
            &maillmsg = 'Option &ZSEL invalid, choose a valid option:'
 VER (&ZCMD,RANGE,0,9,MSG=MAIL000)
)End
./ ADD NAME=TMAILFRN 0105-89306-91269-1259-00027-00027-00000-$A1238
)Attr
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
   ! type(output) intens(low ) caps(on) just(asis ) color(yellow)
   ` type(text) intens(high) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+--------------------------% TSO/E-Mail File Rename +-------------------
%Command ===>_zcmd
%
%
%     Current Data Set Name:  !mailtds
%
%     New Data Set Name   ===>_newname
%
     !mailsubj
%
%     Enter the New Data Set Name or^END%to terminate rename process.
%
)Init
  .CURSOR = newname
  &zcmd = &z
)Proc
 VER (&newname,nb,dsname)
)End
./ ADD NAME=TMAILFWD 0133-89305-93148-1233-00055-00023-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
   _ type( input) intens(high) caps(on ) just(left )
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   | type(output) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
)Body
+----------------------% TSO/E-Mail Forward Processing +----------------
%Command ===>_zcmd
%
%
`      Enter the Node and Userid of the person to receive the forwarded
`      TSO Mail item or%*`to send the mail back to the originator with
`      a response.
+
 `    Userid or Nickname to receive the mail or %*`
%     Userid%===>_FWDUSER
%   Nodename%===>_FWDNODE `    NJE Location for userid

`   Use forwarded flag in text%===>_FWD  ` Yes, No or Never
`       (text is shifted right 2 columns and a '>' is placed in column 1
`       (Yes - set flag, No - do not set flag this time, Never - never s
`
`      Original Sender:
#               Userid:!mailfid
#               Node:  !mailfloc

`     Original Subject:
`              !mailsubj
)Init
 .cursor = FWDUSER
 .help   = TMAILHFW
 if (&fwd NE NEVER)
     &FWD = TRANS(TRUNC(&FWD,1) Y,YES N,YES &Z,YES *,*)
)Reinit
 if (&fwd NE NEVER)
     &FWD = TRANS(TRUNC(&FWD,1) Y,YES N,NO &Z,YES *,*)
)Proc
 IF (&FWDUSER = '*')
     &FWDUSER = &MAILFID
     &FWDNODE = &MAILFLOC
 VER (&FWDUSER,NB)
 VER (&FWDNODE,NAME)
 IF (&FWDNODE NE &Z)
    &FWDID = '&FWDNODE..&FWDUSER'
 IF (&FWDNODE = &Z)
    &FWDID = &FWDUSER
 if (&fwd NE NEVER)
    &FWD = TRANS(TRUNC(&FWD,1) Y,YES N,NO *,*)
 Ver (&FWD,NB,List,YES,NO,NEVER)
 VPUT (FWD) PROFILE
)End
./ ADD NAME=TMAILFWF 0122-89317-93166-1333-00060-00035-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   @ type(output) intens(low ) caps(off) just(asis ) color(white)
     hilite(uscore)
   | type(output) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+----------------------% TSO/E-Mail Forward Processing +----------------
%Command ===>_zcmd
%
`   Enter the Node and Userid of the person to receive the forwarded ite
`
`   Userid or Nickname to receive the mail or %*`
%   Userid  %===>_fwduser
%   Nodename%===>_fwdnode `    NJE Location for userid
`
`   Send the file      %===>_Z+ %Y`Yes%N`No or%M`as a message
`
`   Forward Flag option%===>_FWD  `%Yes No`or%Never
`
`   Subject: @mailsubj
`
`   Origin Node:         |mailfloc
`          User:         |mailfid
`   Original data set:   |mailfds
`   Associated data set: |mailtds
`
`   The Forward flag will shift the message text right 2 columns and
`   place a '>' in column 1.
)Init
 .cursor = fwduser
 .help   = TMAILHFF
 .ZVARS = '(MsgFILE)'
 &Msgfile = trans(trunc(&Msgfile,1) Y,Y N,N M,M &Z,Y)
 if (&fwd NE NEVER)
     &FWD = TRANS(TRUNC(&FWD,1) Y,YES N,YES &Z,YES *,*)
)Reinit
 if (&fwd NE NEVER)
     &FWD = TRANS(TRUNC(&FWD,1) Y,YES N,NO &Z,YES *,*)
)Proc
 IF (&FWDUSER = '*')
     &FWDUSER = &MAILFID
     &FWDNODE = &MAILFLOC
 VER (&FWDUSER,NB)
 VER (&FWDNODE,NAME)
 IF (&FWDNODE NE &Z)
    &FWDID = '&FWDNODE..&FWDUSER'
 IF (&FWDNODE = &Z)
    &FWDID = &FWDUSER
 VER (&MSGFILE,NB,LIST,Y,N,M)
 &FWDFILE = &MSGFILE
 if (&fwd NE NEVER)
    &FWD = TRANS(TRUNC(&FWD,1) Y,YES N,NO *,*)
 Ver (&FWD,NB,List,YES,NO,NEVER)
 VPUT (FWD) PROFILE
)End
./ ADD NAME=TMAILHC  0103-89335-91269-1259-00022-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^File Copy`option will copy the received data set to an existi
`      or new data set.  If the received data set is a PDS then you can
`      elect to have the members replace existing members during the cop
`      to an existing data set if you wish.
`
`      Select^Yes`for ISPF statistics if you are copying a sequential fi
`      into a PDS and wish ISPF statistics to be generated.
`
)End
./ ADD NAME=TMAILHD1 0102-90033-91269-1300-00033-00033-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Defaults +---------------------
%Command ===>_zcmd
%

`    TSO Mail Defaults are important as they provide the necessary infor
`    for the proper processing of your mail.
`
`    Your%Name, Mailing address`and%Electronic address`will be used in t
`    messages that you send to others.
`
`    The current%Node`is used in the messages that you send to identify
`    the message is originating.
`
`    Specifying that you want the outgoing messages to be%Logged`will ca
`    a copy of your messages to be placed into your TSO Mail table which
`    allow you to resend them and/or print them.
`
`    Specifying whether you want your outgoing messages to be%Acknowledg
`    will provide you with notification that your outgoing message has b
`    read by the recipient.
)Init
)Proc
  &zsel = *tmailhd2
)End
./ ADD NAME=TMAILHD2 0102-90033-91269-1300-00027-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Defaults +---------------------
%Command ===>_zcmd
%

`    The option to retain 3 mail logs for diagnostic purposes is availab
`    you or your systems programmer suspect that you may be losing mail
`    because of some unique situation.  Since the mail table is built fr
`    mail logs, retaining copies will help with diagnosis.  The logs wil
`    suffixed HOLD1.LOG, HOLD2.LOG and HOLD3.LOG with a prefix of your u
`    and a qualifier as defined by the installation.
`
`    The%Print`options are used to provide a fast path for printing of y
`    messages.  If the printer destination is blank then you will be pro
`    by a print option selection menu that will allow you to specify sev
`    different print options and printer types.
)Init
)Proc
)End
./ ADD NAME=TMAILHFF 0101-92112-93165-1522-00035-00036-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+------------------------% TSO/E-Mail Forward Message +-----------------
%Command ===>_zcmd
%
`    This dialog allows you to forward a message and/or file to other
`    individual(s) via electronic mail.
`
%    Field       Description
`
^    Userid     `The userid(s) or nickname(s) of the intended recipients
`                an '*' may be used to forward the message to the origin
`                sender (a better technique for answering messages)
`
^    Nodename   `The node name to be used if a userid is specified
`                (this field may be blank if you are using a nickname)
`
^    File       `This message has an associated file.  Specify 'Y' if yo
`                want to forward the file first.  Specify 'M' to send it
`                the file as the message.
`
^    Forwarded  `If this is 'yes' the forwarded text will be shifted rig
`                2 columns and the '>' symbol inserted in column 1.  A l
`                of text will also be inserted before and after the text
`                text to indicate the start and end of the forwarded tex
)Init
)Proc
)End
./ ADD NAME=TMAILHFW 0101-92112-92112-1436-00033-00031-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+------------------------% TSO/E-Mail Forward Message +-----------------
%Command ===>_zcmd
%
`
`    This dialog allows you to forward a message to other individual(s)
`    via electronic mail.
`
%    Field       Description
`
^    Userid     `The userid(s) or nickname(s) of the intended recipients
`                an '*' may be used to forward the message to the origin
`                sender (a better technique for answering messages)
`
^    Nodename   `The node name to be used if a userid is specified
`                (this field may be blank if you are using a nickname)
`
^    Forwarded  `If this is 'yes' the forwarded text will be shifted rig
`                2 columns and the '>' symbol inserted in column 1.  A l
`                of text will also be inserted before and after the text
`                text to indicate the start and end of the forwarded tex
`
)Init
)Proc
)End
./ ADD NAME=TMAILHLP 0115-89300-91269-1300-00028-00029-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+--------------------------% TSO/E-Mail Processing +--------------------
%Command ===>_zcmd
%

`    TSO Mail Processing is an extension to the standard IBM provided TS
`    Transmit and Receive commands that provides full screen ISPF dialog
`    support for sending and receiving messages and files.


     The following tutorial options will be displayed in sequence or may
     be selected by number:

|    1 -^Commands          `Commands that may be entered on the Command
|    2 -^Message Options   `Selection options for individual messages/fi

)Init
)Proc
  &zsel = trans(&zcmd 1,tmailh1 2,tmailh2 *,?)
)End
./ ADD NAME=TMAILHS1 0102-91254-91269-1300-00020-00019-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Epilog +-----------------------
%Command ===>_zcmd
%

`    The TSO/E-Mail EPILOG is any comment, up to 10 lines, that you wish
`    have included at the end of every message that you send.  This is
`    optional.
`
)Init
)Proc
)End
./ ADD NAME=TMAILH0  0101-89335-91269-1301-00034-00034-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+--------------------------% TSO/E-Mail Processing +--------------------
%Command ===>_zcmd
%
`
`    TSO Mail Processing is an extension to the standard IBM provided TS
`    Transmit and Receive commands that provides full screen ISPF dialog
`    support for sending and receiving messages and files.
`
`    With this ISPF Dialog you have the ability to^Open Mail`which will
`    receive all new messages and store the messages into an ISPF table.
`    It will also receive any data sets that have been sent to you and
`    store them as unique data sets pending further action by you.
`
`    The^Send Mail`option simplifies the task of sending messages to
`    another user, either at the current NJE Node or at another location
`
`
`    The following tutorial will be displayed only if selected:
`
|      1 -^Full Tutorial

)Init
)Proc
  &zsel = trans(&zcmd 1,*tmailhlp *,?)
)End
./ ADD NAME=TMAILH1  0111-89335-92014-1044-00037-00026-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
%
`      The following commands may be entered on the ISPF Command line wh
`      in the TSO Mail Table display.  Information on them will be prese
`      in sequence or selected by number.

|      Action Command Options:

|      0 -^Defaults          `Establish TSO Mail Processing Defaults
|      1 -^Find              `Find a character string in any row in the
|      2 -^Refresh           `Process new mail and redisplay the mail ta
|      3 -^Sendmail          `Send a note to another user
|      4 -^Sort              `Sort the table by the specified field name
|      5 -^Safemail          `Registration for SAFE Mail Processing
|      6 -^Logging           `Log the message into a log of related mess

|      Display Command Options:

|         ^Long              `Use the default 2 line display for each me
|         ^Short             `Use a short 1 line display for each messag
|         ^XLong             `Use a extra long 3 line display for each m

)Init
)Proc
  &zsel = trans(&zcmd 0,tmailh10  1,tmailh1a  2,tmailh1b  3,tmailh1c
                      4,tmailh1d  5,tmailh1e  6,tmailh1f *,?)
)End
./ ADD NAME=TMAILH1A 0106-89335-92014-1037-00025-00030-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^FIND`command will scroll the table to the row in which the
`      specified character string is found in any field in the table.
`
`
`      Syntax: ^FIND value
`
`      Note:   ^FIND`can be abbreviated to^F
`
`
`
`
)End
./ ADD NAME=TMAILH1B 0104-89335-91269-1301-00021-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^REFRESH`command will check for new mail and update the mail t
`      for all new mail messages and files.
`
`      Syntax: ^REFRESH
`
`      Note:   ^REFRESH`can be abbreviated to^REF.
`
)End
./ ADD NAME=TMAILH1C 0105-89335-91269-1301-00024-00022-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^SENDMAIL`command allows you to send a message to another user
`      anywhere in the network.
`
`      Syntax: ^SENDMAIL recipient
`
`      Note:    recipient is optional and if not specified a prompting p
`               will be displayed.
`              ^SENDMAIL`can be abbreviated to^SEND.
`
`      SENDMAIL uses the ISPF Editor for the entry of your message text.
)End
./ ADD NAME=TMAILH1D 0106-89335-91269-1257-00030-00031-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`      The^SORT`command will sort the entries in the table using the spe
`      field name as the sort key or display a selection panel to prompt
`      the field(s) to sort by.
`
`      Syntax: ^SORT field-name direction
`
`      Note:  - field-name must be specified and must be one of the supp
`               names documented below^or`if blank will cause a selectio
`               panel to be displayed.
`             - direction is^A`for ascending or^D`for descending (defaul
`
`      Supported field-names:
`
^      FROMNODE  `Sending Node Name       ^DATE      `Date the message r
^      FROMUSER  `Sending User ID         ^TIME      `Time the message r
^      TONODE    `Receiving Node Name     ^ACTION    `Last action perfor
^      TOUSER    `Receiving User ID
)End
./ ADD NAME=TMAILH1E 0102-90122-91269-1301-00025-00025-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^SAFEMAIL`command will invoke the registration dialog for
`      Safe Mail procesing.
`
`      Syntax: ^SAFEMAIL
`
`      SAFE Mail is a process that runs daily to receive mail messages
`      and mail files that have been sent to you from another user for
`      you while you are out of the office for more than a few days.
`      This is to prevent these mail messages and mail files from
`      being deleted from the system because of age.
`
)End
./ ADD NAME=TMAILH1F 0101-91179-91269-1301-00027-00027-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^LOG`command will enter the LOG dialog in which you can review
`      create and process multiple log data sets.
`
`      A LOG is a data set, similar to the TSO/E Mail data set, in which
`      can log messages that are related.  It can also be used as a mess
`      archive facility.
`
`      Message selection option^L`is used to request that a message be
`      logged.
`
`      Optionally, by specifying a message selection option of Ln, where
`      the number of the log data set, you can perform the logging task
`      faster.
)End
./ ADD NAME=TMAILH10 0103-89347-91269-1301-00022-00023-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^DEFAULT`command will display a panel upon which certain requi
`      variables must be filled in by the user.  This panel will be disp
`      the first time it is required and may be selected specifically to
`      change the defaults at any time.
`
`      Syntax: ^DEFAULT
`
`      Note:   ^DEFAULT`can be abbreviated to^DEF.
)End
./ ADD NAME=TMAILH2  0120-89335-92258-1040-00039-00032-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(text) intens(high) caps(off) just(asis ) color(turq)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
%
`      The following mail selection commands may be entered on the
`      selection bar to the left of each message in the TSO Mail Table
`      display.  Information on them will be presented in sequence or
`      selected by number.

|      1^(A)|-!Answer the mail
|      2^(D)|-!Delete the note
|      3^(E)|-!Edit the note
|      4^(F)|-!Forward the note
|      5^(P)|-!Print the note
|      6^(L)|-!Log/Archive the message
|      7^(C)|-!Change the subject and status of a message for clarity
|      8^(M)|-!Merge the data set associated with message into the messa
|      9^(S)|-!Read the mail or Select the note or process the received
`       `File options:  Read, Browse, Copy, Delete,
`                       Edit, PDS, Print, Rename
`
)Init
)Proc
  &zsel = trans(&zcmd 1,tmailh2a  2,*tmailh2a  3,*tmail2a 4,tmailh2b
                      5,*tmailh2b 6,*tmailh2b  7,*tmailh2b
                      8,tmailh2c  9,tmailh2d
                      A,tmailh2a  D,*tmailh2a  E,*tmail2a F,tmailh2b
                      P,*tmailh2b L,*tmailh2b  C,*tmailh2b
                      M,tmailh2c  S,tmailh2d
                      *,?)
)End
./ ADD NAME=TMAILH2A 0111-89335-93158-1243-00031-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   \ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`      The\Answer`option is used to send an anwser to the sender of the
`      selected message.  This option is a fast path to the SENDMAIL
`      command with the receiving node and userid predefined with the no
`      name and userid of the person who sent you the message.  The subj
`      is also predefined with the subject of the original message.
`
`      The\Delete`option is used to delete the message and any associate
`      file that accompanied the message.  Note that you will be prompte
`      to confirm the deletion of the file so that you can keep the file
`      even though you have deleted the message, or delete the file and
`      retain the message. Use of\DN`will delete the message and file
`      without any prompting.
`
`      The\Delete`option may also be specified as\DM`to only delete the
`      message, or as\DF`to delete the file and retain the message.
`
`      The\Edit`option will invoke ISPF Edit to allow the user to edit
`      the saved message.
)End
./ ADD NAME=TMAILH2B 0112-89335-91269-1302-00032-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^Forward`option is used to resend the message and any
`      accompanying file to another user or users.  This option interfac
`      to the SENDMAIL command and will copy in full the original messag
`      into the message text edit area of the new message so that you ca
`      add or change the original message before sending it.
`
`      The^Print`option will display a selection menu of the available
`      ISPF dialogs for printing on various types of printers.  See the
`      tutorial for each of those dialogs for more specific information
`      on the requirements for printing on the different printers.
`      This option will only print the message and not the accompanying
`      data set.
`
`      The^L`option will move the message into another LOG table of rela
`      messages.  ^Ln`where^n`is the number of the table can also be spe
`
`      The^C`option will allow you to change the Subject of a message an
`      it's status to clarify it for later retrieval.
)End
./ ADD NAME=TMAILH2C 0102-92014-93153-0637-00021-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   \ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The\Merge`option is used to merge the data set associated with th
`      message into a message.  This is done only for those data sets th
`      have a line length of 80 characters per line or less.  The data s
`      text is merged into the message.
`
`      Use of\MF`will force a merge even if the data does exceed 80 byte
`      per line.
)End
./ ADD NAME=TMAILH2D 0116-89335-92132-1336-00037-00031-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
%
%
`      The^Select`option will display a selection menu of the options th
`      are provided for processing the received data set.  This option c
`      be specified in the selection field by entering|S.n`where|n`is on
`      of the available options.
`
`      The options available are:
`
|      0 -^Read the message that accompanied this data set
|      1 -^Browse the data set
|      2 -^Edit the data set
|      3 -^Print the data set
|      4 -^Rename the data set
|      5 -^Copy the data set
|      6 -^Use the PDS command to process the data set
|      7 -^Delete the data set
|      8 -^Unterse a data set received from IBMLINK
|      9 -^Forward the data set to another user

)Init
)Proc
  &zsel = trans(&zcmd 0,tmailh3a   1,*tmailh3a  2,*tmailh3a  3,tmailh3b
                      4,*tmailh3b  5,*tmailh3b  6,tmailh3c
                      7,*tmailh3c  8,*tmailh3c  9,*tmailh3d *,?)
)End
./ ADD NAME=TMAILH3A 0105-89335-91269-1302-00030-00023-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^S.0`or^Read`option will display the transmit message that was
`      along with the data set using ISPF Browse.
`
`      The^S.1`or^Browse`option will display the data set using ISPF Bro
`      All of the standard ISPF Browse capabilities are available while
`      browsing the selected data set.
`
`      The^S.2`or^Edit`option will invoke ISPF Edit on the selected data
`      ALL of the standard ISPF Edit capabilities are available while ed
`      the selected data set.
`
%      Notes:
`     ^S.0`may also be specified as:^READ
`     ^S.1`may also be specified as:^BR`or^BROW
`     ^S.2`may also be specified as:^ED`or^EDIT
`
)End
./ ADD NAME=TMAILH3B 0102-89335-91269-1303-00028-00023-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^S.3`or^Print`option will display a menu of the available prin
`      dialogs used for printing data sets.  See that tutorial for more
`      information on printing the selected data set.
`
`      The^S.4`or^Rename`option will allow you to rename the selected da
`
`      The^S.5`or^Copy`option will allow you to copy the selected data s
`      another data set.  If the data set does not exist then it can be
`      allocated.
`
%      Notes:
`     ^S.3`may also be specified as:^PR`or^PRIN`or^PRNT`or^PRT
`     ^S.4`may also be specified as:^RN`or^REN`or^RENA`or^RENM
`     ^S.5`may also be specified as:^CO`or^COP`or^COPY`or^CPY
)End
./ ADD NAME=TMAILH3C 0104-89335-92132-1337-00027-00020-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^S.6`or^PDS`option will invoke the ISPF PDS dialog for the sel
`      data set.  There is a very complete tutorial for this option
`      available once in the PDS dialog.
`
`      The^S.7`or^Delete`option will allow you to delete the selected da
`
`      The^S.8`or^UNT`option will invoke the IBM utility to unterse data
`      compressed using the TERSE utility (e.g. PTFs from IBMLink)
`
%      Notes:
`     ^S.6`may also be specified as:^PDS
`     ^S.7`may also be specified as:^DEL
`     ^S.8`may also be specified as:^UNT, PTF, UNTERSE
)End
./ ADD NAME=TMAILH3D 0101-91179-91269-1303-00023-00023-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+---------------------------% TSO/E-Mail Tutorial +---------------------
%Command ===>_zcmd
`
`
`      The^S.9`or^XMIT`option will allow you to forward the selected dat
`      data set to another user.
`
`
`
`
`
%      Notes:
`     ^S.9`may also be specified as:^XMIT
)End
./ ADD NAME=TMAILMNU 0138-89325-91345-1448-00046-00033-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type(output) intens(high) caps(off) just(asis ) color(white)
   | type(text) intens(high) caps(off) just(asis ) color(turq)
)Body
+--------------------------% TSO/E-Mail Option Menu +-------------------
%Selection ===>_zcmd


+              %0|- Defaults                           %Userid: !ZUSER
+              %1|- Open Mail                          %Date:   !ZJDATE
+              %2|- Read current Mail                  %Time:   !ZTIME
+              %3|- Send a Note                        %Center: !SENDCTR
+              %4|- Send a File
+              %5|- Process TSO Mail LOGs
+              %6|- SAFE Mail Registration
+              %7|- Nick Name Management
+
+
+   %Defaults: |  Set various values related to sending messages.
+   %Open Mail:|  Receive all outstanding mail and display a selection t
|                 of all incoming and outgoing mail messages and files.
+   %Read Mail:|  Read all currently received mail.
+   %Send a Note:|Send a message to another user.
+   %Send a File:|Send a file to another user.
+   %Logs:       |Process and Create log data sets for messages.
+   %SAFE Mail:|  Register for SAFE Mail processing.
+   %Nick Name:|  Work with nick names in your message directory
+
)Init
 .HELP = TMAILH0
 &sendctr = trans(&sendctr *,*)
)Proc
 &zsel = trans(&zcmd
               0, 'CMD(%TMAILDEF) NEWAPPL(MAIL)'
               1, 'CMD(%TSOMAIL) NEWAPPL(MAIL)'
               2, 'CMD(%TSOMAIL READ) NEWAPPL(MAIL)'
               3, 'CMD(%TMAILSND) NEWAPPL(MAIL)'
               4, 'CMD(%TMAILSND SENDFILE) NEWAPPL(MAIL)'
               5, 'CMD(%TMAILLOG) NEWAPPL(MAIL)'
               6, 'CMD(%TMAILSM)'
               7, 'CMD(%TMAILNIK) NEWAPPL(MAIL)'
               )
)End
./ ADD NAME=TMAILNIK 0109-91144-92031-0835-00035-00030-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type(text) intens(high) caps(off) just(asis ) color(white)
   + type(text  ) intens(low ) Skip(on)
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   | type(output) intens(high) caps(on) just(asis ) color(turq)
)Body
+---------------------% TSO/E-Mail Nick Name Management +---------------
%Selection ===>_zcmd
+
+Select which Nick Name data set you wish to process by enter any non-bl
+next to the data set name:
+
+ Sel  Data Set Name
+  @z+ |prim
+  @z+ |alt1
+  @z+ |alt2
+  @z+ |alt3
+  @z+ |alt4
+  @z+ |alt5
+  @z+ |alt6
+  @z+ |alt7
+  @z+ |alt8
+  @z+ |alt9
+
+
!    To create a new alternate nick name data select the primary (1st) d
!    and then update the control information
+
!    Enter PF3 to terminate processing at this point.
)Init
  .ZVARS = 'ZP Z1 Z2 Z3 Z4 Z5 Z6 Z7 Z8 Z9'
)Proc
)End
./ ADD NAME=TMAILNTW 0103-92269-92269-1323-00027-00028-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(left ) color(white)
   | type(text) intens(low ) caps(off) just(asis ) color(white)
   ` type(output) intens(high) caps(off) just(right) color(white)
         hilite(uscore)
   ~ type(text) intens(high) caps(off) just(asis ) color(yellow)
)Body
+-------------------------% TSO/E-Mail Table Rebuild +------------------
%
%
%
%
%          Your TSO Mail ISPF Table data set required more space
%
%          Please be patient while the table is being copied into
%          a new and larger table data set.
%
~          Copying^MEMBERS


)Init
)Proc
)End
./ ADD NAME=TMAILNV  0110-90241-91269-1304-00030-00025-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
   _ type( input) intens(high) caps(on ) just(left )
   | type( input) intens(high) caps(on ) just(left) pad('_') color(white
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
)Body
+---------------------% TSO/E-Mail Nick Name Validation +---------------
%Command ===>_zcmd
%
%
`      The following nick names were specified, but could not be found i
`      any nick name file.  Please correct the nick name(s) or blank the
`      out and then press the^ENTER`key to retry.

`      To cancel this^entire`transmission press the^&END`key.
`
`
`      Invalid Nick Names:
      |tnames
`
)Init
 .cursor = tnames
  &END = PFK(END)
)Proc
 &zcmd = &z
)End
./ ADD NAME=TMAILPTE 0103-90190-92132-1338-00030-00030-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
   _ type( input) intens(high) caps(on ) just(left )
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   | type(output) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
)Body
+----------------------% TSO/E-Mail File UnTerse Status +---------------
%Command ===>_zcmd
%
%
`      File UnTerse Processing was requested.
%
`!ptferr1
`!ptferr2
`!ptferr3
`!ptferr4
`!ptferr5
`!ptferr6
`!ptferr7
`!ptferr8
`!ptferr9
`!ptferr10

)Proc
 &zcmd = &z
)End
./ ADD NAME=TMAILPTF 0104-90190-92121-0951-00027-00024-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
   _ type( input) intens(high) caps(on ) just(left )
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   | type(output) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
)Body
+----------------------% TSO/E-Mail File UnTerse Status +---------------
%Command ===>_zcmd
%
%
`      File UnTerse Processing was requested.
%
`      Return Code from unterse: !rc

`      Original Data Set:  !mailfds

`      Output Data Set:    !targdsn

`      TSO Mail Data Set:  !mailtds

)Proc
 &zcmd = &z
)End
./ ADD NAME=TMAILRDO 0108-89317-91269-1305-00036-00027-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(asis ) color(turq)
   ! type(output) intens(high) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
)BODY
%&Z$SYS -----------------% TSO/E-Mail Transmit Error +------------------
`Command ===>_zcmd
+
+
%An Error occurred during Transmit:
%&maillmsg


`Verify the you have specified below a valid nickname or node.userid:
+
`Send to  %===>_SENDID

+
+
+
`Press%ENTER`to re-send Transmit Message or%END`to terminate.
+
+
+
)INIT
  .CURSOR = SENDID
)PROC
  VER (&SENDID,NB)
  VPUT SENDID SHARED
)END
./ ADD NAME=TMAILRW  0105-89307-91269-1305-00034-00033-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(left ) color(white)
   | type(text) intens(low ) caps(off) just(asis ) color(white)
   ` type(output) intens(high) caps(off) just(right) color(white)
         hilite(uscore)
   ~ type(text) intens(high) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+----------------------% TSO/E-Mail RECEIVE Processing +----------------
%
%
%
%
%              Your~TSO MAIL%is now being RECEIVED for ^userid  %



%                              Please be patient



%                       RECEIVE Issued `rcount% times.


  Note: This only indicates that your messages are being processed into
        sequential file and your data sets received into unique data set
        for further processing shortly.
)Init
)Proc
)End
./ ADD NAME=TMAILSFE 0100-92132-92132-1350-00025-00025-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
   _ type( input) intens(high) caps(on ) just(left )
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   | type(output) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
)Body
+------------------------%TSO/E-Mail File Send Status+------------------
%Command ===>_zcmd
%
%
`       You requested that a file be transmitted to a user via the
`       Internet.  This request is%not`supported.  Only messages can be
`       sent across the Internet.  All messages have been sent and the
`       requested file has been sent to any TSO or VM addresses (if any)
`
`       We regret this inconvience caused by the design of Internet mail
`
`
)Proc
 &zcmd = &z
)End
./ ADD NAME=TMAILSFL 0116-89339-93148-1238-00061-00051-00000-$A1238
)ATTR DEFAULT(%+_)
       %   TYPE(TEXT) INTENS(HIGH)
       +   TYPE(TEXT) INTENS(LOW)
       _   TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT)
   ! type(text) intens(high) caps(off) just(asis ) color(white)
   | type(text) intens(high) caps(off) just(asis ) color(turq)
)BODY
+---------------------% TSO/E-Mail Send File Processing +---------------
%Command ===>_zcmd
+
+
+Send a Data Set to a TSO or VM user
+
+
+To:       Nickname %===>_Z       +(Nickname or Distribution List)%*+
+
+or        Center   %===>_Z       +(NJE Node Name)
+          Userid   %===>_Z       +(Userid)
+
+
+   Data Set Name   %===>_Z
+      Sequential   %===>_Z+        Specify%Y+if the above data set is a
+                                   member and you want it sent sequenti
+
!   Note: MVS/BDT should be considered for large volumes of data
+
+
%*+See TSO HELP on NICKNAMES. Requires 'userid.NAMES.TEXT' file.
)INIT
  .ZVARS = '(SENDNAME SENDNODE SENDUID SENDDSN SENDSEQ)'
  .CURSOR = SENDNAME
  IF (&SENDNODE = &Z) &SENDNODE = &SENDCTR
  &SENDSEQ = N
)PROC
  IF (&SENDDSN NE &Z)
     &FILE = 'DS(&SENDDSN)'
  IF (&SENDSEQ = Y)
        &SQ = SEQ
  IF (&SENDSEQ = N)
        &SQ = &Z
  IF (&SENDDSN = &Z)
     &SQ = &Z
  IF (&SENDNAME  = &Z)
    VER (&SENDUID,NONBLANK)
    VER (&SENDNODE,NB,NAME)
    VER (&SENDDSN,NB,DSNAME)
    VER (&SENDSEQ,LIST,Y,N)
    &ZSEL = 'CMD(%TMAILSND &SENDNODE..&SENDUID &FILE NOMSG &SQ)'
  IF (&SENDNAME NE &Z)
    IF (&SENDUID  NE &Z)  &SENDERR = YES
         IF (&SENDNODE NE &Z)  &SENDERR = YES
    IF (&SENDERR = YES)
     &SENDERR = &Z
     &mailsmsg = 'Send to who? '
     &maillmsg= 'Can''t specify both Nickname &SENDNAME and &SENDNODE..&
      .MSG = MAIL000
    VER (&SENDNAME,NONBLANK)
    VER (&SENDDSN,NB,DSNAME)
    VER (&SENDSEQ,LIST,Y,N)
    &ZSEL = 'CMD(%TMAILSND &SENDNAME &FILE NOMSG &SQ SENDFILE)'
)END
./ ADD NAME=TMAILSM  0103-90122-90122-1153-00034-00031-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type(output) intens(high) caps(off) just(asis ) color(white)
   | type(text) intens(high) caps(off) just(asis ) color(turq)
)Body
+-----------------------% TSO/E SAFE Mail Registration +----------------
%Option ===>_zcmd


+              %A|- Add your userid for SAFE Mail Processing
+              %D|- Delete your userid from SAFE Mail Processing
+
+
|    SAFE Mail Processing will receive all mail messages and mail
|    files for your userid while you are out of the office for more
|    than a few days.  This will prevent these from being lost when
|    the Job Entry Subsystem SPOOL is cleaned.
|
|   %A|- will add your userid for SAFE Mail processing so that your mail
|        messages and mail files are received once per day until you ret
|        to the office.
|
|   %D|- will remove your userid from SAFE Mail processing and should be
|        requested upon your return.
+
)Init
 &userid = trans(&zuser *,*)
)Proc
 VER (&ZCMD,NB,LIST,A,D)
 &opt = trans(&zcmd  A,ADD
                     D,DELETE)
)End
./ ADD NAME=TMAILSND 0156-86286-93181-1602-00066-00047-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(asis ) color(turq)
   ` type(text)   intens(low ) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ! type(text) intens(high) caps(off) just(asis ) color(white)
)BODY
+------------------------% TSO/E-Mail Send Processing +-----------------
%Command ===>_zcmd
+
`Send a Message or Data Set to a TSO or VM user
+
`To:       Nickname %===>_Z
          `(Nickname or Distribution List)%*+
+
`or        Center   %===>_Z       `(NJE Node Name)
`          Userid   %===>_Z       `(Userid)
+
`Optional: Data Set Name %===>_Z
`             Sequential %===>_Z`     Specify%Y`if the above data set is
`                                     member and you want it sent sequen
`      Send as a Message %===>_Z`     Yes or No to send the file as a Me
+
`Press ENTER for ISPF Edit to create your message.
+
%*`Notes:%MVS/BDT`should be considered for large volumes of data
%*`      See TSO HELP on NICKNAMES. Requires 'userid.NAMES.TEXT' file.
)INIT
  .ZVARS = '(SENDNAME SENDNODE SENDUID SENDDSN SENDSEQ SENDMSG)'
  .CURSOR = SENDNAME
  IF (&SENDNODE = &Z) &SENDNODE = &SENDCTR
  &SENDSEQ = trans(&SENDSEQ &Z,'N' *,*)
  &SENDMSG = trans(&SENDMSG &Z,'N' *,*)
  &FILE    = &Z
  &SQ      = &Z
)PROC
  IF (&SENDSEQ = Y)
     &SQ = SEQ
  IF (&SENDSEQ = N)
     &SQ = &Z
  IF (&SENDDSN NE &Z)
     IF (&SENDMSG = Y)
         &FILE = 'MSG(&SENDDSN)'
     IF (&SENDMSG NE Y)
         &FILE = 'DS(&SENDDSN)'
  IF (&SENDDSN = &Z)
     &SQ = &Z
  IF (&SENDNAME  = &Z)
    VER (&SENDUID,NONBLANK)
    VER (&SENDNODE,NB,NAME)
    &ZSEL = 'CMD(%TMAILSND &SENDNODE..&SENDUID &FILE &SQ )'
  IF (&SENDNAME NE &Z)
    IF (&SENDUID  NE &Z)  &SENDERR = YES
         IF (&SENDNODE NE &Z)  &SENDERR = YES
    IF (&SENDERR = YES)
     &SENDERR = &Z
     &mailsmsg = 'Send to who? '
     &maillmsg= 'Can''t specify both Nickname &SENDNAME and &SENDNODE..&
      .MSG = MAIL000
    VER (&SENDNAME,NONBLANK)
    &ZSEL = 'CMD(%TMAILSND &SENDNAME &FILE &SQ )'
)END
./ ADD NAME=TMAILSRD 0105-89313-91269-1307-00028-00027-00000-$A1238
)ATTR DEFAULT(%+_)
  %   TYPE(TEXT)  INTENS(HIGH)
  +   TYPE(TEXT)  INTENS(LOW)
  _   TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT)
)BODY
+&Z$SYS -----------------% TSO/E-Mail Transmit Error +------------------
+Command ===>_zcmd
+
+
%An Error occurred during Transmit
%&MAILLMSG

+
+Send to  %===>_SENDID            + (Nickname or Node.Userid)
+
+
+
+
+Press%ENTER+to re-send TSO Mail Message or%END+to terminate.
+
+
+
)INIT
  .CURSOR = SENDID
)PROC
  VER (&SENDID,NB)
  VPUT SENDID SHARED
)END
./ ADD NAME=TMAILSRT 0101-91269-91269-1242-00043-00044-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   ` type(text)   intens(low ) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) skip(on)    color(turq)
   ! type(text) intens(high) caps(off) just(asis ) color(yellow)
)BODY
+------------------------% SORT the TSO/E-Mail Table +------------------
%Command ===>_zcmd
+
`Select the sort fields by specifying the sort order (Ascending or Desce
+
~ ^Sel     Sort Field
~ @Z~     !From Node     ^Site where message originated
~ @Z~     !From Userid   ^User who sent the message
~ @Z~     !To Node       ^Site where message was sent to
~ @Z~     !To Userid     ^User who message was sent to
~ @Z~     !Date/Time     ^Date and Time the message was received
~ @Z~     !Last Action   ^The last action performed on the message
+
+
`Press ENTER to continue or PF3 (End) to cancel the sort request.
+
)INIT
  .ZVARS = '(FROMNODE FROMUSER TONODE TOUSER DATE ACTION)'
  .CURSOR = FROMNODE
  &fromnode = &z
  &fromuser = &z
  &tonode   = &z
  &touser   = &z
  &date     = &z
  &action   = &z
)PROC
 ver (&fromnode,list,A,D)
 ver (&fromuser,list,A,D)
 ver (&tonode,list,A,D)
 ver (&touser,list,A,D)
 ver (&date,list,A,D)
 ver (&action,list,A,D)
)END
./ ADD NAME=TMAILSUF 0102-91254-91254-1336-00038-00035-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ~ TYPE(INPUT) CAPS(OFF) INTENS(HIGH) COLOR(yellow) hilite(uscore)
   ` TYPE(INPUT) CAPS(off) INTENS(HIGH) COLOR(yellow) just(asis)
)Body
+------------------------% TSO/E-Mail Message Epilog +------------------
%Command ===>_zcmd
%
^     Verify your TSO/E-Mail Message Epilog:
+
+
===>`EPILOG1
===>`EPILOG2
===>`EPILOG3
===>`EPILOG4
===>`EPILOG5
===>`EPILOG6
===>`EPILOG7
===>`EPILOG8
===>`EPILOG9
===>`EPILOG10
+
+
+
^ The above text will be inserted at the end of each TSO/E-Mail message
^ you send.
^
)Init
  .HELP = TMAILHS1
)Proc
    VPUT (EPILOG1 EPILOG2 EPILOG3 EPILOG4 EPILOG5) PROFILE
    VPUT (EPILOG6 EPILOG7 EPILOG8 EPILOG9 EPILOG10) PROFILE
)End
./ ADD NAME=TMAILTBL 0199-89300-93158-1242-00086-00032-00000-$A1238
)Attr Default(%+_)
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ! type(output) intens(high) caps(off) just(asis ) color(white)
   ` type(output) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   % type(text) intens(high)
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   # type(output) intens(high) color(turq)
)Body
^Primary Mail Table+#tmver+-%TSO/E-Mail Processing+---------------------
%Command ===>_zcmd                                            %Scroll ==
%
+Command Options: %Find/LOG/REFresh/SENDmail/SORT/DEFaults      SHORT/LO
+Line Options:%S+Read%A+Answer%C+Change%D+Delete%E+Edit%F+Forward%L+Log%
+
%Sel  %FromUser      Node     ToUser   Node      Date     Time       Las
+
)Model
@z   +!z            !z       !z       !z        !z       !z         !z
      `z
)Init
  .HELP = TMAILHLP
  .ZVARS = '(ZSEL MAILFID MAILFLOC MAILTID MAILTLOC MAILDATD +
  MAILTIME MAILACTN MAILSUBJ)'
  &mamt = CSR
  &zsel = &z
)Proc
 &zselx = &z
 IF (&ZCMD = &Z)
     IF (&ZSEL NE &Z)
       if (&Zsel = '=')
           &Zsel = &osel
       &osel = &zsel
       &ZSEL = TRANS(&ZSEL RE,S.0
                           REA,S.0
                           READ,S.0
                           BR,S.1
                           BRO,S.1
                           BROW,S.1
                           ED,S.2
                           EDI,S.2
                           EDIT,S.2
                           PR,S.3
                           PRT,S.3
                           PRNT,S.3
                           PRIN,S.3
                           RN,S.4
                           REN,S.4
                           RENA,S.4
                           RENM,S.4
                           CO,S.5
                           COP,S.5
                           CPY,S.5
                           COPY,S.5
                           PDS,S.6
                           DEL,S.7
                           PTF,S.8
                           UNT,S.8
                           UNTE,S.8
                           UNTER,S.8
                           UNTERS,S.8
                           UNTERSE,S.8
                           XMIT,S.9
                           *,*)
       &ZT = TRUNC(&Zsel,1)
        IF (&ZT = 'L')
           &LOGSEL = .TRAIL
           &ZSEL = 'L'
       &ZT = TRANS(TRUNC(&ZSEL,'.') A,A L,L E,E M,M MF,MF DF,DF
             R,R B,B D,D F,F P,P S,S DM,DM DN,DN C,C U,U *,X)
        IF (&ZT = X)
         &mailsmsg = 'Error'
         &ml = 'A,B,C,D,DM,E,F,L,M,MF,P,S,U,DF,DN'
         &maillmsg = 'Enter a valid value: &ml Was &zsel'
     IF (&ZSEL NE &Z)
       &ZSEL = TRUNC(&ZSEL,'.')
       &SUBOPT = .TRAIL
       VER (&ZSEL,LIST,R,A,B,D,E,F,P,S,DF,DM,DN,C,M,MF,L,U,MSG=MAIL000)
       IF (&SUBOPT NE &Z)
          VER (&ZSEL,LIST,S,MSG=MAIL001)
          VER (&SUBOPT,RANGE,0,9,MSG=MAIL001)
)End
./ ADD NAME=TMAILTBS 0153-90036-93158-1242-00084-00043-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ! type(output) intens(high) caps(off) just(asis ) color(white)
   ` type(output) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   # type(output) intens(high) color(turq)
)Body Expand(\\) Width(&ZSCREENW)
^Primary Mail Table+#tmver+\-\%TSO/E-Mail Processing+\-\
%Command ===>_zcmd                    \ \                    %Scroll  ==
%
+Command Options: %Find/LOG/REFresh/SENDmail/SORT/DEFaults      SHORT/LO
+Line Options:%S+Read%A+Answer%C+Change%D+Delete%E+Edit%F+Forward%L+Log%
+
%Sel  %FromUser      Date     Time     Subject
+
)Model
@z   +!z            !z       !z       !z       \ \
)Init
  .HELP = TMAILHLP
  .ZVARS = '(ZSEL MAILFID MAILDATD MAILTIME MAILSUBJ)'
  &mamt = CSR
  &zsel = &z
)Proc
 &zselx = &z
 IF (&ZCMD = &Z)
     IF (&ZSEL NE &Z)
       if (&Zsel = '=')
           &Zsel = &osel
       &osel = &zsel
       &ZSEL = TRANS(&ZSEL RE,S.0
                           REA,S.0
                           READ,S.0
                           BR,S.1
                           BRO,S.1
                           BROW,S.1
                           ED,S.2
                           EDI,S.2
                           EDIT,S.2
                           PR,S.3
                           PRT,S.3
                           PRNT,S.3
                           PRIN,S.3
                           RN,S.4
                           REN,S.4
                           RENA,S.4
                           RENM,S.4
                           CO,S.5
                           COP,S.5
                           CPY,S.5
                           COPY,S.5
                           PDS,S.6
                           DEL,S.7
                           PTF,S.8
                           UNT,S.8
                           UNTE,S.8
                           UNTER,S.8
                           UNTERS,S.8
                           UNTERSE,S.8
                           XMIT,S.9
                           *,*)
       &ZT = TRUNC(&Zsel,1)
        IF (&ZT = 'L')
           &LOGSEL = .TRAIL
           &ZSEL = 'L'
       &ZT = TRANS(TRUNC(&ZSEL,'.') A,A L,L E,E M,M MF,MF DF,DF
             R,R B,B D,D F,F P,P S,S DM,DM DN,DN C,C U,U *,X)
        IF (&ZT = X)
         &mailsmsg = 'Error'
         &ml = 'A,B,C,D,DM,E,F,L,M,MF,P,S,U,DF,DN'
         &maillmsg = 'Enter a valid value: &ml Was &zsel'
     IF (&ZSEL NE &Z)
       &ZSEL = TRUNC(&ZSEL,'.')
       &SUBOPT = .TRAIL
       VER (&ZSEL,LIST,R,A,B,D,E,F,P,S,DF,DM,DN,C,M,MF,L,U,MSG=MAIL000)
       IF (&SUBOPT NE &Z)
          VER (&ZSEL,LIST,S,MSG=MAIL001)
          VER (&SUBOPT,RANGE,0,9,MSG=MAIL001)
)End
./ ADD NAME=TMAILTBX 0150-90036-93158-1242-00087-00048-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(asis ) color(turq)
   $ type(text) intens(high) caps(off) just(asis ) color(turq)
   ! type(output) intens(high) caps(off) just(asis ) color(white)
   ` type(output) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ~ type(text) intens(high) caps(off) just(asis ) color(turq)
   # type(output) intens(high) color(turq)
)Body
$Primary Mail Table+#tmver+-%TSO/E-Mail Processing+---------------------
%Command ===>_zcmd                                            %Scroll ==
%
+Command Options: %Find/LOG/REFresh/SENDmail/SORT/DEFaults      SHORT/LO
+Line Options:%S+Read%A+Answer%C+Change%D+Delete%E+Edit%F+Forward%L+Log%
+
%Sel  %FromUser      Node     ToUser   Node      Date     Time       Las
+
)Model
@z   +!z            !z       !z       !z        !z       !z         !z
+     ^z
+Target Dataset:    `z
)Init
  .HELP = TMAILHLP
  .ZVARS = '(ZSEL MAILFID MAILFLOC MAILTID MAILTLOC MAILDATD +
  MAILTIME MAILACTN MAILSUBJ MAILTDS)'
  &mamt = CSR
  &zsel = &z
)Proc
 &zselx = &z
 IF (&ZCMD = &Z)
     IF (&ZSEL NE &Z)
       if (&Zsel = '=')
           &Zsel = &osel
       &osel = &zsel
       &ZSEL = TRANS(&ZSEL RE,S.0
                           REA,S.0
                           READ,S.0
                           BR,S.1
                           BRO,S.1
                           BROW,S.1
                           ED,S.2
                           EDI,S.2
                           EDIT,S.2
                           PR,S.3
                           PRT,S.3
                           PRNT,S.3
                           PRIN,S.3
                           RN,S.4
                           REN,S.4
                           RENA,S.4
                           RENM,S.4
                           CO,S.5
                           COP,S.5
                           CPY,S.5
                           COPY,S.5
                           PDS,S.6
                           DEL,S.7
                           PTF,S.8
                           UNT,S.8
                           UNTE,S.8
                           UNTER,S.8
                           UNTERS,S.8
                           UNTERSE,S.8
                           XMIT,S.9
                           *,*)
       &ZT = TRUNC(&Zsel,1)
        IF (&ZT = 'L')
           &LOGSEL = .TRAIL
           &ZSEL = 'L'
       &ZT = TRANS(TRUNC(&ZSEL,'.') A,A L,L E,E M,M MF,MF DF,DF
             R,R B,B D,D F,F P,P S,S DM,DM DN,DN C,C U,U *,X)
        IF (&ZT = X)
         &mailsmsg = 'Error'
         &ml = 'A,B,C,D,DM,E,F,L,M,MF,P,S,U,DF,DN'
         &maillmsg = 'Enter a valid value: &ml Was &zsel'
     IF (&ZSEL NE &Z)
       &ZSEL = TRUNC(&ZSEL,'.')
       &SUBOPT = .TRAIL
       VER (&ZSEL,LIST,R,A,B,D,E,F,P,S,DF,DM,DN,C,M,MF,L,U,MSG=MAIL000)
       IF (&SUBOPT NE &Z)
          VER (&ZSEL,LIST,S,MSG=MAIL001)
          VER (&SUBOPT,RANGE,0,9,MSG=MAIL001)
)End
./ ADD NAME=TMAILTLG 0104-90120-91269-1309-00035-00030-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(output) intens(high) caps(off) just(asis ) color(turq)
   ` type(text)   intens(low ) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
   ! type(text) intens(high) caps(off) just(asis ) color(white)
)BODY
+-------------------% TSO/E-Mail Log Data Set Disposition +-------------
%Option ===>_zcmd


`    Specify!D`to~delete`or!E`to~edit`your current TSO Mail LOG data set

`    This is the first time that you have used the TSO/E ISPF Mail Dialo
`    and a pre-existing TSO Mail LOG data set has been detected.  This d
`    set must either be deleted or all old and/or unwanted messages shou
`    be deleted before you continue with this dialog.  The reason is tha
`    this LOG data set is used to record all messages which are Transmit
`    or Received and this dialog will use this same data set to copy the
`    messages into an ISPF Table.  The LOG data set is then deleted once
`    copy has been completed.
`
`    If you elect to continue without cleaning out all of the old and/or
`    unwanted messages the dialog make take a while to copy all of the
`    messages into the ISPF Table.
`
`    The default (PF3) is to continue with the complete LOG data set.
+
)INIT
)PROC
  VER (&ZCMD,NB,LIST,D,E)
)END
./ ADD NAME=TMAILUNT 0103-92121-92132-1330-00029-00029-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
   _ type( input) intens(high) caps(on ) just(left )
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   | type(output) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
)Body
+----------------------% TSO/E-Mail File UnTerse Prompt +---------------
%Command ===>_zcmd
%
%
% The following data set has been requested to be un-tersed:
%
`      Original Data Set:  !mailfds

`      TSO Mail Data Set:  !mailtds


%  Enter the desired output data set name:
%      ===>_targdsn
)Init
  .cursor = targdsn
)Proc
 &zcmd = &z
 VER (&TARGDSN,NB,dsname)
)End
./ ADD NAME=TMAILUSE 0102-90061-91269-1309-00028-00028-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(left ) color(white)
   | type(text) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(high) caps(off) just(right) color(white)
         hilite(uscore)
   ~ type(text) intens(high) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+--------------------------% TSO/E-Mail Processing +--------------------
%
%
%
%
%              Your are currently using your`TSO/E-Mail%on your split
%                               ISPF Session.

%              Use PF9 to swap to your other session and then use the
%                          TSO/E-Mail`Refresh%command.


%                            Press`END%to continue
)Init
)Proc
)End
./ ADD NAME=TMAILWAT 0110-89305-91269-1310-00031-00025-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type(output) intens(high) caps(off) just(left ) color(white)
   | type(text) intens(low ) caps(off) just(asis ) color(white)
   ` type(output) intens(high) caps(off) just(right) color(white)
         hilite(uscore)
   ~ type(text) intens(high) caps(off) just(asis ) color(white)
         hilite(uscore)
)Body
+--------------------------% TSO/E-Mail Processing +--------------------
%
%
%
%
%              Your~TSO/E-MAIL%is now being processed for^userid  %



%                              Please be patient



%                       Messages Processed: `mcount%

                           Files Processed: `fcount%
)Init
)Proc
)End
./ ADD NAME=TMIBMFAX 0100-93140-93140-1057-00050-00050-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ~ TYPE(INPUT) CAPS(OFF) INTENS(HIGH) COLOR(yellow) hilite(uscore)
   ` TYPE(INPUT) CAPS(ON)  INTENS(HIGH) COLOR(yellow) hilite(uscore)
   # area(SCRL) Extend(ON)
)Body
+----------------------% TSO/E Mail IBM Fax Information +---------------
%Command ===>_zcmd
%
^     Verify your IBM Fax Information:
+
#help ------------------------------------------------------------------
)Area Help
^     Fax Phone:      ==> ~FAXPHONE                               +
+
^     Subject:        ==> ~FAXSUB
+
^     To Information: ==> ~FAXT1
^                     ==> ~FAXT2
^                     ==> ~FAXT3
^                     ==> ~FAXT4
^                     ==> ~FAXT5
^                     ==> ~FAXT6
^                     ==> ~FAXT7
^                     ==> ~FAXT8
+
^     Your Name:      ==> ~FAXF1
^                     ==> ~FAXF2
^                     ==> ~FAXF3
^                     ==> ~FAXF4
^                     ==> ~FAXF5
^                     ==> ~FAXF6
^                     ==> ~FAXF7
^                     ==> ~FAXF8
+
+  You can alter all of this information in the body of your note if you
+  need to.
+
)Init
  .HELP = TMAILHF1
  IF (&FAXPHONE = &Z)
     &FAXPHONE = 'XXX XXX-XXXX'
)Proc
    VPUT (FAXF1 FAXF2 FAXF3 FAXF4 FAXF5 FAXF6 FAXF7 FAXF8) PROFILE
)End
./ ADD NAME=TMLOGC   0103-91179-91269-1327-00031-00035-00000-$A1238
)Attr
   @ type( input) intens(high) caps(on ) just(left ) pad('_')
   ^ type( input) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   # type(text) intens(low ) caps(off) just(asis ) color(white)
   ` type(text) intens(high) caps(off) just(asis ) color(turq)
   ~ type(text) intens(low ) caps(off) just(asis ) color(turq)
         hilite(uscore)
)Body
+------------------------% TSO/E-Mail Log Processing +------------------
%Command ===>_zcmd

%
% Log Data Set Description%===>^logdesc
%
% Log Data Set Name:           !logds
%
%
%
` Notes:   The description will appear in the display of your TSO/E Mail
`          Logs and helps to easily identify the different logs you may
`          have created.
`
`          The log data set name may not be changed.
)Init
 .CURSOR = logdesc
 .HELP = TMAILH3
 &zcmd = &z
)Proc
 Ver (&logdesc,nb)
)End
./ ADD NAME=TMLOGHLP 0101-91182-91269-1328-00029-00029-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+-------------------------% TSO/E-Mail LOG Tutorial  +------------------
%Command ===>_zcmd
%

     TSO/E-Mail LOG Processing is an extension to the TSO/E-Mail ISPF di
     This facility allows received messages to be 'logged' or 'archived'
     into an alternate ISPF table and data set.  This logging can help t
     group messages by topics, sender, or any other grouping desired.


     The following tutorial options will be displayed in sequence or may
     be selected by number:

|    1 -^Commands          `Commands that may be entered on the Command
|    2 -^Message Options   `Selection options for individual messages/fi

)Init
)Proc
  &zsel = trans(&zcmd 1,tmlogh1 2,tmlogh2 *,?)
)End
./ ADD NAME=TMLOGHT  0105-91182-92280-1215-00030-00031-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+-------------------------% TSO/E-Mail LOG Tutorial +-------------------
%Command ===>_zcmd
%
`      The following commands may be entered on the ISPF Command line wh
`      in the TSO/E-Mail LOG Table display.

|      Action Command Options:

|     ^NEW                   `Create a new TSO/E-Mail LOG table and data
|     ^RENumber              `Renumber the log entries in the table.

|      Log selection options:

|     ^S`                    `Read the selected LOG
|     ^C                     `Change information about the selected LOG
|     ^D                     `Delete the selected LOG
|     ^I                     `Display information about the selected LOG

)Init
)Proc
)End
./ ADD NAME=TMLOGH1  0103-91182-92014-1036-00032-00037-00000-$A1238
)Attr Default(%+_)
/* % type(text  ) intens(high)              Defaults displayed for */
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ^ type(text) intens(high) caps(off) just(asis ) color(yellow)
   ! type(output) intens(low ) caps(off) just(asis ) color(white)
   | type(text  ) intens(high) color(white)
   ` type(text) intens(low ) caps(off) just(asis ) color(turq)
)Body
+-------------------------% TSO/E-Mail LOG Tutorial +-------------------
%Command ===>_zcmd
%
`      The following commands may be entered on the ISPF Command line wh
`      in the TSO/E-Mail LOG Table display.  Information on them will be
`      presented in sequence or selected by number.

|      Action Command Options:

