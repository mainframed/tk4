%TUTORIAL ------------------ IPCS - MASK Operand ----------------------
%Command ===>_ZCMD
%
%MASK+defines a value that is logically ANDed with both comparands prior
+performing the comparison. The mask must be the same size as the data i
+being compared.
+
+The MASK is defined as a literal value.
+
+The following topic will be presented only if selected by number:
+
%   1+- Specifying a literal value
)PROC

 &ZSEL = TRANS(&ZCMD
         1,*TPCVALUE
         *,?)
)END
