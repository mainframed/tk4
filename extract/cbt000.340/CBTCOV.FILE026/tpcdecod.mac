%TUTORIAL ----------- IPCS - Subcommand VERBEXIT DECODE ---------------
%Command ===>_ZCMD
%
%DECODE+"disassembles" memory from the dump into meaningful assembler mn
+This is useful in debugging code for which there is no source available
+
+You may optionally specify from which address space the dumped data is
+disassembled. Also, to help distinguish between constants and instructi
+may optionally inhibit certain classes of instructions from being disas
+(they will appear as constants).
)END
