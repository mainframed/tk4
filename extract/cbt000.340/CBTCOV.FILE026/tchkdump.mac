%Tutorial ------------------  CHKDUMP  ---------------------------------
+
+
+
+   CHKDUMP is a program which will read thru all SYS1.DUMPxx files
+   on a specified volume and report information from the TITLE and
+   SUMDUMP records to ease in determining the cause of the dump.
+
+   By specifying a system id then you do not have to specify a volume
+   serial number as the CHKDUMP clist executed by this panel will fill
+   in the active volser for that system.
+
+   The Panel will validate that if system is specified that it belongsq
+   to the specifed center.
+
+
+
+
+     Depress PF3 or PF15 to return to the AUTONUM panel
)END
