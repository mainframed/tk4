+--------------------------% IEBCOPY COPY Tutorial +--------------------
%
+   IEBCOPY COPY Function.
+
+   IEBCOPY will copy any partitioned dataset from one library to an out
+   partitioned dataset, or to a sequential dataset (unload format).
+
+   If the input dataset is specified with a member name of * then the
+   entire dataset is copied, otherwise you will be prompted with a memb
+   selection list.
+
+   If the output dataset is a sequential format (unload format) then yo
+   will need to reload it using IEBCOPY as well.
+
)PROC
  &UP = IEBCOPYT
  &CONT = TCOPY2
)END
