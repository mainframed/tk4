+---------------------------% ALTERMOD Load Library +-------------------
%
+   ALTERMOD.
+
+   This function of IEBCOPY will modify the RLD count information for e
+   or select members of a Load Library.  This is done to improve the
+   of program fetch, and need not be done if the module was link edited
+   the DFP version of the linkage editor, or if the library was created
+   the IEBCOPY COPYMOD function.
)PROC
  &UP = TCOPY2
  &CONT = IEBCOPYT
)END
