%TUTORIAL --------------- IPCS - Attribute Keywords -------------------
%Command ===>_ZCMD
%
+                                 (continued)
+
%UNSIGNED:+an unsigned number or numbers. If you display or print the da
+shown as an unsigned number translated to decimal. If you specify%LENGT
+can range from one through four.
+
%POINTER×PTR:+a pointer or pointers. If you display or print the data it
+shown in hexadecimal format. If you specify%LENGTH,+it can range from o
+through four.
+
%MODULE <(name)>:+a module. If you display or print the data, each line
+four or eight words in hexadecimal format following by their character
+equivalent. If you specify a%name,+IPCS automatically creates a storage
+entry.
+
%STRUCTURE <(name)>:+a control block. If you display or print the data,
+line contains four or eight words in hexadecimal format followed by the
+character equivalent. If you specify a%name,+IPCS automatically creates
+storage map entry for it.
)END
