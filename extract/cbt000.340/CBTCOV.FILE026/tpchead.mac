%TUTORIAL ---------------- IPCS - HEADER Parameter --------------------
%Command ===>_ZCMD
%
%                                   HEADER
+
%HEADER+specifies that the address is in the header record for a dump. W
+use this keyword, the subcommand accesses data in the header record fro
+zero. That is, the subcommand processes data in the header record at th
+address you specify.
)END
