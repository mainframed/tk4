)BODY EXPAND(@@)
%TUTORIAL @-@  Rockwell added STATUS Primary Command  @-@ TUTORIAL
%OPTION ===>_ZCMD
%
+  ACTION mode of STATUS ISPF command shows a list of jobs.
+  The following line commands may be entered for each job:
%
%    C+ -  CANCEL running batch job
%    Z+ -  CANCEL running batch job and PURGE jobs output
+          (Use if job has completed to purge jobs output)
+
%enter+ -  to update job status display
%
)PROC
  .RESP = END
)END
