%TUTORIAL ---------------- IPCS - VERIFY Parameter --------------------
%Command ===>_ZCMD
%
%VERIFY+controls the optional dumping of data by the following subcomman
+
+   %FIND    +- locate data in a dump
+   %FINDMOD +- locate a module name
+   %FINDUCB +- locate a UCB
+   %LISTMAP +- list storage map entries
+   %RUNCHAIN+- search a chain of control blocks
+
+These subcommands all generate information that may be of value without
+generating dump output.
)END
