%TUTORIAL ---------------- IPCS - LENGTH Parameter --------------------
%Command ===>_ZCMD
%
%LENGTH+specifies the length of the storage area to be used by dump anal
+subcommands. The length can be specified in decimal (nnn), hexadecimal
+(X'xxx'), or binary (B'bbb'). The default is 4.
+
+The following list explains valid address length ranges.
+
+. If the address is absolute, real, or virtual, the length can range fr
+through 2**31.
+
+. If the address is in the status record, the length can range from 1 t
+4096.
+
+. If the address is in the dump header record, the length can range fro
+through 4104.
)END
