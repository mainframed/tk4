+-----------------%ISPFLIST Select Option Tutorial+---------------------
%Command%===>_ZCMD
+
%  OPTION  +Description                    %OPTION  +Description
%  B       +ISPF BROWSE the dataset        %HM      +HSM Migrate a datas
%  C       +Compress the dataset           %PRINT   +Generalized Print D
%  COPY    +Copy the dataset               %R       +Recover a Migrated
%  D       +Delete and Uncatalog dataset   %        +(archive recovery a
           +(HDELETE if VOL=MIGRAT)        %REL     +Release unused spac
%  DN      +Same as D but with Noconfirm   %RENAME  +Rename dataset
%  E       +ISPF EDIT the dataset          %S       +Select Options Menu
%  EMPTY   +Empty a dataset                %STAT    +Provide dataset sta
%  EXPAND  +Expand a PDS directory         %T       +Transfer dataset
           +(non-destructive)              %U       +Uncatalog the datas
%  X       +Remove dataset from display

%  =       +Repeat last selection command
+  Or%any+command/clist which will be invoked as command DSN or command
)PROC
     &CONT = $TFLIST3
)END
