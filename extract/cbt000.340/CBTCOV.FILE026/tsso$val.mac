%TUTORIAL ------------------- TSSO  VALLOC ----------------------------
%command ===>_ZCMD
+

  The VALLOC command will display the UCB device for a specified volser
  and all address spaces which are allocated to it.

% Syntax:

%         VALLOC volume
+
  Optional:  volume
  Default:   if no volume name is specified, the IPL volume is assumed

  Where:
   volume  - is the volser of the DASD volume.








)PROC
  &ZCONT = TSSO$VOL
  IF (&ZERRHM = TSSO$VAL) .RESP = END
)END
