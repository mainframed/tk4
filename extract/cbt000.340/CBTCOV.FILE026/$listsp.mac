)BODY EXPAND(@@)
%TUTORIAL @-@  Rockwell added LISTSP Primary Command  @-@ TUTORIAL
%OPTION ===>_ZCMD
%
+  The%LISTSP+command allows you to browse the output of the TSO LISTSP
   An ACTION display mode is available to process selected datasets.
   This includes Browse, Edit, PDS, Free, Compress, Trim, Migrate, etc.

+Syntax:
%    LISTSP
%    LISTSP dataset
%    LISTSP dataset-pattern
%    LISTSP dataset REAL×NOREAL MIG×NOMIG DETAIL×NODETAIL SUMMARY
%                   PRINT(dataset)×PRINT(*)×FILE×PRIN×PRI×PR×P
%                   VOL(volser)×VOLUME(volser)
+
+Operands:
+
+   See TSOHELP LISTSP for information
+
+Note: LISTSP without any operands will display a panel for dataset entr
       This panel will allow for entry of a list of dataset names.
+      Rockwell ISPF command BRPRINT can be used to obtain hardcopy in B
+
)PROC
  &ZCONT = $LISTSP2
)END
