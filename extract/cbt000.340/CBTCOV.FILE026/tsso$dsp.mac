%TUTORIAL ------------------- TSSO  SYSDISP ---------------------------
%command ===>_ZCMD
+

    The SYSDISP command will return to the user information about the
    system on which the command is executed.

    Information returned is:

         IPL         date and time
         SYSGEN      date and time
         SMF id      for the processor
         CVT SYSNAME for processor
         Information about each online cpu

%  Syntax:

%       SYSDISP
+




)PROC
  &ZCONT = TSSO$IND
  IF (&ZERRHM = TSSO$DSP) .RESP = END
)END
