%TUTORIAL ------------------ IPCS - Dumped ASIDs ----------------------
%Command ===>_ZCMD
%
+The%Dumped ASIDs+panel displays all ASIDs and jobnames present in the d
+
+You may use the%scroll+commands to position the display.
+
+The%FIND+primary command will position the display to an ASID or jobnam
+
+The%LOCATE+primary command will position the display to the default ASI
+one containing an error.
+
+The%S+line command will select the corresponding ASID as the current de
+executing the SETDEF subcommand.
)END
