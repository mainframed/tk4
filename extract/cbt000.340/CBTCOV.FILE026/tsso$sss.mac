%TUTORIAL ------------------- TSSO  SHOWSS   --------------------------
%command ===>_ZCMD
+

  The SHOWSS command displays the names of all subsystems in the
  system and the addresses of each subsystem's SSCT entry and SSVT.

%  Syntax:

%         SHOWSS
+
)PROC
  &ZCONT = TSSO$SMA
  IF (&ZERRHM = TSSO$SSS) .RESP = END
)END
