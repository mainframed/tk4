%TUTORIAL ------------------- TSSO  RSV   -----------------------------
%command ===>_ZCMD
+

  The RSV command displays all active device reserves.

%  Syntax:

%          RSV
+
)PROC
  &ZCONT = TSSO$SSS
  IF (&ZERRHM = TSSO$RSV) .RESP = END
)END
