%TUTORIAL -------------- IPCS - Subcommand IOSCHECK -------------------
%Command ===>_ZCMD
%
%IOSCHECK+formats the contents of specific I/O supervisor (IOS) control
+and related diagnostic information.
+
+If%no option+is specified, the default is to format IOCOM, IOCW, IOS le
+definitions, and IOWA.
+
%VALIDATE +- checks the validity of IOS control blocks.  It formats%inva
+            control blocks.
+
%ACTVUCBS +- performs VALIDATE processing and formats%active+UCBs and
+            associated control blocks.
+
%ALLUCBS  +- performs VALIDATE processing and formats%all+UCBs and assoc
+            control blocks.
+
%SMGRBLKS +- formats entries in the IOS storage manager page table.
+
%EXCEPTION+- checks the validity of IOS control blocks.  It does%not+for
+            invalid control blocks.  It also%suppresses+default output.
)END
