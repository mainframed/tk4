+----------------% User Dialog Primary Option Menu Tutorial +-----------
%Command ===>_ZCMD
%
+  This is a USER definable ISPF Primary Option Menu.
+
+  To customize this menu for your use you need to update the following
+
%  Field 1:+  This is a 1 character selection id (e.g. 0-9, A-Z)
+
%  Field 2:+  This is a Description of the application
+             (e.g. Maintiance of My Datasets)
+
%  Field 3:+  This is the ISPF application to be invoked and must be spe
+             using correct ISPF Panel syntax.  For example:
+
+             to invoke a CLIST specify:        % CMD(clist-name)
+             to invoke an ISPF Program specify % PGM(program-name)
+             to invoke an ISPF Panel specify   % PANEL(panel-name)
+
)END
