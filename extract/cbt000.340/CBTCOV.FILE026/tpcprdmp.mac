%TUTORIAL ----------------- AMDPRDMP - Print Dump ---------------------
%Command ===>_ZCMD
%
%AMDPRDMP+is a service aid program that allows you to:
+. Format and print dump data sets
+. Edit and print system and user trace records created by GTF.
+
+AMDPRDMP is batch-oriented.%IPCS+is recommended for interactive use, be
+AMDPRDMP is slower and produces additional output which may be undesire
+However, AMDPRDMP provides some facilities not supported by IPCS:
+   CPUDATA  - Processor related information
+   EDIT     - GTF trace data
+   LPAMAP   - Link Pack Area map
)END
