%TUTORIAL ------------ DRAW - CALLING FROM OTHER MACROS ---------------
%OPTION  ===>_ZCMD
%
+  The%DRAW+edit macro can be called from other macros to draw special s
   such as linked list nodes.  Your macro can change the current charact
   and even modify particular drawing characters.

+  The following page shows an example CLIST that uses the%DRAW+macro to
   a special symbol.

                           (continued on next page)
)PROC
  &ZUP = DRAW0000
  &ZCONT = DRAW1510
)END
