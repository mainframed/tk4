%TUTORIAL ------------ IPCS - Subcommand VERBEXIT LOGDATA -------------
%Command ===>_ZCMD
%
%LOGDATA+formats the in-storage LOGREC buffer records in the same manner
+formats the SYS1.LOGREC records.
+
+The summary of records found, located at the end of the LOGDATA output,
+indicates the number and type of records found in the in-storage LOGREC
+LOGDATA does not format all record types, so the number of records list
+the summary may not equal the number of records formatted.
)END
