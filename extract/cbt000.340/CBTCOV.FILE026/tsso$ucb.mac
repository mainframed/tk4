%TUTORIAL ------------------- TSSO  UCB   -----------------------------
%command ===>_ZCMD
+

 The UCB command will display the UCB information for a specified UCB or

% Syntax:
%         UCB operand
+
          Operand may be a UCB address, UCB mask, or a Volser name.
          If no operand is specified,
          UCB information for the current IPL volume is displayed.

  Operand format:

   ucb        nnn   is the UCB address to be displayed (e.g. 267)
   ucb-mask   nnX   (e.g. 26X) will  cause a full string of UCBs to be s

   volser     is the volser whose UCB information is to be displayed.




)PROC
  &ZCONT = TSSO$VAL
  IF (&ZERRHM = TSSO$UCB) .RESP = END
)END
