+-------------------------% IPCS DROPDUMP Command +---------------------
%Command ===>_ZCMD
+
+Enter/Verify DROPDUMP options:
+
+   Dataset Name%===>_DROPDSN
+        Records%===>_DROPREC     + ALL / TRANSLATION
+
+   If Dataset Name is blank then the current dataset's records are dele
+
+   Records=ALL indicates that all records associated with the dump are
                be deleted.
+   Records=TRANSLATION indicates that only records generated by an IPCS
                translation process are to be deleted.
+
+   NOTE: If Records=TRANSLATION is used DROPSYM, EQUATE and DROPMAP mus
          used.
)INIT
 .CURSOR = DROPDSN
 &DROPREC = TRANS(TRUNC(&DROPREC,1) ' ',ALL A,ALL T,TRANSLATION *,*)
)PROC
 &DROPREC = TRANS(TRUNC(&DROPREC,1) A,ALL T,TRANSLATION *,*)
 VER (&DROPREC,LIST,ALL,TRANSLATION)
 VER (&DROPDSN,DSNAME)
 &ZSEL = 'PGM(BLSGSCMD) PARM(%IPDRPDMP)'
)END
