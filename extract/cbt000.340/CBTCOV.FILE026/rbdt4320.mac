%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×  Specifying information for a new   ×
%                    ×               dataset               ×
%                    ×             (continued)             ×
%                    ---------------------------------------
+
  The following three fields are only used if you do%not+specify a model
  set name below:

+RECORD FORMAT   %===>+

  The type of record format--any normal recfm is acceptable

+RECORD LENGTH   %===>+

  The logical record length for the dataset.
+
+                           (continued on the next page)
)PROC
 &ZUP = RBDT0000
 &ZCONT = RBDT4330
)END
