%TUTORIAL --------------------- IPCS - Symbols ------------------------
%Command ===>_ZCMD
%
+IPCS maintains a%symbol table+for every dump that you process. The symb
+is kept in your dump directory data set and contains information that m
+future references to that dump faster and more efficient. When you firs
+process a new dump (one you have never processed before), IPCS reads th
+and initializes its symbol table.
+
+A symbol is a label associated with a location in a dump. The label can
+maximum of 31 alphameric characters and the first character must be alp
+
+Also recorded in the symbol table, for each symbol, are attributes of t
+storage it is associated with.
)END
