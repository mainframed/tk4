%TUTORIAL -------------- IPCS - Subcommand RUNCHAIN -------------------
%Command ===>_ZCMD
%
%LINK+specifies the location in each control block of a fullword pointer
+next control block in the chain. The location is an offset relative to
+first byte of the control block; the offset of the first byte is 0. The
+can be a maximum of 16,777,215 and can be specified in decimal, hexadec
+(X'xxx...'), or binary (B'bbb...').
+
+The default is%0.+
)END
