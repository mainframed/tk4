)Attr
   ! type( input) intens(high) caps(on ) just(left ) pad('''')
   ^ type( input) intens(low ) caps(off) just(asis ) pad('')
)Body  Expand(//)
+-/-/-+ Table of Available FONTs for%&RMTID +-/-/-
%Option ===>_zcmd                                 / /%Scroll ===>_amt +
%
+  Enter%ADD+on command line to%add+a new font or Enter%D+by FONT to%DEL
+
%                                              Rotation
%Sel      FONT                                 PHY   LOG
+
)Model
!z   +   ^z                                   ^z    ^z
)Init
  .ZVARS = '(SEL FONT PHY LOG)'
  &amt = PAGE
)Proc
)End
