%TUTORIAL ----------------- IPCS - Dump Directory ---------------------
%Command ===>_ZCMD
%
+The%Dump Directory+panel displays all the dump data sets in your person
+dump directory. You can use the%scroll+commands to position the display
+
+Primary commands:
%SHORT+displays one line per directory entry. Only half the title is dis
%LONG+displays the full title. If the display is wide enough, one line p
+is used. Otherwise, two line are used.
+
+Line commands:
%S+selects the dump for analysis.
%I+displays a panel with additional information about a dump.
%D+specifys use of the%DROPDUMP+subcommand to delete records describing
+from your dump directory.
+
+The following topics will be presented only if selected by number:
%   1+-analyzing a dump
)PROC

 &ZSEL = TRANS(&ZCMD
         1,*TPCOPT
         *,?)

)END
