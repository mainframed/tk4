%TUTORIAL --------------- IPCS - Subcommand FINDUCB -------------------
%Command ===>_ZCMD
%
%FINDUCB+locates the unit control block for a specified device. When the
+subcommand finds the control block, it creates an entry in the symbol t
+UCBddd, where ddd is the device number.
+
+The following topic will be presented only if selected by number:
%   1+-DISPLAY/VERIFY - control dump output
)PROC

 &ZSEL = TRANS(&ZCMD,
         1,*TPCDATA
         *,?)

)END
