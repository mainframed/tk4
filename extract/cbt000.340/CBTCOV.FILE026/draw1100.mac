%TUTORIAL -------------------- DRAW - FILL/OFILL ----------------------
%OPTION  ===>_ZCMD
%
+  The%FILL+and%OFILL+functions are used to fill the marked block with t
   current fill character.  You can change the current fill character wi
   the%OPTIONS+function.

   The%FILL+function will fill the block destroying any characters that
   within the block.

   The%OFILL+(Overlay fill) function will fill the block but will not ch
   any non-blank characters within the block.
)PROC
  &ZUP = DRAW0000
)END
