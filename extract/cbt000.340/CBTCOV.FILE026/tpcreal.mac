%TUTORIAL ----------------- IPCS - REAL Parameter ---------------------
%Command ===>_ZCMD
%
%                               REAL <CPU(cpu)>
+
%REAL+specifies that the address is in real storage in a%stand-alone+dum
+is to simulate prefixing for the specified or current default CPU.
+
+If you omit the%CPU+keyword, the subcommand uses the default CPU.
)END
