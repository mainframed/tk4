%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    × Specifying FROM dataset information ×
%                    ×            ISPF Libraries           ×
%                    ×              (continued)            ×
%                    ---------------------------------------
+
+  A member name enclosed in parentheses may follow the data set name (w
   the apostrophes, if they are present) for partitioned data sets.  If
   partitioned data set is entered without a member name, a member selec
   list will be displayed.  Use a member name of asterisk (*) to specify
   members".  Example:

      %DATA SET NAME ===> 'sys1.maclib(*)'+

+                        (continued on next page)
)PROC
 &ZUP = RBDT0000
 &ZCONT = RBDT3220
)END
