)BODY DEFAULT(%~_)
%TUTORIAL ----------------- IPCS - Array Keywords ---------------------
%Command ===>_ZCMD
%
~The%array~keywords indicate whether the data consists of a single item
~adjacent, similar items. The default is%SCALAR.~
~
%SCALAR:~a single entity with nonrepeating fields.
~
%DIMENSION (nnn) × MULTIPLE (nnn):~an array of dimension nnn. The number
~be a maximum of 2**31 and can be specified in decimal, hexadecimal (X'x
~or binary (B'bbb...'). Each arrary element occupies the length of stora
~the specified address range or the length specified with the%LENGTH~key
~The total size of the array is the size of an element, multiplied by nn
~
%ENTRY (xx<:yy>) × ENTRIES (xx<:yy>):~an array. You specify the number o
~elements in the array with the values xx and yy. The value xx must be <
~These values can range from -2**31 to 2**31-1, and can be specified usi
~signed decimal (<+×->nnn), hexadecimal (X'<+×->xxx'), or binary (B'<+×-
~The size of the total array is the length of storage in the specified a
~range or specified with the%LENGTH~keyword, multiplied by the number of
~elements.
)END
