%TUTORIAL --------------- IPCS - Attribute Keywords -------------------
%Command ===>_ZCMD
%
%Attribute+keywords designate the type of data, and thus, the way IPCS s
+format the storage in which the data resides. The default is%AREA.+
+
%AREA <(name)>:+an area of storage (a subpool, a buffer, etc.) that is n
+module or control block. If you specify a%name,+IPCS automatically crea
+storage map entry for it.  If you display or print the area, each line
+four or eight words in hexadecimal format followed by their character
+equivalent.
+
%B×BIT×X×HEXADECIMAL:+bit string data. If you display or print the data
+shown in hexadecimal format.
+
%C×CHARACTER:+character string data. If you display or print the data it
+shown in character format.
+
%F×SIGNED:+a signed number or numbers. If you display or print the data
+shown as a signed number or numbers translated to decimal. If you
+specify%LENGTH,+it must be two or four.
+
+                               (continued)
)PROC

 &ZCONT = TPCATTR2

)END
