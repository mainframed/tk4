%TUTORIAL ---------- IPCS - Subcommand VERBEXIT RSMDATA ---------------
%Command ===>_ZCMD
%
%RSMDATA+checks the validity of selected real and extended storage manag
+(RSM) control blocks, and formats and prints selected RSM control block
+
+There are two parameters:%VERIFY+and%PRINT.+The PRINT parameter produce
+dumps; you would specify the PRINT parameter only when you cannot obtai
+necessary information by using the VERIFY parameter.
)END
