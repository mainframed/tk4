%TUTORIAL -------------- IPCS - Subcommand RUNCHAIN -------------------
%Command ===>_ZCMD
%
%EXEC+specifies that either a CLIST statement or an IPCS subcommand is t
+appended to the RUNCHAIN subcommand invocation. The appended CLIST or
+subcommand executes after the RUNCHAIN subcommand process completes. Op
+and/or keywords can accompany the CLIST or the IPCS subcommand.
+
+The EXEC keyword permits multiple levels of control block processing th
+chaining of the RUNCHAIN subcommand.
)END
