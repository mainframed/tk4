%TUTORIAL ------------------------- COMPASM ---------------------------
%Command ===>_ZCMD
%
%TRANSLATE ===> <YES×NO>+specifies translation of non-assembler characte
+blanks prior to comparison. SYSPRINT/SYSPUNCH output is not affected.
+
%TERM ===> <YES×NO>+specifies summary output directly to the terminal. T
+includes error messages, parameters used, and module comparison statist
+output is written to SYSTERM.
+
%LIST ===> <YES×NO×ALL>+specifies whether or not a list of statement dif
+is to be produced. %LIST ===> ALL+specifies that matching statements wi
+be listed.  The output is written to SYSPRINT.
+
%LINECOUNT ===> nn+specifies the number of lines per page for%LIST.+
+
%DECK ===> <YES×NO>+specifies whether or not an update deck is to be pro
+which converts the old version of the module into the new version. The
+is written to SYSPUNCH.
+
%FORMAT ===> <SMP×UPDATE>+specifies whether the%DECK+output is in SMP or
+IEBUPDTE format.
+                          (Continued on next panel)
)PROC
 &ZCONT = COMPASM3
)END
