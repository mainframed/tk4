%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×   Selection List for an existing,   ×
%                    ×          available dataset.         ×
%                    ---------------------------------------
+
  When the FROM dataset exists at the center where you are logged on, an
  it is cataloged, you will be presented with a scrollable selection scr
  of all current members of the dataset. Move the cursor to the beginnin
  the line for each member to be selected (S) or excluded (E) and type t
  appropriate letter.%NOTE:+You may%not+mix S and E entries for the same
  transaction. When S is user, rename may be indicated by filling in the
  Action/Newname field for any member.

+ When all selections or exclusions have been indicated on the screen, y
  may:

     Enter   - will cause any marked members to be noted; the screen wil
               reappear.
     Scroll  - will cause any marked members to be noted; screen will sc
  %ENTER+for more information
)PROC
   &ZUP = RBDT0000
   &ZCONT = RBDT3411
)END
