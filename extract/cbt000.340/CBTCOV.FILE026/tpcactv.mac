%TUTORIAL -------------- IPCS - ACTIVE MAIN STORAGE -------------------
%Command ===>_ZCMD
%
%ACTIVE+specifies the main storage for the address space in which IPCS i
+currently executing and allow you to access that active storage as the
+source. You can access private storage and any common storage accessibl
+unauthorized program.
+
+IPCS%does not+create a storage map when this keyword is entered. IPCS%d
+maintain a symbol table but limits its automatic creation of symbols in
+table.
+
+The following topics will be presented only if selected by number:
%   1+-analyzing a dump
)PROC

 &ZSEL = TRANS(&ZCMD
         1,*TPCOPT
         *,?)

)END
