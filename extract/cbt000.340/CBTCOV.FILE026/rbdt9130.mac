%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×              Operands               ×
%                    ---------------------------------------
+
% DAP SEQ×PDS
+
+ Use:        %Required
+
+ Section:    %FROM+or%TO
+
  Specifies the Dynamic Application Program for a transaction.

+ - To transfer a sequential data set, use SEQ
+ - To transfer an%entire+partitioned data set, use PDS
+ - To transfer several members of a partitioned data set, use PDS and t
+   PARMS parameter with the SELECT or EXCLUDE options.
    To transfer only one member of a partitioned data set:
       - to a partitioned data set, do the same as for several members
       - to a sequential data set, use SEQ and the MEMBER parameter (or
         parenthesize the member name as part of the dataset name)
)PROC
 &ZUP = RBDT9010
)END
