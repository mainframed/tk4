%TUTORIAL ----------- IPCS - Subcommand VERBEXIT DAEDATA --------------
%Command ===>_ZCMD
%
%DAEDATA+formats and prints the DAE (Dump Analysis and Elimination) data
+dumped system.
+
+This information is extracted from the dump header record and is format
+both MVS and RETAIN format.
+
+This symptom data is useful for
+   (1) searching the FE RETAIN data base,
+   (2) communicating with FE, and
+   (3) starting the problem determination process.
)END
