%TUTORIAL ----------------- IPCS - ASID Parameter ---------------------
%Command ===>_ZCMD
%
%                       ASID (asid) <CPU(cpu)×NOCPU>
+
%ASID+specifies that the address is in virtual storage. IPCS is to simul
+dynamic address translation for the specified address space number.
+
+The ASID can range from 1 through 32,767. To directly process summary d
+records, use the special ASID 65,530 (X'FFFA'). You can specify the ASI
+decimal, hexadecimal (X'xxx...'), or binary (B'bbb...').
+
+If the dump is a stand-alone dump and the%CPU+keyword is specified or i
+default, IPCS also simulates prefixing for a processor.
)END
