%----------------------  AMDPRDMP - Print Dump PRUCB   -----------------
%Command ===>_ZCMD
%
+                         Unit Control Blocks (UCB)
+
+Note: the default is an abbreviated list of%all+UCBs.
+
+Detailed list of selected UCBs:
+   DEVICE%===>_UCBDEV
+
+
+
+Note: devices are 3 digit hex numbers (nnn), 000-FFF.
+     %DEVICE+= nnn × nnn,nnn,... × nnn-nnn
)INIT
 .CURSOR = UCBDEV
 &UCBDEV = ''
)END
