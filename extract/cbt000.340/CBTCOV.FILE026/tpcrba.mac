%TUTORIAL ------------------ IPCS - RBA Parameter ---------------------
%Command ===>_ZCMD
%
%                             RBA <(0×rba-group)>
+
%RBA+specifies that the address is in relative byte address group number
+"rba-group". Each relative byte address group consists of up to 2**31 b
+from a data set as follows:
+
+RBA (0) contains the first 2**31 bytes.
+RBA (1) contains the second 2**31 bytes.
+RBA (2) contains the third 2**31 bytes.
+RBA (3) contains the fourth 2**31 bytes.
+...
+
+The group number can range from 0 through 2**24-1. If the group number
+omitted, it defaults to%0.+You can specify the group number in decimal,
+hexadecimal (X'xxx...'), or binary (B'bbb...').
)END
