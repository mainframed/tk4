%TUTORIAL ----------------------- DRAW - RESET ------------------------
%OPTION  ===>_ZCMD
%
+  The%RESET+function is used to remove the POINT label (.POINT) and/or
   MARK labels (.MARK, .MARKT, and .MARKB) and any special lines (MSG, N
   COLS, etc.).

   The%RESET+parameter can be followed by either%POINT+or%MARK+to indica
   which type of reset you wish.  If you do not indicate the type of res
   both POINT and MARK will be reset.

   The%RESET+function is most useful when you are using the%DRAW+edit ma
   from another edit macro.
)PROC
  &ZUP = DRAW0000
)END
