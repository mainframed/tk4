%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×              Operands               ×
%                    ---------------------------------------
+
% PARMS(R=Y  {SELECT×EXCLUDE   M=(member, . . .)} )
+
+ Use:        %Optional+
+
+ Type:       %PDS+
+
+ Section:    %FROM+
+
  For transactions involving all of a partitioned dataset, use R=Y if li
  named members are to be replaced, otherwise they will not be replaced.

  For a PDS transaction involving one or several members (not all), use
  as applicable, and use either SELECT or EXCLUDE and a member list. A
  maximum of 270 members may be supplied.
)PROC
 &ZUP = RBDT9010
)END
