+----------------% Tutorial for AOST Process Held Output +--------------
%Command===>_ZCMD
+
+      This application is similiar to the standard ISPF/PDF 3.8 option
+      except:
+
+      1) You can specify the DSN for the OUTLIST dataset
+      2) You can specify the space and DCB for the output dataset
+      3) Option S (SAVE) will function the same as blank but KEEP the d
+      4) Option L (STATUS) is only applicable to the specified job
+      5) You can requeue a job to a new DEST
+
)INIT
)PROC
)END
