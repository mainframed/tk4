+---------------------% COPY and REBLOCK Load Library +-----------------
%
+   Allocation of the output load library.
+
+   If the specified output load library is not currently allocated then
+   user will be prompted with the COPYMOD allocation panel which will h
+   been initialized with the SPACE and DCB values derived from the inpu
+   load library.
+
+   Any of the values may be altered.
+
+   After the values have been altered, or if no change is desired then
+   ENTER key should be pressed and the COPYMOD function will allocate t
+   output load library and proceed with the copy.
)PROC
  &UP = COPYMDT
  &CONT = COPYMDTS
)END
