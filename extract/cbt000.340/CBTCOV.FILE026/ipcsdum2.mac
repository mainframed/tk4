%--------------------------  SYS1.DUMPnn Data Sets  --------------------
%
%
+The dump header records are now being read for every SYS1.DUMPnn data s
+every system in the center.
+
+When all headers have been read, information on all dumps will be prese
+tabular form. Empty dump data sets are omitted from the display.
+
+The initial display is sorted by date and time, with the most recent du
+displayed first. A command is available to sort by CPU name or by dump
)END
