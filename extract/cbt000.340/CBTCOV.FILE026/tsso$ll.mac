%TUTORIAL ------------------- TSSO  LINKLIST --------------------------
%command ===>_ZCMD
+

  The LINKLIST command displays the current link list datasets.

%  Syntax:

%         LINKLIST
+








%Only valid for MVS/XA
+

%Note:+Link list datasets can also be searched for selected members usin
       FINDMEM dialog.

)PROC
  &ZCONT = TSSO$LOC
  IF (&ZERRHM = TSSO$LL) .RESP = END
)END
