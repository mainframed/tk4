%TUTORIAL ----------------------- DRAW - BOX --------------------------
%OPTION ===> _ZCMD
%
+  The%BOX+function draws a box with one corner at the most recently def
   point and the other at the the current cursor position.  The cursor m
   positioned on a valid data line when you draw a box.  The minimum box
   is 2 characters by 2 characters.

   After the box is drawn, the current cursor position becomes the most
   recently defined point.
)PROC
  &ZUP = DRAW0000
)END
