%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                        Job Related Commands (continued)
+
+  %JCL jobname+
+        This displays the selected job's JCL via a BROWSE interface.  N
+        that any job's JCL can be displayed by any user.
+
+  %JLOG jobname+
+        This displays the selected job's JES2 job log via a BROWSE inte
+        face.  Note that any job's job log can be displayed by any user
+
+  %JMSG jobname+
+        This displays the selected job's JCL messages via a BROWSE inte
+        face.  Note that any job's messages can be displayed by any use
+
+  %JHIS jobname+
+        This displays the selected job's JES history, showing the time
+        date stamps associated with the job's progress through the syst
+
+                          (continued on next page)
)PROC
   &ZUP = BFTQU000
   &ZCONT = BFTQU022
)END
