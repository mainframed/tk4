%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                           System Display Commands
+
+   The system display commands display the current status of either a j
+   or group of jobs, or of all those active on this CPU in a JES2 MAS
+   complex.
+
+  %DA+  This command displays all jobs in active in JES2 on any CPU in
+        complex.  It displays the input queue first then the output que
+
+  %DT+  This command displays all TSO users on any CPU in the JES2 MAS
+
+  %DS+  This command displays all started tasks on any CPU in the JES2
+        complex.
+
+                          (continued on next page)
)PROC
   &ZUP = BFTQU000
   &ZCONT = BFTQU011
)END
