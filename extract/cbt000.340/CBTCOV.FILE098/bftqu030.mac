%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                         Input Queue Display Commands
+
+   The input queue display commands allow viewing of the jobs awaiting
+   execution in a number of different modes.
+
+   The operand%class+in the following commands is any of JES2's valid
+   class codes (i.e.%A-Z,0-9+are valid).
+
+  %DQ+
+        This displays for each input queue the total number of jobs, th
+        number executing, waiting, held, and duplicated.
+
+  %DI class+
+        This displays all the jobs in the input or execution queue for
+       %class+specified.  If no operand is specified then all classes a
+        displayed.
+
+
+                          (continued on next page)
)PROC
   &ZUP = BFTQU000
   &ZCONT = BFTQU031
)END
