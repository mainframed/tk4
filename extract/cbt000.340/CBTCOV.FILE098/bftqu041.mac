%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                  Output Queue Display Commands (continued)
+
+
+  %AO class+
+        This displays for the selected%class+all the jobs available for
+        For each job, its job name, job number, JES2 priority, number o
+        lines, active device, and destination is displayed.  If no clas
+        is specified all classes are displayed.
+
+  %HO class+
+        This displays for the selected%class+all the jobs with held sys
+        For each job, its job name, job number, JES2 priority, number o
+        lines, active device, and destination is displayed.  If no clas
+        is specified all classes are displayed.
+
+                          (continued on next page)
)PROC
   &ZUP = BFTQU000
   &ZCONT = BFTQU042
)END
