CONTROL NOMSG NOFLUSH
ALLOC DD(ISPPROF) SHR DS('&SYSUID..ISPPROF')
IF &MAXCC NE 0 THEN DO
  ATTR PROF LR(80) BLK(6160) RECFM(F,B)
  ALLOC DD(ISPPROF) DS('&SYSUID..ISPPROF') SP(1) TR DIR(1) US(PROF) -
        UNIT(DISK)
  FREE AT(PROF)
END
ISPF PANEL(MIKES)
