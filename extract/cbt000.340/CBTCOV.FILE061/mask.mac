)F   FUNCTION -
         MASKLIST IS A UTILITY PROGRAM THAT LIST THE STATUS OF UNIT
         GENERIC DEVICE TYPES.  IT CAN BE RUN AS A TSO COMMAND BY
         PREALLOCATING SYSPRINT AND SYSIN.

)O       PARAMETERS SUPPORTED:

         NONE

)X       THE FOLLOWING WILL RUN MASKLIST AS A TSO COMMAND:

         ALLOCATE DD(SYSPRINT) DSN(*)
         ALLOCATE DD(SYSIN) DSN(*)
         MASKLIST
