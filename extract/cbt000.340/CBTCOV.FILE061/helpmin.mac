)F FUNCTION -
  The ALLOCMIN command allows you to remove an SPF library from an
  existing concatenation, requiring only that you know the ddname from
  which it is to be removed.  This command may only execute if SPF is
  not active, that is, in the native TSO environment.
)X SYNTAX -
         ALLOCMIN  ddname  dsname
  REQUIRED - ddname  dsname
  DEFAULTS - none
)O OPERANDS -
))ddname   - The SPF ddname which is associated with your library

))dsname   - The data set name that you want removed from a
             concatenation.  It may be specified without quotes if it
             is qualified by your TSO id and the first qualifier is
             omitted, or it can be specified with quotes and be fully
             qualified.  In order to remove more than one dataset name,
             issue this command once for each dataset name.
