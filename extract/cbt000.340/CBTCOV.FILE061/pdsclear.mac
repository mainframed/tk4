)F   FUNCTION -
         PDSCLEAR IS A UTILITY PROGRAM THAT RESETS THE DIRECTORY OF
         A PARTITIONED DATASET AND INDICATES NO MEMBERS EXIST. THE
         PROGRAM MUST BE AUTHORIZED AND BE IN AN AUTHORIZED LIBRARY.

)O       PARAMETERS SUPPORTED:

         NONE

)X       THE FOLLOWING JCL WILL RUN PDSCLEAR:

    //RESET EXEC PGM=PDSCLEAR
    //SYSPRINT DD SYSOUT=A
    //DSN1 DD DSN=FIRST.DATASET.RESET,DISP=SHR
    //DSN2 DD DSN=SECOND.DATASET.RESET,DISP=SHR
      ..
    //DSNX DD DSN=LAST.DATASET.RESET,DISP=SHR
