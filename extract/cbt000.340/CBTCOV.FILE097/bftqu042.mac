%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                  Output Queue Display Commands (continued)
+
+
+  %WO +
+        This displays all jobs with an external writer name specified.
+        For each job, its job name, job number, JES2 priority, number o
+        lines, active device, and destination is displayed.
+
+  %NJ +
+        This displays all jobs and output datasets that are either queu
+        for transmission to another node, or are actively transmitting
+        to/from another node.
+
)PROC
   &ZUP = BFTQU000
)END
