%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                     System Display Commands (continued)
+
+
+  %DC+  This command displays all users currently active on this CPU, s
+        the CPU time used, core occupied, and current step name.  It al
+        operand of %B, J, S,+or%T+ to display only the Batch, Job, Star
+        task, or Tso users.
+
+  %STAT+This command displays the status of all jobs starting with the
+        characters entered as an operand.  If no operand is entered the
+        id is used.  The command may be abbrievated as %ST.+
)PROC
   &ZUP = BFTQU000
)END
