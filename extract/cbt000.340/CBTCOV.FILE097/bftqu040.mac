%TUTORIAL ---------------------- QUEUE COMMAND ------------------------
%OPTION ===>_ZCMD
+
+                         Output Queue Display Commands
+
+   The output queue display commands allow viewing of the jobs awaiting
+   output in a number of different modes.
+
+   The operand%class+in the following commands is any of JES2's valid
+   class codes (i.e.%A-Z,0-9+are valid).
+
+  %DF+
+        This displays for each output class the total number of jobs, a
+        the number printing/punching.
+
+  %DO class+
+        This displays for the selectd%class+all the jobs awaiting outpu
+        For each job, its job name, job number, JES2 priority, number o
+        lines, active device, and destination is displayed.  If no clas
+        is specified all classes are displayed.
+
+                          (continued on next page)
)PROC
   &ZUP = BFTQU000
   &ZCONT = BFTQU041
)END
