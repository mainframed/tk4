%TUTORIAL------------------  SCAN PDS / MEMBER LIST  -------------------
%OPTION  ===>_ZCMD
%                          -------------------------
                           × CANcel, REBUILD and ? ×
                           -------------------------

+The%CANcel+command (must be abbreviated CAN) is used to "end" the curre
 List (or stacked Member Lists after a SCAN), and return to the menu.

+The%REBuild+command can be  used to end the  current Member  List  (or
 Member Lists after a SCAN), and reconstruct a full a Member List for th
 inal input dataset or datasets. You may specify a member name  (which m
 course, be a generic member name) as an operand on this command.  The
 may be abbreviated REB.

+The%?+command will set a message indicating how many  Member Lists in t
 ical screen are stacked.
)PROC
  &ZUP = DST@SC50
)END
