)ATTR
 DEFAULT (!+_)
)BODY
!TUTORIAL------------------------ LIST CATALOG -------------------------
!OPTION  ===>_ZCMD
!                              ------------------
                               ×      Sort      ×
                               ------------------

+The! Sort+command is  only valid for  ISPF Version 2 or later.  It can
 to sort  the List Catalog display on any!two+columns in  either  ascend
 descending  order.  Valid   operands  for  SORT  are VOL  or VOLUME,  L
 CREATED, LRECL, BLKSIZE, ORG, TRKS, %USED, XT, EXPIRES, DSNAME and A or
 ascending or descending order.   The default is DSNAME A.   For example
 "SORT" on the COMMAND  line to sort the  display using  the default.
 by LASTREF descending and then DSNAME ascending, enter "SORT LASTREF D
 on the COMMAND line.
)PROC
  &ZUP = DST@FC50
)END
