%TUTORIAL------------------- LIST CATALOG - DISPLAY --------------------
%OPTION  ===>_ZCMD

+All Select Codes are valid from the COMMAND line, too. Each requires a
 name as an  operand.  You  should enter it  as the dsname displayed (i.
 shorter name--without  the prefix).   For example, if the prefix is "TS
 you could  invoke ISPF  Browse  for this  dataset  "TSO.MVS.CLIST"  by
 entering "B CLIST" from the COMMAND line.
)PROC
   &ZUP = DST@FC30
)END
