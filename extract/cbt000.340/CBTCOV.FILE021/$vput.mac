.pa
.subsection '$VPUT'
.ix '$VPUT Macro'
.ix Macros $VPUT
.ix Macros ISPF $VPUT
.sp 2
     This macro is used to call the subroutine generated by the VPUTSUB
macro.  The syntax is:
.sp
         label  $VPUT  name,data
.sp
     Where name is the name of the ISPF variable you wish to create or
replace.  And data is the name of the user field containing the data
to be placed in ISPF variable name.
     NOTE: The length of data is determined in the macro.
