)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ÷ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-÷PDS MEMLIST General Information+-\-\-
%OPTION  ===>_ZCMD
   The%special control codes+perform the following special actions:
    %1+Suspend ISPF   %-+terminates ISPMODE; retains command log during
    %4+Sublist =      %-+create a sublist of all members in the member t
    %5+Check aliases  %-+checks main and alias members by TTR in current
    %6+MEMLIST all    %-+nullifies and rebuilds the member table
    %7+Output table   %-+outputs the member table to print or a data set
    %9+Swap panels    %-+switches to an alternate display
    %L+Locate         %-+scrolls the display to a named member
    %O+Options        %-+displays a directory of÷PDS+subcommands
    %R+Recall         %-+displays the last÷PDS+subcommand entered
    %X+Above/Below/All%-+deletes a portion of the member table based on
    %8+View log       %-+display the session log
   %LA+List allocation%-+display the allocation table
   %LV+List volume    %-+clears the member list table
 %FUNC+Function       %-+displays function status and selection panel
 %QUIT+Terminate      %-+terminates÷PDS+

   In addition to the special control codes,÷PDS+subcommands and ISPF
   commands may be entered on the command line.  Note: if you wish to us
   a÷PDS+subcommand name which matches the name of an ISPF subcommand, u
   a substring of the name such as H, HE or HEL instead of HELP.
)END
