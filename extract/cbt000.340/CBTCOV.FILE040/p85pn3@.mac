)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    Ö TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-ÖPDS Subcommand Checkpoint Panel+-\-\-
%OPTION  ===>_ZCMD
%               ---------------------------------------------
%               ×           Checkpoint Option           %   ×
%               ---------------------------------------------
+

   AÖPDS+subcommand has reached an output checkpoint (250 lines of data
   by default) and has been suspended so that you can examine the
   session log.  When you want to terminate theÖPDS+subcommand in
   control, enter an END command.

   Note:ÖPDS+subcommands may%not+be entered on the checkpoint panel.


   The following topics are presented in sequence or may be requested
   by number:
    %1+ General ISPMODE information
    %2+ F (find) log command
   The following optional topic may be requested by number:
    %3+ ISPF commands and PF keys
)INIT
  &USRLEFT  = ''
  &USRRIGHT = ''
)PROC
  &ZSEL = TRANS(&ZCMD  1,P85PN3A   2,P85PN1B   3,*ISP01304  *,'?')
)END
