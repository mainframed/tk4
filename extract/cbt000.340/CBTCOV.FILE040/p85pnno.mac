)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    Ö TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-ÖPDS TCT Panel+-\-\-
%OPTION  ===>_ZCMD
+
   The TCT panel provides for 3 changeable fields and one information
   field:
     The Option field is set by the user for the operation option desire

         The MSG/RC field provides information on the last action for th
         or the return code from the last TSO command.

         The TYPE field is set to the user to "P" for PDS commands,"*" f
         comment, a blank or dash is for a TSO command or CLIST, or a "T
         for Trapping the output from a TSO command.

         The command data field is filled in by the user with the actual

         If the TYPE field or command data field is changed and the opti
         is blank a "SELECT" option is assumed.
)END
