)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    Ö TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-ÖPDS o.h HELP line command+-\-\-
%OPTION  ===>_ZCMD
%               ---------------------------------------------
%               ×             ÖPDS+HELP subcommand %        ×
%               ---------------------------------------------
+
   The formal documentation forÖPDS+is in the HELP data set under the sa
   member name as theÖPDS+command itself (example: both are called PDS84
   This panel formats a HELP request for any of the definedÖPDS+subcomma

   To get an explanation of aÖPDS+subcommand, enter the subcommand name
   or some legal abbreviation and optionally HELP keywords to indicate
   the type of information you want.  For example to get syntax and
   operand information for the VERIFY subcommand, you should enter:
  %verify sy op+after %===>+on the HELP entry assistance panel.

+  To get an explanation ofÖPDS+messages such as PDS300A and PDS999E,
   you could enter:%msg msg(pds300,pds999)+ As an alternative, you can
   request PDS's extended help just after receiving a warning (PDSxxxW)
   or an error (PDSxxxE) message by entering%.?+after%COMMAND ===>+on a
   ISPMODE or MEMLIST panel.

                                   (continued)
)PROC
 &ZCONT = P84LOHH
)END
