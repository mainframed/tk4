)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ÷ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-÷MEMLIST PROMPT+-\-\-
%OPTION  ===>_ZCMD
+
% MEMLIST prompt     %===>+???  (Yes/No)  LKED Date for load %===>    +
+ Alias check        %===>    + (Yes/No)  Auto member update %===)    +
+ Remove deleted mem %===>    + (Yes/No)  Remove old members %===>    +
+ Save in profile    %===>    + (Yes/No)

%MEMLIST prompt:+
   Each CHANGE subcommand causes any member list to be discarded; this
   option provides a MEMLIST prompt panel after each CHANGE subcommand
   to allow specifying a member group for a new member list.

   Respond with%YES+if you want÷PDS+to prompt for a member list after
   each CHANGE subcommand.
)END
