)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    Ö TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
+-\-\-ÖO.ISPM    ISPMODE Subcommand+-\-\-
%OPTION ===>|ZCMD
+
+You are already in %ISPMODE+ There are no operands for this option.
+
+ Note:  Operands CHKMAX,CMDMAX,TBLMAX,LIST no longer supported,
+        The processing values for these can be displayed and changed
+        by the SETLOG option.  Hit Enter for the SETLOG option.
)INIT

  .HELP   = P84OPHH
)PROC
  &PDSZSEL='SETLOG'
)END
