)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    Ö TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
+-\-\-ÖO.V    VERIFY Subcommand+-\-\-
%OPTION  ===>|ZCMD
%
+Enter any operands below for%VERIFY:
%===>|LOPER

+
+Operands: %NOINPUT   -+no data set reads are to be performed
%           NOSTATS   -+no statistics are to be provided
%           MAXBLK(s) -+output an message if any data block size exceeds
+
+Defaults:  INPUT, STATS, MAXBLK(data set blksize)
)INIT

  &LOPER = '&OPERS'
  .CURSOR = LOPER
  .HELP   = P84OPHH
  IF(&LOPER  = &Z) &LOPER=&P84#VE2
)PROC
 &P84#VE2=&LOPER
  &PDSZSEL='Verify  &LOPER'
)END
