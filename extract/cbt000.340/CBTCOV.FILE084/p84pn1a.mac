)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ÷ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-÷PDS+ISPMODE Session Display+-\-\-
%OPTION  ===>_ZCMD
+
   The%special control codes+perform the following special actions:
    %1+Suspend ISPF   %-+terminates ISPMODE; retains command log during
    %6+MEMLIST all    %-+nullifies and rebuilds the MEMLIST display
    %7+Output log     %-+outputs the ISPMODE log to print or a data set
    %9+Swap panels    %-+switches to an alternate ISPMODE display panel
    %O+Options        %-+assists with÷PDS+subcommands
    %R+Recall         %-+displays the previous÷PDS+subcommand entered
    %X+Above/Below/All%-+deletes portions of the log
    %*+Memlist *      %-+adds the default member group to the member tab
   %LA+Lista          %-+displays the allocation table
   %LV+Listv          %-+displays the volume table
   %ML+Memlist        %-+displays the member table
% FUNC+Function       %-+displays function status and selection panel
% QUIT+Terminate      %-+terminates÷PDS+

   In addition to the special control codes,÷PDS+subcommands and ISPF
   commands may be entered on the command line.

  %Note:+if you wish to use a÷PDS+subcommand name which matches the name
   an ISPF subcommand, use a substring of the name such as H or HE for H
)END
