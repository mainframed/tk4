)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ÷ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-÷SAVE IN PROFILE+-\-\-
%OPTION  ===>_ZCMD
+
+   Select source command%===>+    +Line command for MEMLIST source
+   Select load command  %===>+    +Line command for MEMLIST load
+   Select Lista command %===>+    +Line command for Lista
+   Select Listv command %===>+    +Line command for Listv
%   Save in profile      %===>+???+ (Yes/No)

%Save in profile:+
   Normally, the other variables on the panel apply only to the current
   invocation of the÷PDS+command.  With this variable, you can choose to
   apply these defaults to future÷PDS+sessions.

   Respond with%YES+if you want÷PDS+to save the other variables in your
   ISPF profile; this makes them apply to future÷PDS+sessions.
)END
