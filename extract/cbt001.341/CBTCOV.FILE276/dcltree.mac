0DECLARE
      PHEAD POINTER STATIC EXTERNAL INITIAL(NULL()),
   01 NODE BASED,
      02 NODE_TYPE FIXED BINARY(31),
      02 MSELECT FIXED BINARY(31) INITIAL(0),
      02 (PACROSS,PDOWN)POINTER INITIAL(NULL()),
   01 LEAF BASED,
      02 LEAF_NODE_TYPE FIXED BINARY(31) INITIAL(1000),
      02 LEAF_MSELECT FIXED BINARY(31) INITIAL(0),
      02 (LEAF_PACROSS,LEAF_PDOWN)POINTER INITIAL(NULL()),
      02 MAP_KEY FIXED BINARY(23)INITIAL(-1),
         /* OPTIMIZER REQUIRES PRECISION<=23 FOR INLINE CODE */
      02 MAP_SUBSCRIPT FIXED BINARY(15),
      02 FOLD_CODE FIXED BINARY(15)INITIAL(0),
      02 INDX_ENTRY,
         03 DESCRIPTOR CHAR(32),
         03 INITIAL_MAP_KEY FIXED BINARY(23),
         03 INITIAL_MAP_SUBSCRIPT FIXED BINARY(15),
         03 RECORD_COUNT FIXED BINARY(15),
      02 MAP_ENTRY,
         03 ERIC_KEY(42)FIXED BINARY(31);
