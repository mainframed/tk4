0DECLARE
   01 ENV STATIC EXTERNAL,
      02 TSO BIT(1)INITIAL(0B),
      02 BATCH BIT(1)INITIAL(0B),
      ERRCODE FIXED BINARY(31)EXTERNAL INITIAL(0),
      QERROR CONDITION EXTERNAL,
      SCANPTR FIXED BINARY(31)EXTERNAL;
