0DECLARE /* ROUTINES INVOKED ONLY FROM COMPILE */
      CMPCHNG ENTRY(CHAR(10),CHAR(*)VAR,PIC'999')EXTERNAL, /* CHANGE */
      CMPFLD ENTRY(CHAR(10),CHAR(*)VAR,FIXED BINARY(31)),/* FLD STMTS */
      CMPLST ENTRY(CHAR(10),CHAR(*)VAR)EXTERNAL, /* LIST/RETAIN/DEL */
      CMPOPT ENTRY(CHAR(10),CHAR(*)VAR)EXTERNAL, /* COMPILE OPTIONS */
      CMPPRT ENTRY(CHAR(10),CHAR(*)VAR,BIT(1)ALIGNED)EXT, /* PRINT */
      CMPRD ENTRY(CHAR(10),CHAR(*)VAR)EXTERNAL, /* COMPILE READ */
      EXSRCH ENTRY(CHAR(10))EXTERNAL, /* EXECUTE SEARCH */
      LSTDICT ENTRY(CHAR(10),CHAR(*)VAR)EXTERNAL, /* LIST INDX */
      LSTOBJ ENTRY(CHAR(*)VAR,CHAR(*)VAR)EXTERNAL, /* LIST OBJCODE */
      LSTTREE ENTRY(POINTER)EXTERNAL, /* LIST READ STMT TREE */
      PRTITLE ENTRY EXTERNAL, /* PRINT QUANDRY RELEASE */
      SBMIT ENTRY EXTERNAL, /* SUBMIT SEARCH */
      SBSAVE ENTRY(CHAR(10),CHAR(*)VARYING)EXTERNAL; /* SAVE STMT */
