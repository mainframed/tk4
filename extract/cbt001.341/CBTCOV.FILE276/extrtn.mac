0DECLARE /* COMMON SERVICE ROUTINES */
      PRTERR ENTRY EXTERNAL, /* PRINT ERROR MESSAGES */
      SCNPARM ENTRY(,,FIXED BINARY(31),FIXED BINARY(31))EXTERNAL,
         /* SCAN OPTION-TYPE STATEMENT */
      SCNSBST ENTRY EXTERNAL; /* SCAN SBSTMTS INTO STMAP */
