0DECLARE
      ERIC KEYED DIRECT INPUT
         ENV(REGIONAL(3) SCALARVARYING),
      INDX KEYED SEQUENTIAL INPUT ENV(INDEXED GENKEY KEYLOC(1)),
      MAP DIRECT INPUT ENV(REGIONAL(1)),
      SUBMIT STREAM OUTPUT
         ENV(FBS RECSIZE(80) BLKSIZE(400) BUFFERS(1)),
      SYSIN STREAM INPUT ENV(FB RECSIZE(80)),
      SYSOUT PRINT,
      SYSPRINT PRINT;
