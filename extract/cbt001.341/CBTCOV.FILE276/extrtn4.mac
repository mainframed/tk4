0DECLARE /* ROUTINES INVOKED ONLY FROM QUANDRY */
      COMPILE ENTRY(CHAR(10),CHAR(*)VAR)EXTERNAL, /* COMPILE STMTS */
      GETSTMT ENTRY(CHAR(10),CHAR(*)VAR)EXTERNAL, /* READ IN STMT */
      GTCLEAR ENTRY EXTERNAL, /* CLEAR STMT BUFFER */
      GTRTRAN ENTRY EXTERNAL, /* SET STMT BUFFER FOR RETRANSLATE */
      TESTENV ENTRY EXTERNAL; /* ANALYZE ENVIRONMENT */
