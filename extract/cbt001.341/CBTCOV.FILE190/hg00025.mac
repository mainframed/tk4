++ USERMOD(HG00025)
    /* THIS ZAP TO THE UPDATE UCB SUBROUTINE OF ALLOCATE
       MAKES THE DEFAULT STATUS FOR DIRECT ACCESS MOUNTS
       'PRIVATE' INSTEAD OF 'PUBLIC'. THE PLS STATEMENT IS
       IF VOLPUB = ON × (VOLPUB = OFF & VOLSTG = OFF & VOLPRV = OFF)
       & UCBBPRV= OFF THEN UCBBPUB = ON;
       THE ASSEMBLER CODE TESTS THE THREE SWITCHES IN PARENTHESES
       AND BRANCHES TO THE FALSE BRANCH IF ANY ARE ON. I CHANGE
       THE SUBSEQUENT TEST OF UCBBPRV TO AN OR IMMEDIATE AND MAKE THE
       CONDITIONAL BRANCH TO THE FALSE LABEL UNCONDITIONAL.  */.
++ VER(Z038) FMID(EBB1102) PRE(UZ27785).
++ ZAP(IEFAB435).
 NAME IEFAB435
 VER 0644 9110,C022,4770
 REP 0644 9610,C022,47F0
 IDRDATA HG00025
