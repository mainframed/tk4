++ USERMOD(KK00010)
   /* IEFACTRT UPDATE
      USE THE FOLLOWING SMP CONTROLS:
      // - - -
      //SOURCOBJ DD DSN=SYSTEM.SOURCLIB.OBJ,DISP=SHR
      //SMPPTFIN DD DSN=YCC1.SYSMOD.PTFIN(KK00010),DISP=SHR
      RECEIVE.
      APPLY S(KK00010) DIS(NO).
      ACCEPT S(KK00010) DIS(NO) USERMODS.
  */.
++ VER (Z038) FMID(EBB1102).
++ MOD(IEFACTRT)  TXLIB(SOURCOBJ).
