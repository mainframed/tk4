 BLDCRD: PROC OPTIONS(MAIN);
  DCL VOLNO PIC'99999';
  DCL OUT FILE OUTPUT;
   DO VOLNO=0 TO 499;
   PUT FILE(OUT) EDIT(' DEFINE VOLUME A',VOLNO,' RINGOUT GROUP(STAF);')
      (COL(1),3 A);
   END;
 END;
