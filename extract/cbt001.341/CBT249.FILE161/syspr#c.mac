PROC 0  DEBUG
CONTROL NOLIST MSG PROMPT NOFLUSH
IF &DEBUG NE &STR() THEN +
  CONTROL LIST CONLIST SYMLIST
SET &LUS EQ &STR(S)
SET &LUID EQ  &STR(&SUBSTR(1:1,&SYSUID))
IF &LUS  EQ &LUID      THEN   +
   ISPEXEC SELECT  PANEL(SYSPR#P)
