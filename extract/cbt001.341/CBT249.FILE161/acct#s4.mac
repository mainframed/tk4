PROC 0
CONTROL PROMPT NOFLUSH  MSG
 ACCOUNT
 ADD (&USERID *  * )                 +
)SEL &PROCED =   AEHLOGON
     DATA(&PROCED) SIZE(1024)             +
)ENDSEL
)SEL &PROCED =   DMSLOGON
     DATA(&PROCED) SIZE(1800)             +
)ENDSEL
)SEL &PROCED =   IQNLOGON
     DATA(&PROCED) SIZE(1500)             +
)ENDSEL
)SEL &PROCED =   SADLOGON
     DATA(&PROCED) SIZE(512)              +
)ENDSEL
)SEL &PROCED =   SFDLOGON
     DATA(&PROCED) SIZE(1024)             +
)ENDSEL
)SEL &PROCED =   SPFLOGON
     DATA(&PROCED) SIZE(1024)             +
)ENDSEL
)SEL &PROCED =   TMSLOGON
     DATA(&PROCED) SIZE(1024)             +
)ENDSEL
)SEL &PROCED =   CJCLOGON
     DATA(&PROCED) SIZE(1300)             +
)ENDSEL
)SEL &PROCED =   JRWLOGON
     DATA(&PROCED) SIZE(1200)             +
)ENDSEL
     UNIT(SYSALLDA)
 END
END
