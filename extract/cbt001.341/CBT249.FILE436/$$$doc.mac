member $ostdoc is the primary input to script.
member $vmdoc  is the primary input to script for the vm test manual.

ddname aost must also be included pointing to this library to resolve
imbed statements.

member $$$JCL is a sample jcl which can be used to generate this manual
in batch.

member $vmJCL is a sample jcl which can be used to generate the vm
manual in batch.

Members which begin with $F are flashes which I send to all users within
Rockwell of the OS&T system to inform them of changes.


NOTE: The table of contents is generated at the end of the document and
      must be moved to the front.  This is by design with this SCRIPT.

