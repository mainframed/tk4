.section 'CONVERT - Hex to Display Macro'
.ix Macros CONVERT
.ix 'Convert Macro'
.sp 2
.subsection 'Description'
.sp
     This macro will convert hex data to printable format.
.sp
.subsection 'Syntax'
.sp
     label CONVERT (to,from,length)
.sp
     where:
           to = register with address of output location
         from = register with address of input location
       length = register with the length of data to convert
