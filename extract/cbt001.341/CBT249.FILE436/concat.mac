.section 'CONCAT (dynamically concatenated)'
.ix CONCAT
.ix 'TSO Commands' CONCAT
.sp 2
.subsection 'Description'
         The CONCAT command dynamically concatenates a list of
         previously defined data sets.  The DECONCAT or FREE
         commands can be used to deconcatenate files.
.sp 2
.subsection 'Syntax'
         CONCAT  'filelist'
         Required - Filelist with two or more entries
         Defaults - none
         Alias    - none
.sp 2
.subsection 'Operands'
         'filelist' - list of file names (DDnames) to be concatedated.
                   The list must have two or more entries.  The first
                   name specified will be the prime name to which
                   other files are concatenated.
