.pa
.ix VGETSUB
.ix Macros VGETSUB
.ix Macros ISPF VGETSUB
.subsection 'VGETSUB'
.sp 2
     This macro may be coded only once within any program and will
generate the code to do a VDEFINE, VGET and VDELETE.  The ISPF
variable is placed into an 80 byte storage location generated by this
macro and is pointed to by register 1 upon exit.
.sp
     The syntax is:
.sp
               VGETSUB
.sp
     No label or operands are supported.
     Register 14 is saved on entry and restored prior to return after
pointing register 1 to the data.
