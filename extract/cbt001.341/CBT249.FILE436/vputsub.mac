.pa
.subsection 'VPUTSUB'
.ix VPUTSUB
.ix Macros VPUTSUB
.ix Macros ISPF VPUTSUB
.sp 2
     This macro may be coded only once within any program and will
generate the code to do a VDEFINE, VPUT and VDELETE.  The ISPF
variable is replaced by the 80 byte storage location generated by this
macro and updated by the $VPUT macro.
.sp
     The syntax is:
.sp
               VPUTSUB
.sp
     No label or operands are supported.
     Register 14 is saved on entry and restored prior to return .
