.section 'ACMD PROC'
.ix 'ACMD' 'PROC'
.pp
ACMD provides the user the ability to invoke ACMD as a subsystem,
and to specify where the input is to come from.
.sp 2
.section 'ACMD SYSPARM Input'
.pp
The input for ACMD is to be found in SYS1.SYSPARM members IEAPRExx.
.sp
     The existing IEAPRExx members are:

     AM   -   Invokes AMS
     CN   -   Allows for input via WTOR
     DS   -   DSI varies
     00   -   Invoke APFUP (used at IPL)
