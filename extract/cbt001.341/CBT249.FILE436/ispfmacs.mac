.section 'ISPF Macros'
.ix 'ISPF Macros'
.sp 2
.subsection 'Description'
.sp 2
     These macros allow a program to be executed from an ISPF panel or
via the ISPEXEC command of a CLIST and to access ISPF variables and use
them in the program.
.sp
     Note: at this time a maximum of 80 characters is supported for the
VGET/VPUT of an ISPF variable.
.im aost($vget)
.im aost($vput)
.im aost(vgetsub)
.im aost(vputsub)
