.section 'JACCT'
.ix JACCT
.ix 'ISPF Programs' JACCT
.sp 2
     This is an ISPF program to return the 52 character job accounting
information to the ISPF variable JINFO.

     To invoke JACCT via CLIST:

     ISPEXEC SELECT PGM(JACCT)

     To retrieve the JINFO via CLIST:

     ISPEXEC VGET (JINFO)
