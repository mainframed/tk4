  /*TGSDQA*/
 TGSDQ:  PROC(DSNRPTR);
  /* SUBRTNE TO DEQUEUE A DSNREC */
         % INCLUDE TGSINITA;
         % INCLUDE TGSPREDC;
         % PRENPT = 'DNDSNPT';
         % PREPPT = 'DPDSNPT';
         % PRERUT = 'DSNFPT';
         % PREKEY = 'NEWVOL';
         % INCLUDE TGSDQC;
         % PRENPT = 'DNDUPPT';
         % PREPPT = 'DPDUPPT';
         % PRERUT = 'ISAM.DAMASTER';
         % PREKEY = 'DSN';
         % INCLUDE TGSDQC;
         FUNCTION = 'READ';
         ISAMKEY = NEWVOL;
         CALL ISAMIO(ISAMWRK,ISAMIN);
         BDAMKEYA = ISAM.DAMASTER;
         READ FILE(BDAMFLE) INTO(BDAMREC) KEY(BDAMKEY);
         BDAM -> FTRKS = BDAM -> FTRKS + DTRKS;
         WRITE FILE(BDAMFLE) FROM(BDAMREC) KEYFROM(BDAMKEY);
         END;
