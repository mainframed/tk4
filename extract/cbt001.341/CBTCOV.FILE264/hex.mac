 /* HEX
0*$.HE GENERALIZED SUBROUTINES - HEX
0*$.HE CONVERT TO PRINTABLE HEX
0*$.PL 55
0*$.PN 0
0*$.PA
0*$.SS
0*$.NF
0*$04/14/87
0*$AUTHOR:  T. SMITH
0*$SOURCE MODULE NAME:  HEX
0*$DOCUMENTATION ID:  0*$
0*$.FI L
0*$.SP 5
0*$THE HEX SUBROUTINE CAN BE USED TO CONVERT A CHARACTER STRING TO
0*$PRINTABLE HEX.  CALL FORMAT IS AS FOLLOWS:
0*$.NF
0*$.SP 1
0*$DCL HEX ENTRY(CHAR(*)) RETURNS(CHAR(32767) VAR);
0*$STR='HOW NOW BROWN COW';
0*$PUT SKIP EDIT(HEX(STR))(A);
 */
 HEX: PROC(PARM) RETURNS(CHAR(32767) VAR) OPTIONS(REENTRANT);
   DCL PARM CHAR(*);
   DCL TROUT ENTRY;
   DCL IN CHAR(16383) VAR;
   DCL OUT CHAR(32767) VAR;

   IF LENGTH(PARM)=0
     THEN RETURN('');
   IN=PARM;
   OUT=REPEAT(' ',LENGTH(IN)*2-1);
   CALL TROUT(IN,OUT);
   RETURN(OUT);
   END HEX;
