 SCR3440:  PROC(MYSTR) RETURNS(CHAR(32767) VAR);
   DCL MYSTR CHAR(32767) VAR;
   % INCLUDE @FULLSCR;
   % @ROWS = 43;
   % @COLS = 80;
   @SCREEN('&(3439)X');
   @ENDPNL(LAST);
   @FLDMOD(SCREEN1.FIELD1=MYSTR);
   @DOPNL(1);
   RETURN(SCREEN1_IN.FIELD1);
   END;
