 /* PTRIN/PTROUT */
 /*
0*$.HE GENERALIZED SUBROUTINES - PTRIN/PTROUT
0*$.HE PTR ARITHMETIC
0*$.PL 55
0*$.PN 0
0*$.PA
0*$.SS
0*$.NF
0*$08/19/81
0*$AUTHOR:  HOWARD GILBERT, YALE COMPUTER CENTER
0*$         MODIFIED BY T. SMITH, KIM
0*$SOURCE MODULE NAME:  PTRIN
0*$DOCUMENTATION ID:  0*$
0*$.FI L
0*$.SP 5
0*$PTRIN AND PTROUT ARE SUBROUTINES THAT FACILITATE POINTER
0*$ARITHMETIC. ALTHOUGH THE ROUTINES ARE RELATIVELY SIMPLE,
0*$IT SEEMS A GOOD IDEA TO FORMALIZE AND ISOLATE THE METHODS BY WHICH
0*$POINTER ARITHMETIC IS ACCOMPLISHED IN PL/I.  CALL FORMAT IS AS
0*$FOLLOWS:
0*$.IN 5
0*$.NF
0*$.SP 1
0*$DCL PTRIN ENTRY(PTR)
0*$  RETURNS(FIXED BIN(31,0));
0*$DCL PTROUT ENTRY(FIXED BIN(31,0))
0*$  RETURNS(PTR);
0*$DCL CVTPTR PTR;
0*$*** NOW GET ADDR(CVT) ***
0*$CVTPTR=PTROUT(16);
0*$*** NOW PRINT ADDR(CVT) ***
0*$PUT LIST(PTRIN(CVTPTR));
 */
 PTRIN: PROC(PTRA) RETURNS(FIXED BIN(31)) OPTIONS(REENTRANT);
 DCL (I,J) FIXED BIN(31),(PTRA,PTRB) POINTER;
 UNSPEC(J)=UNSPEC(PTRA);
 RETURN(J);
 PTROUT: ENTRY(I) RETURNS(PTR) OPTIONS(REENTRANT);
 UNSPEC(PTRB)=UNSPEC(I);
 RETURN(PTRB);
 END;
