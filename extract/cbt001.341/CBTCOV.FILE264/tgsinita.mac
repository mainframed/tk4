         % INCLUDE TGSDSNR;
         % INCLUDE TGSVOL;
         % INCLUDE TGSBDAM;
         % INCLUDE TGSQCR;
         DCL QCRMAIN CHAR(110) EXT;
         QC = ADDR(QCRMAIN);
         % INCLUDE TGSGF;
         % INCLUDE TGSPF;
         DCL RETURN LABEL;
         % INCLUDE TGSFREE;
         % INCLUDE TGSISWK;
         % INCLUDE TGSDESCR;
         DCL MSGPROC ENTRY(BIN FIXED(15));
         DCL (DSNRPTR,VOLRPTR) PTR;
         DCL RETCODE FIXED BIN(31,0);
         DCL RETCHAR CHAR(4) DEF RETCODE;
         DCL RET BIT(32) DEF RETCODE;
