 /* @NUMSTR */
 %NOPRINT;
         % DCL @NUMSTR ENTRY;
 % @NUMSTR:  PROC(P1) RETURNS(CHAR);
 /*
0*$.HE GENERALIZED SUBROUTINES - @NUMSTR
0*$.HE PLI PREPROCESSOR TEXT
0*$.HE FIXED TO CHARACTER CONVERSION
0*$.PL 55
0*$.PN 0
0*$.PA
0*$.SS
0*$.NF
0*$06/13/77
0*$AUTHOR:  T. SMITH
0*$SOURCE MODULE NAME:  @NUMSTR
0*$DOCOMENTATION ID:  0*$
0*$.FI L
0*$.SP 5
0*$THE PLI PREPROCESSOR PROCEDURE CAN BE INVOKED TO CONVERT A FIXED
0*$ITEM TO CHARACTER, STRIPPING BLANKS IN THE PROCESS.  CALL FORMAT
0*$IS AS FOLLOWS:
0*$.SP 1
0*$.IN 5
0*$.NF
0*$DCL A CHAR, B FIXED;
0*$B = 23;
0*$A = @NUMSTR(B);
 */
         /* THIS PROCEDURE TAKES A NUMBER, CONVERTS IT TO CHAR
            AND STRIPS BLANKS */
         DCL (P1,I,J) FIXED;
         DCL STR CHAR;
         STR = P1;
         DO I = 1 TO 8;
              IF SUBSTR(STR,I,1) ^= ' '
                   THEN RETURN(SUBSTR(STR,I,9-I));
              END;
         % END @NUMSTR;
 %PRINT;
