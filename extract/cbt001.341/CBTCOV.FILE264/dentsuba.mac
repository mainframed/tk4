 /* COPYRIGHT 1983 BY THOMAS GLEN SMITH */
   %NOPRINT;
   /* DENTSUBA - INCLUDED IF SUBROUTINE DOES ITS OWN PARSING. */
         %IF @PROCNAME = 'RENT'
           %THEN %DO;
             %@PROCNAME = @RENTNAME;
 @PROCNAME: PROC(CMDINDX,CMDDATA,RHPTR,SUBENT,RHHDRPT)
   OPTIONS(REENTRANT);
   HALT;
               %END;
           %ELSE %DO;
 @PROCNAME: PROC(CMDINDX,CMDDATA,RHPTR,SUBENT,RHHDRPT);
   HALT;
               %END;
   DCL CMDDATA CHAR(*) VAR;
   % @PROCNAME='';
   % INCLUDE DENTSUB;
   %PRINT;
