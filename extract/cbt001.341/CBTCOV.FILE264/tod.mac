 /* TOD */
 % INCLUDE CPINIT;
 DCL DATE BUILTIN;
 DCL SPELDTE ENTRY(CHAR(6)) RETURNS(CHAR(400) VAR);
 @SETL(&MYDATE = SPELDTE(DATE));
 @WRITE('DATE=&MYDATE, TIME=&SYSTIME, USERID=&SYSUID, PREFIX=&SYSPREF');
 END;
