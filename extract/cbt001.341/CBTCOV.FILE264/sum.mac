 SUM:  PROC OPTIONS(MAIN);
         ON ENDFILE(SYSIN) GO TO EOJ;
         DCL (SUM,ITEM) FIXED DEC(15,0) STATIC INIT(0);
 NXT:
         GET LIST(ITEM);
         SUM = SUM + ITEM;
         GO TO NXT;
 EOJ:
         PUT EDIT(SUM,' = SUM')(P'ZZZ,ZZZ,ZZZ,ZZZ,ZZ9S',A) SKIP(1);
         END;
