 STRIP:  PROC OPTIONS(MAIN);
 DCL (INREC,OUTREC) CHAR(80);
 ON ENDFILE(IN) GO TO EOJ;
 NXT:
 READ FILE(IN) INTO(INREC);
 OUTREC = SUBSTR(INREC,1,72);
 WRITE FILE(OUT) FROM(OUTREC);
 GO TO NXT;
 EOJ:
 END;
