%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×  Specifying information for a new   ×
%                    ×               dataset               ×
%                    ×             (continued)             ×
%                    ---------------------------------------
+
+PRIMARY QUAN    %===>+           (In above units)

  The original amount of Space Units to be requested

+SECONDARY QUAN  %===>+           (In above units)

  The amount that will be requested each time additional space is
  required

+DIRECTORY BLOCKS%===>+           (Zero for sequential data set)

  For a partitioned data set, you must specify how many blocks will be s
  aside for the directory. Do not use this field for a sequential data s
+                            (continued on the next page)
)PROC
 &ZUP = RBDT0000
 &ZCONT = RBDT4320
)END
