%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×  Specifying TO dataset information  ×
%                    ×            ISPF Libraries           ×
%                    ---------------------------------------
+
+  An%ISPF library+is a cataloged partitioned data set with a three-leve
   data set name of the form 'project.library.type'.

   To specify an ISPF library, enter the project, library, and type unde
   the heading:
  From ISPF Library:
          PROJECT%===>+
          LIBRARY%===>+
          TYPE   %===>+
          MEMBER %===>+

  %Note:+For convenience, any cataloged data set (partitioned or sequent
   with a three level name may be entered in this fashion, even if it is
   an ISPF library.  library.
                           (continued on next page)
)PROC
   &ZCONT = RBDT4110
   &ZUP = RBDT0000
)END
