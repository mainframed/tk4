%-------------------  FLIST SELECTION OPTION TUTORIAL PANEL  -----------
%
+  FLIST Dataset Selection  Options:
+
+  OPTION    Description
+
+  B         ISPF BROWSE the dataset
+  C         Compress the dataset
+  D         Delete and Uncatalog dataset (HDELETE if VOL=MIGRAT)
+  E         ISPF EDIT the dataset
+  EMPTY     Empty a dataset
+  HR        HSM RECALL the dataset
+  LISTSP    List dataset size and DCB
+  PRINT (P) Print dataset on hardcopy or Xerox
+  R         Restore an Archived dataset (HSM HRECOVER)
+  RENAME    Rename a dataset
+  T         Transfer dataset to another center (using BDT)
+  TRIM      Release unused space in a dataset
+  U         Uncatalog the dataset
+  X         Remove dataset from display table
+
)PROC
     &UP = TFLIST
)END
