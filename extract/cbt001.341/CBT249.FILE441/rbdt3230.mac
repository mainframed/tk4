%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    × Specifying FROM dataset information ×
%                    ×            ISPF Libraries           ×
%                    ---------------------------------------
+
  You will have to supply volume identification information if the datas
  not cataloged:

+If not cataloged:
+      VOLUME SERIAL %===>+
+   or VOL=REF DSNAME%===>+
+
   If you enter "other" data set name, you may optionally specify a volu
   serial or the name of a dataset that resides on the same volume. When
   either of these fields is used, the catalog will not be searched.

   Note that if you must specify either field, you must use the "other"
   set name field.
)PROC
 &ZUP = RBDT0000
)END
