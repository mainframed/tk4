%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×  Specifying information for a new   ×
%                    ×               dataset               ×
%                    ---------------------------------------
+
%NOTE+
  If the data set you are sending is cataloged at the center where you a
  logged on, the fields on this panel will be "preset" with the correspo
  values from that sending data set.


+DSORG           %===>+

  Specify PS for a sequential or PO for a partitioned dataset

+SPACE UNITS     %===>+          +(TRKS or CYLS)

  Allocation may be done in either tracks (TRKS) or cylinders (CYLS)
+
+                           (continued on the next page)
)PROC
 &ZUP = RBDT0000
 &ZCONT = RBDT4310
)END
