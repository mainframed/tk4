+--------------------% IPOUDTE Control Cards +--------------------------
+Command%===>_ZCMD
+
+The format of the control cards for IPOUPDTE is:
+
+stringa<stringb<stringc<
+
+where the # is a special character to delineate the strings
+
+where stringa will be replaced by stringb
+
+stringc is optional and further delimits the search/update to records w
+stringa is found with stringc.
+
)PROC
     &UP = TIPOUP
)END
