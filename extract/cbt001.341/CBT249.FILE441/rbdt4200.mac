%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×  Specifying TO dataset information  ×
%                    ×            "Other datasets"         ×
%                    ---------------------------------------
+
+  When you want to specify a data set that is not an ISPF library, ente
   it under the heading:

+To other partitioned or sequential data set:
+   DATA SET NAME %===>+
+
   You may enter any fully qualified data set name by enclosing it in
   apostrophes.  Example:  %DATA SET NAME ===> 'fully-qualified-dsname'+
   you omit the apostrophes, your TSO prefix is left-appended to the dat
   name.

   Whenever "other" data set name is entered, it is used even if an ISPF
   library is also entered.  The "other" data set name overrides.

                           (continued on next page)
)PROC
   &ZCONT = RBDT4210
   &ZUP = RBDT0000
)END
