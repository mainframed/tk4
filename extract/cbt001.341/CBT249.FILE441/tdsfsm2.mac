+-----------% Data Set and Free Space Manager 5796-PNN Tutorial +-------
%Command ===>_ZCMD
%
+Release Unused Space Processing
+
+    This option is used to release any allocated but unused space in a
+    to the free space pool.  The DADSM RELEASE function is used, so the
+    provision to release only a portion of the unused space.  If the da
+    meets the specified criteria, then all unused space is released.
+
+    AGE and JULIANAGE select datasets by age (days or older than x date
+
+    Partitioned is used to exclude PDS datasets.
+
+    Minrelease will selected only those datasets with more than x track
+    for processing.
+
+    Minsecondary specifies that only datasets with a secondary specific
+    of x tracks are to be processed.
+
+    INCLUDE is used to mask datasets to be processed: ALL, INCLUDE or E
)PROC
)END
