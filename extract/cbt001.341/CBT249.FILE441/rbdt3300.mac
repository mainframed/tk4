%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    × Specifying FROM dataset information ×
%                    × Specifying the dataset organization ×
%                    ---------------------------------------
+
+ If the dataset you are sending is not located at the center where you
+ logged on, or if it is at your logon center, but is not cataloged, you
+ be asked to supply the "dataset organization." Only two types are acce
+ PO for a partitioned dataset and PS for a sequential dataset.
+
+ If you have supplied a partitioned dsname with a member name, then the
+ dataset organization is derived as PO from that entry.
+
)PROC
   &ZUP = RBDT0000
)END
