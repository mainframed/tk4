%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×        Building a GMJD Member       ×
%                    ×             (continued)             ×
%                    ---------------------------------------
+
+ When the GMJD has been built, you will be given various options as to
+ disposition.
+
+ A member of GMJDLIB consists only of the FROM and TO information for a
+ transaction. Operands such as MESSAGE LEVEL and NET control commands
+ are appended to the TO information (where they are perfectly valid).
+ Overrides entered at Submit time for such fields may be entered normal
+ following the TO operator.
+
)PROC
 &ZUP = RBDT0000
)END
