%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    × Specifying FROM dataset information ×
%                    ×            ISPF Libraries           ×
%                    ×              (continued)            ×
%                    ---------------------------------------
+
   Generation data sets may be referenced by using a signed or unsigned
   number in place of a member name.  You may specify a generation data
   only under "other" data set name.  Example:

      %DATA SET NAME ===> gds.test(0)+     (Userid will be supplied)

   This example references the most recently allocated data set in the
   generation data group.  Minus numbers reference previously allocated
   sets and positive numbers reference unallocated data sets of the grou
+
+                        (continued on next page)
)PROC
 &ZUP = RBDT0000
 &ZCONT = RBDT3230
)END
