+-----------------%ISPFLIST COMMAND OPTION TUTORIAL PANEL +-------------
%
+  ISPFLIST Primary Command Line Options:
+
+  COMMAND   Description
+
+  ALLDA         Refresh with all Online and MIGRAT datasets
+  ALLOC         Allocate a New dataset
+  ARCHIVE       Display Archived Datasets
+  CONFIRM       Confirm Deletes prior to actual Delete
+  FIND=xxx      Find dataset xxx or dataset beginning with xxx* (or F=x
+  INDEX=xxxxx   Use new Index level and invoke REFRESH   (or L=xxx)
+  MIG           Refresh with only MIGRAT datasets
+  NOCONFIRM     Delete of dataset without confirmation
+  NOMIG         Refresh with no MIGRAT datasets
+  NOWAIT        Set the HSM Processing option to NOWAIT
+  REFRESH       Refresh the dataset list display
+  SECURE        Lock up your TSO terminal without Logging Off
+  TRANSFER      Transfer all datasets to another center using BDT
+  WAIT          Set the HSM Procssing option to WAIT
+
)PROC
     &CONT = $TFLIST2
)END
