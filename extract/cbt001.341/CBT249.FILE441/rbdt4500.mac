%TUTORIAL -------------  MVS Bulk Data Transfer Tutorial  ------------ T
+  Command%===>_ZCMD
%
%                    ---------------------------------------
%                    ×     Sending a transaction to the    ×
%                    ×           Internal Reader           ×
%                    ---------------------------------------
+
+                                    To Internal Reader:
                                         %===>+             (Y or blank)

  Specify "Y" to this option to send a JCL stream from the sending datas
  be executed at the receiving center. The transaction must be sequentia

  When this option is used, the MESSAGE LEVEL and NODE are required; the
  transaction name field is optional. All other fields are ignored.
+
)PROC
 &ZUP = RBDT0000
)END
