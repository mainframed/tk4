         MACRO
&LABEL   SRCHTYPE &R,&TYPEADD
&LABEL   LM    15,1,TCTYFST-TCINDX(&R) GET 1ST ADDR,LEN,LAST ADDR
         CLC   TYPETYPE-TCTYPE(,15),&TYPEADD    SEE IF MATCH
         BE    *+8                 IF SO,GET OUT
         BXLE  15,0,*-10           ELSE INCREMENT AND LOOP IF NOT OVER
*        NOTE THAT IF CONDITION 'E' THEN FOUND, PTR IN R15
*        IF 'NE' THEN NOT FOUND
         MEND
