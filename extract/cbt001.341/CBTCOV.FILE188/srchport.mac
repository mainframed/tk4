         MACRO
&LABEL   SRCHPORT &R,&PORTADD
&LABEL   LM    15,1,TCPTFST-TCINDX(&R) GET 1ST ADDR,LEN,LAST ADDR
         CLC   PORTNAME-TCPORT(,15),&PORTADD    SEE IF MATCH
         BE    *+8                 IF SO,GET OUT
         BXLE  15,0,*-10           ELSE INCREMENT AND LOOP IF NOT OVER
*        NOTE THAT IF CONDITION 'E' THEN FOUND, PTR IN R15
*        IF 'NE' THEN NOT FOUND
         MEND
