  DECLARE
    A#SORT
          ENTRY(
            FILE,                      /* DUMMY INTERFACE FILE     */
            CHAR(*) VARYING,           /* THE SORT STATEMENT        */
            CHAR(*) VARYING,           /* THE RECORD STATEMENT      */
            FIXED BINARY(31,0)         /* STORAGE AVAILABLE OR LEFT */
          )
          OPTIONS (ASSEMBLER INTER)
  ;
