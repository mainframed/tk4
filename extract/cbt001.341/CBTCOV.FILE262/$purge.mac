The $purge subcommand is designed for use when editing the msgclass
sysout for a batch job.  It will end editing of the msgclass sysout
data set, and will issue the tso delete command for the data set. The
command syntax is as follows:

$write  $purge

Do you want it executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd $purge
