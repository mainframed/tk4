The $edit subcommand is designed for use when editing the msgclass
sysout for a batch job.  It will first cause the currently edited data
set to be deleted, and will then display for editing data set
lastjcl.cntl.

 $write $EDIT

Do you want the command executed?  Enter null line for yes.
$input
$if answer=''
  $cmd $edit
