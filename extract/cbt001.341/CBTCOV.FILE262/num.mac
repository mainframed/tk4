If the data set currently being edited is an unnumbered data set,
issuing the num subcommand will cause it to become a numbered data set
when it is saved the next time.

The generated command is as follows:
 num
do you want it executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd num
