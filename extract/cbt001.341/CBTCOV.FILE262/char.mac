The char subcommand is used to return to normal character display mode
from hex display mode. The command syntax is as follows:

 char

do you want it executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd char
