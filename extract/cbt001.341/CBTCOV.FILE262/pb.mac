The pb subcommand is used to move the display one or more pages
backward.

Enter the number of pages to page back on the display, or enter a null
line to page back 1 page.
$input
$cmd setl L1 &ANSWER
The generated command is as follows:
  pb &L1
Do you want the command executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd pb &L1
