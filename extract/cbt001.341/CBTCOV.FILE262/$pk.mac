The $pk subcommand is designed for use when editing the msgclass
sysout for a batch job.  It will first issue the $prt subcommand to
cause the data set to be printed, and will then end editing of the
msgclass sysout data set.

$write $pk

Do you want the command executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd $pk
