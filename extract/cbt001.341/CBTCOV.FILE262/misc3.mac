$ misc2
This text will build a subcommand to delete all lines in a range that
contain only blanks.

$cmd aid lrange

The generated command is as follows:
  &L1 &L2 sno stack trim anchor len(0) rpos(0) = seq ' del'
Do you want the command executed?  Enter a null line for yes.
$input
$if answer=''
  $Cmd &L1 &L2 sno stack trim anchor len(0) rpos(0) = seq ' del'
