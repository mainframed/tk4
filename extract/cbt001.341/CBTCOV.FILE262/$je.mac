The $je subcommand is designed for use when editing the msgclass
sysout for a batch job.  It will locate message ief376i (job end),
which is normally the last message in jcl.  Using this subcommand, you
can quickly skip to output from your programs.

$write $je

Do you want the command executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd $je
