Use the verify subcommand to compare all data sets currently on the
display, beginning with the second line on the display for each data
set.  The comparision will stop whenever a line in one of the data
sets doesn't match the others.

The generated command appears as follows:
  verify
Do you want it executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd verify
