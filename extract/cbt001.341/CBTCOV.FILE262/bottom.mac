The bottom subcommand repositions the display so that the last line
of the data set is the last line on the display.
The command syntax is as follows:

 bottom

do you want it executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd bottom
