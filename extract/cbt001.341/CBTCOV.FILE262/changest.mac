$until answer^=''
  Enter the string specifying what is to be replaced.  Do not enter
  the string in apostrophes, even though it may contain apostrophes,
  blanks or other delimiters.  I will take care of that for you.
  $input
$cmd setquot ver

Enter the replacement string, which may be a null line.  Again, don't
enter the string in apostrophes.  I'll take care of it.
$input
$cmd setquot rep
