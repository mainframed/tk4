enter <l1> or null for default = t
$input
$if answer=''
  $cmd setl l1 t
$else
  $cmd setl l1 &ANSWER
enter <l2> or null for default = b
$input
$if answer=''
  $cmd setl l2 b
$else
  $cmd setl l2 &ANSWER
enter <incr> or null for default = 1
$input
$if answer=''
  $cmd setl incr 1
$else
  $cmd setl incr &ANSWER
enter <off> or null for default = 1
$input
$if answer=''
  $cmd setl off 1
$else
  $cmd setl off &ANSWER
enter <len> or null for default = 32767
$input
$if answer=''
  $cmd setl len 32767
$else
  $cmd setl len &ANSWER
enter <ncols> or null for default = 1
$input
$if answer=''
  $cmd setl ncols 1
$else
  $cmd setl ncols &ANSWER
