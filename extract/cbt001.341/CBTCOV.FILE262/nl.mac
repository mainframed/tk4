Use the NL subcommand to add a line of text to the data set.
The specified line of text will be added behind the current line,
which is the top line on the display.

Enter the text for the new line without surrounding quotes.
$input
$cmd setquot text
$cmd setl text &TEXT
The generated subcommand is as follows:

  nl &TEXT

Do you want the subcommand executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd nl &TEXT
