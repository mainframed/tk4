This text will build a subcommand to store incremental numbers in a
range of records.

$cmd aid lsu

The generated subcommand is as follows:

$write &LSU substr 1 incr

Do you want the subcommand executed?  Enter a null line for yes.
$input
$if answer=''
  $cmd &LSU substr 1 incr
