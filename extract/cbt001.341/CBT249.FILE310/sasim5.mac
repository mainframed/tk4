%TUTORIAL------------- SAS - SPF INTERACTIVE INTERFACE -----------------
%NEXT SELECTION ===>_OPT      +
+
% File statement (continued)
+        If  you use a FILE statement to write output of a SAS data grou
+     to  a  TSO  dataset,  enter  the file name after the "FILE==>"
+     keyword.   Put the name for the dataset after the "DATASET==>"
+     keyword.   If  the  dataset is new (this is the first time you
+     called  up  this  panel)  enter  in  a  NEW  for  the  dataset
+     disposition.   If  you  wish  to  replace an existing dataset,
+     specify  OLD  for  the  disposition.   To  add to the end of a
+     dataset,  use  a  disposition  of MOD.  For input or output to
+     come directly to the terminal, specify an asterisk (*) for the
+     dataset name.
+
+
+
+
%              (PRESS ENTER TO CONTINUE WITH THE TUTORIAL)
)PROC
  &CONT=SASIM6
)END
