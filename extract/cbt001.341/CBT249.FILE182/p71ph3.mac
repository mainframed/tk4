%TUTORIAL -----------  PDS Subcommand Checkpoint Panel  ---------------
%OPTION  ===>_ZCMD
%               ---------------------------------------------
                ×           Checkpoint Option               ×
                ---------------------------------------------
+

   A PDS subcommand has reached an output checkpoint (250 lines of data
   by default) and has been suspended so that you can examine the sessio
   log.  When you want to terminate the PDS subcommand in control, enter
   an END command.

   Note: PDS subcommands may%not+ be entered on the checkpoint panel.


   The following topics are presented in sequence or may be requested
   by number:
    %1+ General ISPMODE information
    %2+ F (find) log command
   The following optional topic may be requested by number:
    %3+ ISPF commands and PF keys
)PROC
  &ZSEL = TRANS(&ZCMD  1,P71PH3A   2,P71PH1B   3,*ISP01303  *,'?')
)END
