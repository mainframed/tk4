)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    Ö TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-ÖExtended Left/Right+-\-\-
%OPTION  ===>_ZCMD
+
% Extended Left/Right%===>+???+ (Yes/No)
+ Function prompt    %===>+   + (Yes/No) Ordered Pending     %===>+   +
+ Save in profile    %===>+   + (Yes/No)

%Extended Left/Right:+
   Normally, if the ISPF command table is setup by your installation,
   the LEFT AND RIGHT commands using PFK's will shift the display betwee
   the left and right normal displays (if defined for the given function

   Respond with%YES+will allow the LEFT and RIGHT commands to shift betw
   all defined displays for the given function including any dual line
   displays.
)END
