)ATTR DEFAULT(%+_)
    % TYPE(TEXT) INTENS(HIGH) COLOR(WHITE) SKIP(ON)
    ÷ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    + TYPE(TEXT) INTENS(LOW)  COLOR(TURQ) SKIP(ON)
    _ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED)
    ^ TYPE(INPUT) INTENS(HIGH) CAPS(ON) JUST(LEFT) COLOR(RED) PAD(_)
    | TYPE(INPUT) INTENS(HIGH) CAPS(OFF)
)BODY EXPAND(\\)
%TUTORIAL  +-\-\-÷PDS LISTV Primary Commands+-\-\-
%OPTION  ===>_ZCMD
+
   The following commands are supported as primary commands from the
   volume table display:
        %7+Output table   %-+output the LISTV table to print or a data s
        %8+View Log       %-+display the log table
        %9+Swap panels    %-+switch to an alternate display
        %C+Change         %-+change to another data set
        %F+Find           %-+position to a LISTV table entry
        %O+Options        %-+display the available options
        %S+Sort           %-+sort the volume table
        %X+Above/Below/All%-+clear the LISTV table relative to current p

       %LA+List allocation%-+display the list allocation table
       %LV+List volume    %-+prompt for list volume parameters
       %ML+Member list    %-+display the member table
 %FUNCTION+Function       %-+function status and selection panel
 %    QUIT+Terminate      %-+terminate÷PDS+

)END
