+%TSD Utilities panel functions:+(Continued)----------------------------
+ %COMPARE+- The two sequential files or PDS members selected by a%C+in
+            options list will be compared exhaustively and all differen
+            will be listed. For source files the last eight bytes will
+            compared, whilst for load modules the entire record will be
+%SUPERLIST+-Provides a full screen%EDIT+and%BROWSE+facility which displ
+            members%six+to a%line.+One can enter a command on the comma
+            or select a member directly by placing the option before th
+            name. Valid line commands are:
+                 %E+Member,%B+Member,%D+Member,%R+Member Newmember.
             representing, in order,%Edit, Browse, Delete,+and%Rename.+
               These options may also be placed%before+a%member name+in
            %list.+In the case of rename an%R+placed before the member n
             will cause the program to prompt you for the new name after
            %Enter Key+is hit. PFK%7/19+is scroll up and PFK%8/20+is scr
             down; A number (No. of lines) may simultaneously be placed
             command line, as may%M+(maximum). The default is one page.
             may also locate a member with%L NNN+where%NNN+is any valid
             generic member name. One other line command is%N+which simp
             rereads the directory; This command is useful in circumstan
             which arise through creating or deleting members indirectly
             through the%SPF Edit+or%Browse+screens.
+ Hit%ENTER+for the%next panel+or%Exit+to return
)PROC
 &UP   = TSDPUT11
 &CONT = TSDPUT15
)END
