)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

%                     +-------------------------------+
                      ×  INTERACTIVE    DISASSEMBLER  ×
                      ×       SECTION   LIST          ×
                      +-------------------------------+
+
   Once you leave the%"CSECT"+field in the entry panel blank, the Reasm
list panel appears.  You can use this panel to select name of the sectio
the previously chosen load module.  Section list is an ISPF table, which
contains names and attributes for all SD and LD names found. Each table
presents%CSECT+or%ENTRY+name.

   You may scroll table, sort it and choose name using line%"S"+command.
   %SORT A+ (SA)  This command sorts the table by offsets in ascending o
   %SORT N+ (SN)  This command sorts the table by names   in ascending o
   %L name+       This command locates the specified name by prefix.
                  (use the LOCATE command ONLY after the "SORT N" comman

)PROC
   &ZUP   = REA00000
)END
