)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

%                      'PRINT'  primary command
+                      ------------------------

   Primary command%PRINT+allows you print assembler-type listing of the
to the ISPF LIST data set.
   The format of PRINT command is:

  %P{RINT} {X{REF}}+

   If operand%XREF+specified then cross reference table will be printed.

   Once PRINT command is complete you may use ISPF LIST command to outpu
data set.

   Examples:
  %PRINT      +- Print the project listing
  %P X        +- Print the project listing and the cross reference table


)PROC
&ZUP   = REA00000
)END
