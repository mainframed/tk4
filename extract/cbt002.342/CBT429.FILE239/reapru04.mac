)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD
+

  -%Second+operand of the USING command defines the number of the base
register.  You may specify ANY number of register (from 0 to 15). Hopefu
register 0 should be used ONLY to base the predefined control block%@PSA

    You may type number of register as decimal digit or as combination%"

  -%Third+(optional) operand of USING command may define the displacemen
the project where the assembler USING command should be put. As the defa
will appear at the offset equal to the first operand.  Offset3 must be e
in hexadecimal, like Offset1.  Instead of typing Offset3 you may show th
necessary place by cursor. Assembler command USING will be put before th
where the cursor occurred.

)PROC
&ZUP   = REA00000
)END
