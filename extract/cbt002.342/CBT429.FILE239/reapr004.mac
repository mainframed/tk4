)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

+
  External data command SAVE allows you save the generated assembler tex
the project as the member of source library or sequential data set.

  The following topics are presented in sequence, or may be selected by

  %SAVE       + - Save assembler text

)PROC
   &ZSEL = TRANS(&ZCMD
                SAVE,        REAPRS00
                )
&ZUP   = REA00000
)END
