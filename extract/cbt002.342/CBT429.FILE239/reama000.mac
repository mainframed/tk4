)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

%                     +-------------------------------+
                      ×  INTERACTIVE    DISASSEMBLER  ×
                      ×          MARK  NAMES          ×
                      +-------------------------------+
+
  Reasm lets you mark points of project. Later you may "point" project t
desired offset using primary command%LOCATE+or through the mark table. O
entered primary command%MARK+ without operands the mark table appears on
screen.

  You can scroll mark table to find the appropriate mark name and then s
it by "S" at the first position. Project will be pointed to the correspo
offset.



  The following topic will be presented only if selected by number:
   %1+- Reasm primary commands

)PROC
   &ZSEL = TRANS(&ZCMD
                1,*REAPR000
                )

&ZUP   = REA00000
)END
