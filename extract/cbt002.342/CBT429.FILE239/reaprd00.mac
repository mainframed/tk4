)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

%                      'DROP'   primary command
+                      ------------------------

   Primary command%DROP+allows you delete from the project previously de
base register. The usage of command DROP%IS NOT+similar to the assembler
"DROP" command !  Command does NOT limit the register activity but erase
corresponding USING commands from the project text.

   Format of DROP primary command is:

  %D{ROP} Reg/ALL+

   If %Reg+specified then all the USING commands utilizing this register
be deleted from the project text.

   If operand%ALL+specified then ALL the USINGs will be deleted from the
project text.

                        ( continued on next page )

)PROC
&ZCONT = REAPRD01
&ZUP   = REA00000
)END
