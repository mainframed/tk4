)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

%                     +-------------------------------+
                      ×  INTERACTIVE    DISASSEMBLER  ×
                      ×     GENERAL  INTRODUCTION     ×
                      +-------------------------------+

+   This application is a powerful interactive tool to restore source as
  program from the MVS load module.

    Reasm allows you prepare high quality sources and provides a full se
  edit tools.

    Reasm provides a 'recovery' option which makes it possible to restor
  disassembling process from the 'hot point'.



                        ( continued on next page )

)PROC
&ZCONT = REAGE001
&ZUP   = REA00000
)END
