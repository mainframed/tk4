)ATTR DEFAULT(%+_)
       %   TYPE(TEXT)   INTENS(HIGH)
       +   TYPE(TEXT)   INTENS(LOW)
       $   TYPE(OUTPUT) INTENS(HIGH) COLOR(YELLOW)
)BODY
%TUTORIAL ------------------ $REASID   +$REASVR % -------------------- %
%OPTION  ===>_ZCMD

%                     +-------------------------------+
                      ×  INTERACTIVE    DISASSEMBLER  ×
                      ×      LABELS  REDEFINITION     ×
                      +-------------------------------+
 +
   You are able to overtype any label in the project. Once you did it al
references to the label will be automatically changed in all the command
the project. This feature usually increases the readability fantasticall

   You are able to erase any label. If there remained any references to
erased label Reasm will restore the label back.

   You may change the first sign of label to asterisk (*). This label wi
still exist in the project but all the references to it will be changed
nearest previous label plus offset. This facility allows you drop "junk"
(offsets within the logical block or so).


)PROC
&ZUP   = REA00000
)END
