)ATTR DEFAULT(%|_)
    ^ TYPE(TEXT) INTENS(LOW) COLOR(BLUE)
    @ TYPE(TEXT) INTENS(HIGH) COLOR(TURQ)
    _ TYPE(TEXT) INTENS(HIGH) COLOR(YELLOW)
    $ TYPE(TEXT) INTENS(HIGH) COLOR(WHITE)
    # TYPE(TEXT) INTENS(HIGH) COLOR(RED)
    % TYPE(INPUT) INTENS(HIGH) CAPS(ON)
)BODY
$-------- BUILDING DEPOSIT LAN TO CENTRAL ARCHIVE HOST RESTORE HELP PANE
*COMMAND$===>%ZCMD
^
   * example directory path entry:
     /MISTECH/MYFILE/LOTUS/       * NOTE: use forward slash
^
@  ENTER FILE NAME TO BE RESTORED ===>
^  * required field entry for option 1 file restore only - ( max length
   * example file name entries:
     1) USERFILE
     2) USERFILE.UA
     3) USERFILE.OUT

$  After all required screen fields data is entered, DEPRESS the ENTER K
   to continue to the the next selection panel.

#      E N D   O F   H E L P   P A N E L     ( PF3 = RETURN )
)PROC
)END
