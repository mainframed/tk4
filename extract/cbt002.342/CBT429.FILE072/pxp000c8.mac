%TUTORIAL --- PANEXEC- ELEMENT SELECTION LISTS - LINE COMMANDS ---------
%COMMAND ===>_ZCMD
+
   You can select one or more elements for processing from the element s
   list by entering any valid line command in front of each element name
   can use the cursor control keys to position the cursor on the desired
   Any of the functions described on the primary PANEXEC library utility
   may be used as line commands on an element selection list. Any line c
   entered, however, will be ignored if a primary command is entered.

   When an element is renamed (R), promoted (PM), copied (C), deleted (D
   has its mode modified (M) the element selection list is updated by ad
   and/or deleting the element name. The other statistics, however, are
   updated until the element selection list is re-displayed.

+                               ×%COMMAND ===>
+  The example at the right     ×        Fully Qualified Name  Coresize
+  shows how element PROD.ABC100×    Group    Element  Type SM Required
+  is printed, PROD.ABC150 is   ×%p +PROD    .ABC100  /EXEC;TA  0010408
+  deleted, PROD.QRS005 is      ×%d +PROD    .ABC150  /EXEC;TD  0000100
+  browsed, TEST.AAA100 is      ×%b +PROD    .QRS005  /EXEC;PA  0015000
+  renamed, TEST.AAA110 is      ×%r +TEST    .AAA100  /EXEC;TA  0000455
+  copied and TEST.AAA115 has   ×%c +TEST    .AAA110  /EXEC;TA  0000455
+  its mode modified.           ×%m +TEST    .AAA115  /EXEC;TA  0000455
)PROC
   &ZUP = pxp000c2
)END
