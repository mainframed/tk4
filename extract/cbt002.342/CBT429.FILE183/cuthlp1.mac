)BODY DEFAULT(%@_)
%TUTORIAL -------------------- EDIT - 'CUT' COMMAND -------------------
%COMMAND ===>_ZCMD

@  The%CUT@command is used to copy one or more lines of the data being e
   to a clip-board from which it can later be retrieved using the%PASTE@
   command.  The clip-boards are erased when you logoff.

   To specify the lines to be put into the clip-board, use line commands
  %CC@to copy line(s), or%M@or%MM@to move lines, or use%labels@.  If you
   not specify a line command or labels, then%ALL@of the lines are copie
   the clip-board.

   If you specify a%clip-board@name as part of the command, you can use
   clip-boards, each of which you can later refer to with the%PASTE@ com
   A clip-board names has the same format as a PDS member name.  If you
   a + sign instead of a clip-board name, the data is appended to the de
   clip-board.

        %COMMAND ===> cut                   @cuts the lines specified by
                                            @commands to default clip-bo

        %COMMAND ===> cut 2 .zf .zl         @cuts all lines to clip-boar

)END
