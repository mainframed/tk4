)BODY
%TUTORIAL ------------ TRANSMIT/RECEIVE : LOG PROCESSING  ------------ T
%COMMAND ===>_ZCMD
%
+ The LOG PROCESSING field provides the ability to view (via option B) o
+ (via option E) your LOG dataset. This dataset contains a record of all
+ TRANSMIT and RECEIVE activity. Log entries are chronologically arrange
+ transmission of data creates a time-stamped record in the LOG indicati
+ "what" was sent to "who".  Receipt of the data creates an "acknowledge
+ entry, thereby providing a convenient time-stamped cross-reference to
+ original transmission.
+
+
+ Data in this file may be freely edited (lines deleted, changed, etc.)
+ It is suggested that periodic "housecleaning" be performed so as to re
+ only needed information.
+
+
+Press%PF3+to exit this screen
)PROC
)END
