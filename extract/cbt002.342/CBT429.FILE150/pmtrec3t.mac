)BODY
%TUTORIAL -------------- RECEIVE : Restore Parameters ---------------- T
%COMMAND ===>_ZCMD
+
% Restore parameters+are:
+
%  DATASET+ You may specify the name of a dataset to contain the file be
+   received. This dataset will be created if it doesn't exist.  If you
+   that a dataset that was TRANSMITted to you matches a dataset name al
+   allocated to your userid, a new dataset name may be selected during
+   processing so as to prevent overlaying an existing dataset. When the
+   command prompts you with  "Enter restore parameters or 'DELETE' or '
+   enter:  DATASET(new.dataset.name).  By explicity specifying the DATA
+   keyword,  the contents of the dataset created will be identical to t
+   transmitted dataset with the exception of it's new dataset name.
+
+   A valid response to RECEIVE%restore parameters+is to press ENTER.  I
+   case, the dataset received will be the name of the transmitted data
+   you already have a dataset of this name and it is a sequential-type
+   then RECEIVE will prompt you for your permission to overwrite it. If
+   dataset is a partitioned-type dataset, identically named members wil
+   automatically replaced upon your permission.
+
% Tutorial continued on next screen ... press ENTER to continue.  +
+ Press%PF3+to exit this screen
)INIT
  &ZCONT = XMTREC4T
)PROC
)END
