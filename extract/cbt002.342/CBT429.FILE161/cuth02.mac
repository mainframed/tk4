%TUTORIAL ------------------ EDIT - 'PASTE' COMMAND -------------------
%COMMAND ===>_ZCMD
+
+  The%PASTE+command is used to copy one or more lines of data from a
   temporary location created by the%CUT+command to the data being edite

   To specify where the data is to be copied, use the%A+(after) or%B+(be
   line commands or specify%AFTER+or%BEFORE+as a parameter, followed by
  %label+name.  AFTER and BEFORE can be abbreviated %AFT+and%BEF+.

   If you specify a level number as part of the command, you can refer t
   any one of multiple temporary locations that may have been created by
  %CUT+command.  Level numbers are '1' thru '9'.  The default level is '

        %COMMAND ===> paste                 +copies the lines from tempo
                                            +location level '1' to a loc
                                            +specified by a line command

        %COMMAND ===> paste 2 before .zf    +copies the lines from tempo
                                            +location level '2' to the l
                                            +before the first line.
)PROC
   &ZUP = ISR2M600
   &ZCONT = ISR20000
)END
