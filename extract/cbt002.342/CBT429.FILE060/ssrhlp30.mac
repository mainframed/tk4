%TUTORIAL --- SOFTWARE STATUS REPORT - ADD PTF/MOD ENTRY --------------T
%COMMAND ===>_ZCMD
+
      This panel allows you to add new products and product maintenance
 onto the Software Status database. Up to 5 entries can be added at one
 from this panel.
      If you are adding a new product (PRODUCT NAME must be unique), ent
 under NEW PRODUCT, and enter (in order) ACRONYM (optional), RELEASE
 (optional), FMID (optional), ORDER number (optional), and PRODUCT NAME
 (required). PRODUCT NAME can be up to two lines of 32 characters each a
 entered under ACRONYM.
      If you are applying maintenance to an existing product, leave NEW
 column blank. Enter either the ACRONYM or the PRODUCT NAME. (NOTE: Sinc
 ACRONYMs need not be unique, you may not get the intended product when
 ACRONYM). If you are not sure of the ACRONYM or PRODUCT NAME, enter an
 an asterisk (*) in the ACRONYM or PRODUCT NAME field; you will be shown
 of existing products from which you may select.
      Upon pressing ENTER, you will see the most recent RELEASE, FMID, a
 ORDER for any previously existing products. You can change any field be
 pressing ENTER again.(NOTE: If you change PRODUCT NAME and the new NAME
 does not already exist, place a "Y" in the NEW PRODUCT column. This wil
 a new product to the database). Press ENTER again to to continue.
      END will cancel the addition of new entries.
)END
