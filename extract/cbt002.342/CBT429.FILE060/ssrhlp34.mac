%TUTORIAL --------SOFTWARE STATUS REPORT - PRODUCT HISTORY ------------T
%COMMAND ===>_ZCMD
+
      This panel displays detailed maintenance history entries for one p
 All fields are changeable by overtyping. Changing the VENDOR or CATEGOR
 will change VENDOR or CATEGORY for this product only (unlike the LIST P
 panel). Changing the DATE INSTALLED will cause the maintenance entries
 reordered on the screen, since they are listed in chronological order.
 Individual maintenance entries may be deleted, or the entire product an
 its maintenance entries can be deleted (DELETE PRODUCT).
      Eight maintenance entries are displayed on this panel. DOWN will e
 display the next eight maintenance entries for this same product, if th
 any, or display the next product within the current search argument. UP
 moves back to the first maintenance entry of the prior product within t
 current search argument.
)END
