         MACRO
&NAME    CHCKA2    &ADDR,&MODE
&NAME    TM        &ADDR,ARRAYELM      LAST ARG WAS ARRAY ELT ?    V1L4
         BNO       *+8
         LA        R1,4(,R1)
         CHCKA     &ADDR,&MODE
         MEND
