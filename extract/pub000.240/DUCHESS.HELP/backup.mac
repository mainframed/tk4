backup (n) -- Retract moves from game history

"backup n" retracts n half-moves from game history.
If "n" is not specified 2 is assumed.
Example:
The user types "QxP" and Duchess replies "PxQ".
Then "backup" or "backup 2" will retract the user move
and the computer reply.

See also: history
