The HELP PDS includes the following key members:

BASINFO    -- basic information (names, addresses, notices)
TAPE       -- how to extract data sets from tape
INSTALL    -- installing Duchess on a typical system
COMMANDS   -- typing commands to Duchess
NOTATION   -- notation for specifying moves to Duchess
RULES      -- the rules of chess
