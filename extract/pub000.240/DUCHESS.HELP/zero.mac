zero -- Remove all pieces from board

Useful before using "update" in setting up problem positions.
En passant status is cleared and castling is disabled
(there is no method at present for specifying these).
White is to move.
The game history is cleared.
