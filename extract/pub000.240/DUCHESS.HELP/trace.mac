trace (on off) -- Control tree search print out on SYSLIST

Not intended for general use.
"trace on" turns on tracing facility during search.
"trace off" cancels "trace on".

Note: Tracing generates several pages/second, so be careful.
