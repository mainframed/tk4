%TUTORIAL--------------  DSNLIST - ENTRY PANEL  -----------------------
%NEXT SELECTION ===>_ZCMD
+
      This panel is used to enter PDF Edit, Browse or the PDS command.

      This screen will maintain all the dataset names entered.  To selec
      particular dataset, simply enter its letter in the first field. If
      is a partitioned dataset and no member is specified, you'll be sho
      a member list.

     %Select Member:+is used to override the member specification of the
      first eight dataset names.  If it is%*+then the member list will b
      displayed regardless of the contents of%Member.+

)PROC
 &ZUP = DSNH
)END
