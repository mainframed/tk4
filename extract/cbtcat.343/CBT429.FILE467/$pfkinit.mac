%TUTORIAL------------------- ISPF/PDF COMMANDS -------------------------
%SELECTION ===>_ZCMD
%                     --------------------------------
                      ×    THE 'PFKINIT' COMMAND     ×
                      --------------------------------
+
     When creating an ISPF function, some vendors decide to set their
     function up to run with a new set of Pfkeys. One example is JESMAST
+
     To set Jesmaster's Pfkeys to look identical to your regular Pfkeys
     the rest of ISPF, use this command while under JESMASTER. It will h
     no affect under the rest of ISPF.

%SYNTAX:
     PFKINIT
)PROC
 &ZUP = $PFKINIT
)END
