%TUTORIAL --------------  PRODUCTION BATCH SYSOUT BROWSE  --------------
%COMMAND ===>_ZCMD
%
+This system is used to either:
+ 1) Retrieve a production jobs output from the DISKWTR's sysout dataset
+    allow a user to browse that output online.
+or
+ 2) If the user does not want to wait for a sysout retrieval, submit a
+    job which will select the job for printing.
+
% To use this panel , you must:
+
+    1. Enter%JOBNAME+ -  use an entire jobname for the%P+- print, or%S+
+                         option. The entire jobname can also be used fo
+                         list option or a generic name can be used. A g
+                         name consists of one or more characters follow
+                         an asterisk. (ie: &ZUSER.*  TM*  pmsds*)
+
+    2. Enter%JOB NUMBER+- The job number is used if a specified job is
+                          If the job number is not used, last job run m
+                          the jobname is selected. Job number is not us
+                          the%L+- list option is used.
+                          (Do not use a "J" in the job number)
+ Hit%ENTER+to obtain next help panel
)INIT
   &ZCONT = CMNFS00B
)END
