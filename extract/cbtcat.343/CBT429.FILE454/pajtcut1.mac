%Tutorial --------------------------  EDIT  ---------------------------
%COMMAND ===>_ZCMD
+
%                     -----------------------------------
                      ×        Edit Macro  "CUT"        ×
                      -----------------------------------
+
%1+-write lines from a file to the user profile POOL for later inclusion
    INSERT option.
   %SYNTAX :+CUT (default level is 1) or CUT LN (level-number)
   %RESTRICTION :+the level-number may range between 1 and 99 included o
%USE :+      Enter%CUT+on the command line and use the%C+or%M+line comma
             any form) to select the lines to be cut. If the%M+line comm
             used, the indicated lines are%deleted+(reset to blanks) aft
             have been copied. A level-number may be used to separate di
             levels of CUT. This way you can make many CUTs before you I
             If the level-number is omited, level 1 is assumed and anyth
             the level 1 will be overlayed.
             CUT%3+(or any number) will CUT the indicated line(s) into l
             (or level indicated) and can be INSERTed using the level-nu
+
                           (continued on next page)
+
)PROC
  &ZUP = PAJTCUT
  &ZCONT = PAJTCUT2
)END
