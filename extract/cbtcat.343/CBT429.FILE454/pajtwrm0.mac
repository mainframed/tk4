%Tutorial - WORM - A program for TSO 3270 terminals --------------------
%Method :+once the WORM is moving ten (10) TGETs are done one-tenth of a
          apart. If no input is received the WORM is moved one location
          current direction. When WORMomatic is active, only one (1) TGE
          done when in burst mode, or two (2) TGETs one-tenth of a secon
          otherwise. The current meaning of PF1 (displayed in the title
          and the move speed indicate the current AUTOMATIC/MANUAL/BURST
          status.
          An optional operand of one or two decimal digits may be suppli
          specifies a target upper limit to TASK-TYPE CPU time consumpti
          WORMomatic expressed in terms of percentage of elapsed time. T
          default of zero means that WORMomatic will not try to limit it
          service absorption rate.
+                          (continued on next page)
+Hit%ENTER+to go on to the next tutorial panel.
+Hit%END KEY+to return to the WORM invocation panel
+
)PROC
  &ZUP = PAJTWRM0
  &ZCONT = PAJTWRM1
)END
