)BODY Expand(//)
%Tutorial /-/  EDIT  /-/ Tutorial
%COMMAND ===>_ZCMD
+
%                     -----------------------------------
                      ×       Edit Macro  "BLOCK"       ×
                      -----------------------------------
+
%FUNCTION :+ This Edit macro may be used to create multiple block charac
             in your text (Super-Type Text macro).
%SYNTAX :+   BLOCK T=string of characters to generate after pointed line
            %Warning :+ If the string of characters must contain any com
                        delimiter character (special character for comma
                        stacking, see ISPF option 0.1), you must use the
                        way of entering the command, otherwise the strin
                        be truncated at first encountered delimiter char
                        and the rest will be handled as another command,
                        probably would be invalid.
             BLOCK or BLOCK T= and string of characters to generate read
                                   pointed line
             BLOCK HELP or H

                           (continued on next page)
+
)PROC
  &ZCONT = PAJTSTT2
)END
