)BODY Expand(//)
%Tutorial /-/  EDIT  /-/ Tutorial
%COMMAND ===>_ZCMD
+
%                       --------------------------------
                        ×      Edit Macro  "ZOOM"      ×
                        --------------------------------
+
%FUNCTION :+By placing the cursor on a JCL card containing an EXEC state
            and issueing the%ZOOM+command, this Edit macro will "zoom in
            (display in browse) the JCL PROC featured on the statement.
%SYNTAX :+  ZOOM
            ZOOM HELP or H to view this tutorial.
%NOTE :+    This command is most useful when defined to a PF key, thus l
            the cursor free to move to the line containing the EXEC stat
+
)PROC
  &ZCONT = PAJTZOOM
)END
