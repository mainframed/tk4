%Tutorial --------------------------  EDIT  ---------------------------
%COMMAND ===>_ZCMD
+
%                       --------------------------------
                        ×      Edit Macro  "PLUG"      ×
                        --------------------------------
+
%FUNCTION  :+This Edit macro may be used to%plug data+into a range of li
             starting at a given column.
%SYNTAX :   +On the COMMAND line type in :%PLUG STRING START-COL+
             and then mark the lines with the%On+or%OO+range command.
            +or :%PLUG HELP+or%H+to view this tutorial.
             If%START-COL+is not specified, the%cursor position+will be
%USE :+     %COMMAND ===> PLUG XDCCMD              +(cursor pos.)%---×+
            %Before+********************** TOP OF DATA *************%×+*
            %OO+001 ... data line ...                               (UXM
            +000002 ... data line ...                               (UXM
            %OO+003 ... data line ...                               (UXM
            %After+*********************** TOP OF DATA *****************
            %OO+001 ... data line ...                               (XDC
            +000002 ... data line ...                               (XDC
            %OO+003 ... data line ...                               (XDC
                                                 +(at end, cursor pos.)%
)PROC
  &ZCONT = PAJPLUG
)END
