)PANEL KEYLIST (ISRHLP2 ISR)
)ATTR
  Ö TYPE(TEXT)    INTENS(LOW)  COLOR(YELLOW)
  ! TYPE(TEXT)    INTENS(LOW)  COLOR(WHITE)
  + TYPE(TEXT)    INTENS(LOW)  COLOR(GREEN)
  @ TYPE(TEXT)    INTENS(LOW)  COLOR(TURQ)
  # AREA(SCRL) EXTEND(ON)
)BODY EXPAND(¼¼) DEFAULT($|_)
ÖMXI - HELP - Summary Of Changes ---------------------------------------
!Option ===>_ZCMD
#MXIHAREA
#
#
#
#
#
#
#
#
#
#
#
#
#
#
+
)AREA MXIHAREA
!Changes For Version 2.2a
Öo +Added an internal security table to control authorized commands.
Öo +Added the@JOB()+and@ASID()+keywords to the@CDE+command to allow list
Ö  +of the JPAQ and TCB modules for other address spaces.
Öo +Added the@JOB()+and@ASID()+keywords to the@TCB+command to allow list
Ö  +of the TCB structure of other address spaces.
Öo +Added the@CHP+command to list channel path information.
Öo +Added the@MAKE+command to change address space swapability.
Öo +Added the@/+command to issue operator commands.
Öo +Added the@ONLY(JPAQ)+keyword to the@CDE+command.
Öo +Changed the primary@DASD+command display to show Free DSCBs instead
Ö  +Free Extents
Öo +The@TYPE=DASD+and@TYPE=TASK+exception rules now support the use of
   +pattern masks for volsers and jobnames.
Öo +The@PRT+command will use USERID if the TSO prefix is null.
Öo +Added the OMVS and RTLS statements to the@PARM+command.
Öo +Fixed the@TAPE+command storage creep problem.

!Changes For Version 2.1e
Öo +Added the@AGRP+command to display SMS aggregate groups.
Öo +Added the@UCB+command to show the actual UCB addresses of all device
Öo +Added the@CDE+command to show the JPAQ and TCB loaded modules.
Öo +Added the@ONLY()+and@NOT()+keywords to the@DASD+command.
Öo +Added subsystem version information (via SSI-54) to the@SSI+command.
Öo +Added support for JES3.
Öo +Added the@UCB+map to the@MEM+command.
Öo +Changed the@SGRP+command to show all volumes defined to the SMS stor
Ö  +group when the more detailed display is shown.
Öo +Changed the@TAPE+command to accept unit address masking.
Öo +Changed the@SVC+command to accept masking.
Öo +Removed the authorized version of the@CAT+command.
Öo +The command parsing routines have been changed so that all commands
Ö  +accept keywords do NOT have to have the positional mask specified.
Öo +Added SMS Status to the DASD display for a single volume.
Öo +Improved authority checking when running under ISPF or REXX.
+
!Changes For Version 2.1d
Öo +Added the@SORTXA+and@SORTXD+commands to allow correct sorting of hex
Öo +Added the@JOB()+and@ASID()+keywords to the@DDNS+command to allow lis
Ö  +and searching of dataset allocations for other address spaces
Öo +Added the@A=asid+form of the@DA+command to allow specification of
Ö  +hex address space id. The ASID column in the@DA+command display is
Ö  +now point-and-shoot.
Öo +Added the@TCB+command to list TCB and RB structures
Öo +Added the@USP+command to list user subpool storage by TCB
Öo +Changed the@SRCH+command to show only one line per dataset if a matc
Ö  +found. Invoking the 'point-and-shoot' action on the dataset name wil
Ö  +show each individual member within that dataset.
Öo +Added the CPUS exception (TYPE=SYS) to examine number of online CPUs
Öo +Renamed the@SMF+command to@SMFD+
Öo +Changed the@SMF+command to list general SMF information
Öo +Added the@MAP+command to allow easy switching to DSECT mapping while
Ö  +the@MEM+display.
Öo +Changed the@EDT+command to show the allocated jobname and device typ
Ö  +each device
Öo +Changed the@DA+command to show CPU Time Limit value.
Öo +Adjust the TCB mapping when MAP(TCB) is used on the@MEM+command.
Öo +Added the@VER+command to list the MXI version and generation informa
Öo +The@ISPF Tab to point and shoot+setting is now toggled on for MXI if
Ö  +not set in the ISPF profile. The setting is reset (if changed) on ex
Ö  +from MXI.
Öo +Re-worked the HELP dialog.
Öo +Cater for null commands when using the MXIREXX API.
Öo +Allow MXIREXX API to accept new parameter 'NOTITLES'.
Öo +Allow MXIREXX API to accept authorized commands.
Öo +Fixed 0C6 abends when entering invalid format keywords for@SVC+and@M
Ö  +commands.
+
!Changes For Version 2.1c
Öo +Added the@EXC+command to list system exceptions.
Öo +Added CPU% and SIO to the@DA+command.
Öo @ENQC+command now lists both Enqueue conflicts and all RESERVEs.
Öo +Allow the@SRCH+command to be issued on the@PARM+display.
Öo +Adjust the CVT mapping when MAP(CVT) is used on the@MEM+command.
Öo +Allow the@MPF+command to recognise SUP(ALL) entries
Öo +Allow the NORMAL attribute to be used for point-and-shoot fields whe
+   the@SET SCREEN+command.
Öo +Ensure that all scrollable panels have 'Row n or n' placed in the to
+   corner.
Öo +Assign console key of 'MXI' to all E-MCS consoles and ensure that th
+   migration ID is always released.
+
!Changes For Version 2.1b
Öo +Added the@DA+command to list active address spaces.
Öo +Added the@RS+command to list real and auxiliary storage usage by add
   +space.
Öo +Added the@UIC+command to list UIC information by address space.
Öo +Added the@SGRP()+keyword to the@DASD+command to filter on SMS storag
Öo +Changed the@DASD+display for multiple volumes to include percentage
   +and mount attribute.
Öo +Changed the@DASD+display for a single volume to include EDT unitname
   +jobname, open DCBs, free VIRs and device capacity.
Öo +Changed the@TAPE+display for a single device to include EDT unitname
Öo +Changed the@PARM+command to show all PARMLIB datasets.
Öo +Changed the@DDNS+command to show EXCP counts for each dataset
Öo +Display routine now remembers sort order and current line when a
   +scrollable list is refreshed by pressing the enter key.
Öo +Added alternate display panel for displays that do not need the top
   +title lines.
+
!Changes For Version 2.1a
Öo +Added the@CS+command to list common storage usage by address space.
Öo +Added the@CSR+command to list common storage remaining (or orphaned)
   +address space.
Öo +Added the@CA1+command to display CA-1 configuration.
Öo +Added the@DCLS+command to display SMS data classes.
Öo +Added the@LLS+command to display linklist sets.
Öo +Added the@LLSU+command to display linklist set usage by address spac
Öo +Added the@CAX+keyword to the@CAT+command to force usage of the CAXWA
   +instead of using the E-MCS method.
Öo +Changed the@LINK+and@LPA+displays to include APF status and also the
   +serial when DSCB is OFF.
Öo +Added the MXICMDS ISPF table to be placed in ISPTLIB. This ensures t
   +commands are not mistaken for in-house commands.
Öo +Added support for MXI to be invoked via ISPF SELECT PGM(MXI) PARM(cm
+
!Changes For Version 1.3c
Öo +Changed main panel to use correct screen defaults.
Öo +Cater for shorter RIBE length in MVS Version 5 for@ENQ+and@ENQC+comm
Öo +Changed the@EDT+command to handle 4-digit devices and 31-bit UCBs.
Öo +Changed the@PARM+command OS-Level requirements to OS/390 1.2.
Öo +Cater for empty SYSIN when running in Batch mode.
Öo +Fixed 0C4 abend in the CF command when there is more than one coupli
Ö  +facility.
Öo +Fixed 0C4 abend during initialisation when RMF is not present.
Öo +Cater for ERBSMFI missing when using the@STOR+command.
+
!Changes For Version 1.3b
Öo +Added the@CF+command to show coupling facility information.
Öo +Added the@HSMQ+command to show DFHSM request queues.
Öo +Added the@NET+command to show VTAM major nodes.
Öo +Added the@SCLS+command to display SMS storage classes.
Öo +Added the@MCLS+command to display SMS management classes.
Öo +Changed the@EDT+command to cater for dynamic UCBs.
Öo +Removed the CAXWA logic from the@CAT+command.
Öo +Changed the@SMS+display to scrollable.
Öo +Make all command names on the main panel 'point-and-shoot'.
Öo +Fixed IPL prose date display problems.
Öo +Changed the@CPU+command to handle more than 999Mb of real/expanded s
+
!Changes For Version 1.3a
Öo +Added the@MAP()+and@LEN()+keywords to the@MEM+command.
Öo +Added the@JOB() MAJ()+and@SYS()+keywords to the@ENQ+command.
Öo +Added the@DSCB()+keyword to the@LINK LPA+and@APF+commands.
Öo +Added the@SGRP+command to display SMS storage groups.
Öo +Added the@SMS+command to display SMS configuration.
Öo +Added the@SMSM+command to display SMS modules.
Öo +Added the@SPD+command to display subpool descriptions.
Öo +Changed the@SP+command to show detail to the block level if requeste
Öo +Changed the@SRCH+command to bypass VIO datasets.
Öo +Point-and-shoot fields can now be selected using the TAB key.
Öo +Cater for increased answer area size for JES2 SSI-54 call.
Öo +Standardize date and time formats (yyyy/mm/dd hh.mm.ss) and add Year
Ö  +support.
Öo +Changed the@SET+command syntax to replace the old DSCB, ENQJ, ENQM a
Ö  +commands.
Öo +REXX API now uses IRXEXCOM instead of IKJCT441.
+
)INIT
.ATTRCHAR(Ö)  = 'COLOR(&M7CC) HILITE(&M7CH) INTENS(&M7CI)'
.ATTRCHAR(!)  = 'COLOR(&M08C) HILITE(&M08H) INTENS(&M08I)'
.ATTRCHAR(+)  = 'COLOR(&M06C) HILITE(&M06H) INTENS(&M06I)'
.ATTRCHAR(@)  = 'COLOR(&M09C) HILITE(&M09H) INTENS(&M09I)'
)PROC
 &ZUP   = MXIHTOC
 &ZCONT = MXIHTOC
)END
