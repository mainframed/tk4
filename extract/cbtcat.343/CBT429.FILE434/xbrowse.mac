PROC 1 DSN VOL()
/*                           */
/* AUTHOR: MARK ZELDEN       */
/*                           */
 CONTROL NOLIST NOPROMPT
ISPEXEC CONTROL ERRORS RETURN
ISPEXEC BROWSE DATASET(&DSN) VOLUME(&VOL)
      IF &LASTCC ^= 0 THEN ISPEXEC SETMSG MSG(&ZERRMSG)
END
