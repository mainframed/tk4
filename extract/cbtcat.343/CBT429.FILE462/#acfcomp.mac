)ATTR
  _ TYPE(input) INTENS(HIGH) COLOR(RED)
  ! TYPE(TEXT) INTENS(low) COLOR(GREEN)
  + TYPE(TEXT) INTENS(low) COLOR(blue)
  $ TYPE(TEXT) INTENS(high) COLOR(white)
)body
+                    ---> $ACFCOMP: EDIT MACRO HELP  +<---
+
$Syntax: !Enter ACFCOMP on the EDIT command line when you have completed
           an ACF2 rule source PDS member.
!
$Use:    !This macro will invoke ACF2 and automatically compile and stor
           rule you were editing. ACF2 displays the complilation process
           user is returned to the EDIT display panel.
!
$Note:   !This macro is for use by H.I.S. ACF2 division security officer
           ONLY!!

!
!
!
!
!
!
+Press$ENTER+to return to Tutorial menu, or$PF3+to return from whence yo
)INIT
)PROC
  &ZUP = EMHIS000
)END
