         MACRO -- $DOM -- DELETE OPERATOR MESSAGE
&N       $DOM  &CMB=(R1)
&N       $DECODE &CMB
         L     R15,$DOM            POINT TO $DOM ROUTINE
         BALR  LINK,R15            ENTER IT
         MEND
