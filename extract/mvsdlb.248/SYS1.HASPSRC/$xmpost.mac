         MACRO -- $XMPOST -- BRANCH ENTRY CROSS MEMORY POST ROUTINE
&NAME    $XMPOST &SJB
&NAME    $DECODE &SJB
         L     R15,$XMPOST         GET XM POST ROUTINE ADDRESS
         BALR  LINK,R15             AND LINK TO IT
         MEND
