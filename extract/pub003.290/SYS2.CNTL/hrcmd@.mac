---------------- BSPHRCMD - Run HERCULES command from Batch ---------
*
This program executes HERCULES commands in batch.  The commands can be
passed via PARM statement, or via SYSIN DD statement, or both
*
Required DD statements:

   none

Optional DD statements

   SYSPRINT  for messages
   HRCPRINT  for replies from Hercules

Sample JCL can be found in SYS2.CNTL(HRCMD#)
